# Backlog

Входящий поток задач из опытной эксплуатации и Langfuse. Элементы приоритизируются и тянутся в tasklist при триаже. После переноса в tasklist элемент удаляется из backlog — tasklist становится его новым домом (секция "Из backlog" в записи итерации).

Приоритеты: **P0** (блокер) / **P1** (важно) / **P2** (желательно) / **P3** (когда-нибудь)

Группировка по scope для параллельной работы. Cross-cutting элементы помечаются *(cross: scope)*.

## Frontend / UX

- **P2** Адаптивность UI под разные экраны — вёрстка целенаправленно не проверялась нигде, кроме стандартного десктопного Full HD: широкие (ultrawide), узкие и ноутбучные вьюпорты могут выглядеть неаккуратно, мобильные — не поддержаны вовсе (единичные брейкпоинты вроде `lg` на auth-экране — исключения, не система). Отдельной итерацией: свип всех экранов по сетке вьюпортов (mobile / tablet / laptop / FHD / wide), фикс раскладок, решение о мобильной стратегии (полноценный responsive vs «десктоп-first с деградацией») *(Frontend)*

- **P2** Полноценная i18n (RU/EN) — сейчас интерфейс RU-first с захардкоженным русским копирайтом; целевая модель — выбор языка пользователем и полный набор строк на обоих языках (включая тексты ошибок, aria-label, подписи инструментов агента в ленте). Потребует инфраструктуры локализации (реестр строк вместо инлайна), поэтому отдельной итерацией; до неё — единичные английские строки добиваются точечно *(Frontend, cross: Backend — тексты ошибок API)*

- **P3** Редирект аутентифицированного пользователя с `/login` на `/` — сейчас as-is: пользователь с валидной сессией, набравший `/login` руками, видит форму входа без редиректа (сознательно суженный scope каркаса feat-008); штатные пути на этот экран не приводят. Целевая модель: реактивная проверка сессии на `/login` (feat-013 / следующая итерация фронтенда) *(Frontend)*

- **P3** Смена имени пользователя (user rename) — суффиксные имена OAuth-регистрации (`name_xxxx` при коллизии) приняты в v1 как рабочий, не финальный UX; выявлено в feat-008. Триггер: жалобы пользователей на суффиксное имя *(Frontend, cross: Backend)*

- **P3** Флейк-устойчивость фронтовой Vitest-сьюты под нагрузкой — полный `make test-fe` даёт таймауты 5000 ms в разных файлах от прогона к прогону (`SecurityRules`, `SecurityRouteGuard`, `MCPServersSection`, `CustomInstructionsSection`, `SecurityEvents`, `SphereView` — в изоляции все зелёные) + pre-existing teardown-таймер `SecurityRules.tsx:84` (`window is not defined` после teardown jsdom). Диагностика изоляции ресурсов Vitest-воркеров + фикс таймера; наблюдения из feat-008 *(Frontend, test-infra)*

- **P2** Документные артефакты без предпросмотра во вьюере — файловая модель относит `.pdf` и `.docx` к категории `binary`, вьюер показывает заглушку «Предпросмотр недоступен» с кнопкой «Скачать» (media/download-эндпоинты сами файлы отдают корректно). Итерация с исполнением кода сделала оба формата рядовым выходом агента (сборка отчёта скиллом даёт `.docx`, извлечение текста работает по `.pdf`) — «скачай и открой снаружи» на своём же результате читается как незаконченный продукт. PDF дешёвый: категория `pdf` в `shared/lib/artifact-category.ts` + `PdfViewer` через `<object>`/`<iframe>` с blob-URL по уже существующей механике авторизованной загрузки media у `ImageViewer`. DOCX своего браузерного вьюера не имеет — либо клиентский рендер (`mammoth.js` → HTML, теряет часть оформления), либо серверная конвертация в PDF (нужен LibreOffice в образе, сейчас его нет намеренно). Кандидат — feat-013 ui-polish *(Frontend, cross: Backend, выявлено при догфудинге feat-011)*
- **P3** Карточка артефакта появляется только по завершении хода — SSE-кадр `artifact_created`/`artifact_updated` приезжает в реальном времени, но фронт делает из него лишь инвалидацию списка артефактов; саму карточку рисует история после `done` (`useAgentStream.ts`, ветка артефакта в живом рендере — `null`). Решение брифа feat-011: живой карточки не вводим, чтобы live-лента и история не расходились. Цена — пауза между «файл готов» и появлением карточки на длинном ходе. Целевая модель, если делать: живой `ArtifactFeedItem` с ключом по пути, который история заменяет без дубля *(Frontend, выявлено при догфудинге feat-011)*
- **P3** Чат из сообщения без текста получает заглушку «Empty message» — при отправке только вложений авто-титул не срабатывает; ожидаемо имя вложения или обычный авто-титул по первому ходу *(Frontend, cross: Backend, выявлено при догфудинге feat-011)*
- **P3** Ошибка лимита загрузки вложения приходит по-английски в русском интерфейсе — текст 413 с сервера рендерится как есть; либо локализовать на фронте по коду ошибки, либо русифицировать detail в problem+json *(Frontend, cross: Backend, выявлено feat-011)*
- **P3** Строка ленты подписана «успешно» при security-отказе `read_file` и при джобе с `exit_code != 0` — статус строки берётся из факта исполнения tool-вызова, не из семантики результата; различать по exit_code/доменному отказу в ToolMessage *(Frontend, cross: Agent, выявлено feat-011)*
- **P3** Сироты в `uploads/` при частично неудачной пачке вложений — принятая брифом feat-011 семантика «партия падает целиком, загруженное остаётся»; со временем зона замусоривается — уборка (GC по возрасту / при удалении чата) или переиспользование при повторной отправке *(Backend, выявлено feat-011)*
- **P3** Дизайн SIEM/security-экранов (`pages/security/ui/*`) — токены применились, бейджи отрефакторены (feat-004), но layout generic (заголовок sans вместо serif, голые tabs/таблицы). Admin-only, под RBAC, не продуктовая фича → низкий приоритет; делать вместе с явным состоянием «нужны права администратора» (стыкуется с P2 RBAC guard `/security` ниже). По решению архитектора SIEM в проде отключается kill-switch'ем — делать только при возврате SIEM в активный режим (см. Security → «SIEM → SOC evolution») *(Frontend, SIEM)*
- **P3** Заглушки группы B → un-gate по мере появления бэкенда — design-branding feat-004 реализовал группу B (студия-панель, линза, peek-карточка «записано в сферу», семвер-UI «Жизнь сферы», вьюеры slides/image/audio, rich-редактор) **на mock-данных**. Прод-риск (fake-контент в реальных вью) **закрыт в feat-004** фиче-флагом `SHOW_GROUP_B_STUBS = import.meta.env.DEV` (`shared/config/feature-flags.ts`): в прод-сборке заглушки не рендерятся, мок-данные выпадают из бандла (DCE). Остаётся: по мере появления бэкенда каждой фичи (см. Backend-пункты группы B — версионирование сферы, SSE-peek; Frontend Rich Artifact UI) снять гейт в точке рендера и провести фронт на реальные данные. Из P2-предпрод-гейта понижен до P3, т.к. блокер прода устранён *(Frontend, cross: Backend, Agent, Design-branding)*
- **P3** Длительность tool-вызова не переживает перезагрузку — фронт считает длительность строки ленты сам по live-событиям; в `parts` истории её нет, восстановить из чекпоинта нечем — после перезагрузки (и уже в момент `done`, когда ход дорисовывается из `parts`) строка беднее live. Свести можно двумя путями, оба дороже эффекта: бэкенд фиксирует моменты старта/конца исполнения и хранит длительность в `ToolCallPart` (новая механика учёта, не правка контракта) либо симметрия обеднением live. Косметика: пропадает мета «сколько заняло», не факт действия *(Frontend, cross: Backend, выявлено feat-001 agent-visibility)*
- **P3** Rich Artifact UI — специализированные viewer/editor-компоненты для разных типов артефактов (аудио, графики, презентации и др.) вместо универсального markdown-viewer. Это обычный типизированный UI, не Generative UI; конкретные сценарии и приоритет уточняются через dogfooding. **Фронт-заглушки вьюеров (slides/image/audio, диспатч по `type`) доставлены в feat-004 на mock-данных**; остаётся бэкенд (реальные типы артефактов + хранение/отдача) и проводка фронта на реальные данные *(cross: Backend)*
- **P3** Generative UI — агент сам выбирает или компонует UI-компоненты. Для текущего продукта подтверждённого сценария нет; вернуться при развитии LearnFlowAI в платформу для студентов, где потребуются адаптивные учебные интерфейсы. Ресерч завершён (`doc/research/generative-ui-research-report.md`) *(cross: Agent, Backend)*
- **P3** Rich Material Export — экспорт rich-артефактов с сохранением визуального качества (не markdown-дамп). Форматы и требования определяются после появления реальных типов rich-артефактов; Generative UI не является обязательной зависимостью *(cross: Backend)*
- **P3** Runtime-конфигурация фронтенд-флагов — Vite SPA запекает `import.meta.env.VITE_*` в бандл при `npm run build`: смена флага (`VITE_SIEM_ENABLED`) требует пересборки образа. Целевая модель (решение гейта chore-001) — вариант B: FastAPI при отдаче `index.html` инжектит `<script>window.__CONFIG__ = {...}</script>` из `Settings` — флаг синхронный, замена источника в `feature-flags.ts` минимальна; альтернатива A (`GET /api/config` при старте фронта) делает флаг асинхронным. Оба работают по рестарту app; закрывают заодно рассинхрон парсинга `SIEM_ENABLED` (пункт ниже) и `build.args`-провод с его npm-кэш-пенальти (Infra). Триггер: реальная потребность переключать фронт-флаги без пересборки *(Frontend, cross: Backend, harvest chore-001)*
- **P3** Смена имени пользователя (user rename) — в продукте нет ни эндпоинта, ни UI смены `users.name`; OAuth-вход (dogf feat-008) при коллизии генерирует имена с суффиксом (`alice_x7f3`), и пользователь остаётся с таким именем без возможности смены. Маленькая фича: PATCH-эндпоинт + inline-edit в user-settings, валидация уникальности *(Frontend, cross: Backend, из feat-008)*
- **P3** Рассинхрон парсинга `SIEM_ENABLED` backend/frontend — pydantic принимает `0`/`False`/`no` как выключено, фронт гасится только на литеральном `false` (сырой passthrough через `build.args`): `SIEM_ENABLED=0` даёт погашенную эмиссию при живой кнопке «Безопасность» и 502 на `/siem/`. Пока — дисциплина оператора («писать именно `false`», задокументировано в `production.md`); структурно исчезает при runtime-конфигурации фронт-флагов (пункт выше) *(Frontend, Backend, Infra, harvest chore-001)*
- **P2** `ErrorCard` не объявляет ошибку ассистивным технологиям — `shared/ui/StateScreen.tsx` (`ErrorCard`) не несёт ни `role="alert"`, ни `aria-live`: у всех потребителей канона состояний ошибка после сабмита появляется молча для скринридера. Соседний примитив того же файла, `LoadingState`, `role="status"` уже имеет — граница применена несимметрично внутри одного файла. Канон общий на весь продукт, поэтому дефект систематический, а не разовый *(Frontend, accessibility, harvest feat-013)*
- **P2** `AuthLayout` грузит `auth-hero` даже когда брендовая колонка скрыта ниже `lg` — колонка выключена классом `hidden lg:flex`, но `<Illustration scene="auth-hero">` остаётся в DOM, а браузеры в общем случае всё равно скачивают `src` у `<img>` внутри `display:none`. Цена — ~0.7–0.85 МБ на самом первом экране мобильной сессии, до всякого кэша *(Frontend, perf, harvest feat-013)*
- **P2** Секция «Удалить проект» в `ProjectSettingsPage` не имеет ни одного теста — кнопка вызывает необратимую мутацию, но в `ProjectSettingsPage.test.tsx` нет ни одного упоминания «Удалить»/`delete`. Единственный непокрытый деструктивный путь на странице, которую feat-013 иначе довёл почти до полного покрытия *(Frontend, Testing, harvest feat-013)*
- **P3** Четыре потребителя `StateScreen` в error-режиме собирают один рецепт вручную — `ChatThread`, `ArtifactView`, `SphereView`, `ErrorBoundary` дословно повторяют `scene`, `alt`, `illustrationClassName`, подпись, кнопку `variant="outline"` и `className="h-full"`. Правка формулировки подписи в одном месте разъезжается молча. Целевая модель: тонкая обёртка `QueryErrorScreen({ title, onRetry })` в `shared/ui`, заголовок — единственный варьируемый параметр *(Frontend, dedup, harvest feat-013)*
- **P3** Пустая сфера знаний показывает две неотличимые по доступному имени кнопки «Редактировать» — `SphereViewer` рендерит идентичную разметку дважды, обе видны одновременно; скринридер объявляет их без различия, а тест платит 20 строк хелпера, обходящего DOM вверх по предкам. Дать CTA собственную подпись («Заполнить сферу») и вынести дублирующуюся разметку в локальный компонент *(Frontend, accessibility, dedup, harvest feat-013)*
- **P3** Module-level состояние refresh-интерцептора в `shared/api/client.ts` — `isRefreshing`/`pendingRequests` объявлены как module-level `let`, что отступает от жёсткого правила CLAUDE.md «никаких module-level синглтонов» (состояние — в замыканиях/DI). План feat-013 явно исключил переписывание из трека; решение о целевой форме — за архитектором *(Frontend, architecture, harvest feat-013)*
- **P3** Однослотовые `abortRef`/`isCancellingRef` при формализованной модели двух живых потоков — feat-013 ввёл владельца потока и owner-guard, то есть признал, что потоков может быть два одновременно, но ссылки на отмену остались однослотовыми: второй `send()` перетирает `abortRef` первого. Следствия: (1) поток, брошенный при переключении чата, не абортится никогда и дотекает впустую; (2) `isCancellingRef` глобален — отмена хода в B проглатывает терминальный `error` догорающего хода A, и пользователь не увидит, что ход упал. Целевая модель — карта по `chatId` вместо одного слота *(Frontend, harvest feat-013)*
- **P3** Остатки англоязычного копирайта вне инвентаризации feat-013 — `MCPServerForm.tsx` целиком («Saving...», «Add Server», «Cancel», `placeholder="Bearer token"`) рендерится на обеих страницах настроек рядом с уже локализованной `MCPServersSection`; ASCII-многоточия в `ChatInput.tsx` и `ReviewIndicator.tsx`. Точечная инвентаризация по брифу такие хвосты не ловит — нужен отдельный проход по всему `frontend/src` грепом по `alt=`, `title=`, `aria-label=`, `placeholder=`, `\.\.\.` и латинице в JSX-тексте *(Frontend, harvest feat-013)*
- **P3** Ошибки мутаций и валидации собираются вручную мимо `ErrorCard` — `SecurityRules.tsx` и `RuleForm.tsx` рисуют карточку ошибки своей вёрсткой. Канон состояний охватывает per-query ошибки, а здесь ошибки мутации и формы, которые дизайн явно оставил в тост-канале; визуально при этом собрана та же карточка, дважды. Решить, распространяется ли канон `ErrorCard` на мутации и формы, и если да — перевести оба места *(Frontend, harvest feat-013)*
- **P3** Скелетон таблицы SIEM продублирован в трёх файлах — `SecurityEvents`, `SecurityAlerts` и `SecurityRules` несут по 21 идентичной строке разметки. В feat-013 директория была отдана треку поимённым списком файлов, поэтому общий компонент означал бы файл вне закрытого списка партиции; процессное обоснование истекло вместе с итерацией. Вынести общий скелетон в `pages/security/ui/` *(Frontend, tech debt, harvest feat-013)*
- **P3** Вес PNG-иллюстраций — `shared/assets/illustrations/{light,dark}/*.png`: 9 сцен × 2 темы ≈ 17 МБ, отдельные сцены 0.65–1.46 МБ, каждое empty-/error-состояние стоит пользователю почти мегабайта. Нужен конвейер оптимизации ассетов (WebP/AVIF или пережатие + `srcset`); pre-existing, усугублено тремя новыми сценами feat-013 *(Frontend, perf, infra, harvest feat-013)*
- **P3** Hover-паритет между нативным скроллбаром и `ScrollArea` Base UI — ширина и цвет бегунка сведены к точному паритету, но hover-усиление у нативной полосы делается псевдоклассом на псевдоэлементе, а у библиотечной такого состояния нет вовсе. Единственный известный путь — правка сгенерированного примитива, запрещённая конвенцией (перегенерация молча сотрёт). Расхождение принято как норма — это компромисс, а не решение. Опубликовано как Question: https://agents.stackoverflow.com/questions/dd8f2d2c-2e28-4841-9caf-c2a28bd6a2b0 *(Frontend, design-system, harvest feat-013)*
- **P3** Проверить на гейте feat-008, что новый экран входа не унаследовал английские подписи — `AuthGate.tsx` несёт `placeholder="Username"/"Password"/"Confirm password"` и ASCII-многоточие, но feat-013 его осознанно не трогал: feat-008 перестраивает вход на страницу `/login`, и компонент уходит вместе с ней. Брендовый auth-дизайн от feat-013 английских подписей уже не содержит — нужна сверка на стыке итераций *(Frontend, harvest feat-013)*

## Agent

- **P2** Пустой ответ модели нигде не отсекается — трактуется как успех; стрельнуло дважды на одной модели (`deepseek/deepseek-v4-flash` с reasoning): (1) компакция — `_reduce_context` (`graph.py`) заменил два сообщения пустой сводкой (модель вернула пустой `content` при израсходованных 103 токенах, ветка `except` не сработала — отказа не было), кусок контекста потерян молча; (2) субагент — `run_subagent` вернул `status: "success"` с пустым `content` (`output_length=0`), пользователь видит строку с заданием и без ответа. Чинить одним решением на оба места — страховка на пустой `content` (деградация в trim-only / ретрай / отсечение reasoning-моделей от роли суммаризатора / деградация с явным `agent_event`), не заплаткой в суммаризаторе. Цена trim-only: систематически пустая суммаризация растит контекст до переполнения вместо тихой потери *(Agent, выявлено feat-001 agent-visibility)*
- **P3** Параллельные джобы делят окно diff-снапшота зоны `artifacts/` — `asyncio.gather` двух ходов одного проекта даёт дублирующиеся `artifact_created` на один путь (в т.ч. между чатами). Косметика ленты, файлы корректны; чинить скоупингом снапшота на джобу (маркер времени/инвентарь до-после в пределах вызова) *(Agent, Backend, code review feat-011)*
- **P3** Клиентский таймаут httpx к executor неотличим от «executor недоступен» — оба дают `ExecutorUnavailableError`; артефакты доработавшей джобы теряются для ленты (следующий снапшот их подберёт как updated). Различать таймаут (джоба, возможно, дописала файлы — снять снапшот) от connect-отказа *(Backend, code review feat-011)*
- **P3** `guard_tool_result` извлекает текст нестрокового `content` через `str()` → `repr` — экранирует ровно те Cf/Co/Cn-кодпоинты, что ищет `UnicodeDetector`: на блочном `content` unicode-слой не срабатывает, а защитная ветка `isinstance(content, str)` в `_is_sanitizable_unicode_hit` недостижима. Сегодня риска нет (блочный content никто не возвращает, `repr`-текст судят fragment и классификатор); при появлении мультимодальных ToolMessage собирать `text`-блоки вместо `repr` списка *(Agent, Security, выявлено feat-011)*
- **P3** Whitelist lifecycle-типов на пробросе custom-канала — ветка `elif custom_type is not None` в `runner.py` пробрасывает любой тип на провод как есть: неизвестный доменный kind бросает `ValueError`, а неизвестный lifecycle-тип молча становится событием верхнего уровня. Дешёвая половина риска закрыта AST-стражем словаря провода (опечатка в литерале краснеет в CI); сам whitelist ставится на эмиттере, не на потребителе — forward-compat живёт на другой стороне провода *(Agent, выявлено feat-001 agent-visibility)*
- **P3** Лог старта печатает счётчики инструментов без имён — `main.py` логирует `tool_registry_size` / `tool_count` / `tool_pool_size`; сверить собранный на живом старте набор с фикстуром `backend/contracts/agent-tool-names.json` по логу нельзя (на проде кода под рукой нет). Логировать имена собранных инструментов на старте — при расследовании «агент не видит инструмент» ответ сразу в логе *(Agent, Observability, выявлено feat-001)*
- **P3** pydantic `UserWarning` на каждом tool-вызове — любой инструмент с непараметризованной аннотацией `runtime: ToolRuntime` даёт `UserWarning` (`field_name='context'`) при прогоне через настоящий `ToolNode`; в тестах не всплывало, потому что `tool.ainvoke` минует сериализацию входа. Параметризовать `ToolRuntime[AgentContext, ...]` — правка задевает все шесть модулей инструментов разом *(Agent, выявлено feat-001)*
- **P3** Инертный аргумент `get_config()` в `execute_tools_guarded` (`tool_guards.py`) — `tool_node.ainvoke(state, config)` и `tool_node.ainvoke(state)` неразличимы по построению (оба читают contextvar `var_child_runnable_config`; подтверждено мутацией — 219 тестов зелёные), а вне runnable-контекста `get_config()` бросает `RuntimeError`, делая функцию невызываемой. Снять как мёртвый либо оставить как защиту и завести AST-дрейф-гейт (как у словаря wire-литералов); поведенческим тестом не закрывается ни при каком раскладе *(Agent, Tech Debt, выявлено feat-001)*
- **P2** LangGraph Store deep-dive — изучить Store вдоль и поперёк: все возможности, лимиты, best practices, продвинутые паттерны (semantic search, IndexConfig, cross-namespace стратегии). Цель — максимально использовать Store как unified memory backend
- **P3** Proactive KS maintenance — отдельный canvas для обсуждения актуализации Knowledge Sphere с агентом (параллельно с основной работой) *(cross: Frontend)*
- **P3** Message compaction: trim_messages выполняется безусловно, должен — только при превышении порога и неудачной суммаризации
- **P3** Audio Overviews — генерация аудио-обзора (подкаста) из подготовленного материала. Мини-фича, выходной артефакт наряду с markdown и слайдами. Перенесено из устаревшего roadmap *(cross: Backend)*
- **P3** Выбор image-модели пользователем — расширение каскада settings (thread → project → user) отдельным полем image-модели поверх tool `generate_image` (post-mvp feat-010; в MVP модель задаётся конфигом). Whitelist можно не хардкодить: у OpenRouter есть публичный discovery `GET /api/v1/images/models` *(Agent, cross: Backend, Frontend)*
- **P3** Бинарные документы в skill-context — референсные изображения стиля и подобные не-текстовые материалы пользователя при скилле; Store хранит только текст. Зависит от blob-хранилища (post-mvp feat-010) и skill-context (post-mvp feat-012) *(Agent, cross: Backend)*
- **P2** Накопление голоса пользователя из обратной связи — система запоминает правки-предпочтения по ходу работы над текстом и применяет дальше (аналог Voice_Notes из discovery-цикла статьи). Продуктовая фича поверх skill-context (post-mvp feat-012) *(Agent)*
- **P3** Обратная связь на сохранение в Сферу — пользователь видит, как агент записал в Knowledge Sphere, и даёт фидбек «сохранил не так — сохраняй вот так»; агент фиксирует рекомендацию и применяет её при последующих записях в сферу. Родственно «Накоплению голоса» выше (тот — про правки текста, этот — про манеру ведения памяти); UI-опора — peek-карточка записи в сферу (§ Backend, SSE-событие + peek) *(Agent, cross: Frontend)*
- **P3** Кристаллизация скиллов из работы — механизм-наблюдатель (отдельный классификатор) замечает повторяющиеся объяснения пользователя («в десятый раз описываешь один и тот же процесс») и предлагает упаковать их в скилл; по согласию агент собирает скилл из истории работы. Продуктовая механика, которой нет у generic-платформ (там SKILL.md пишут руками); строится поверх skill-context и «Накопления голоса». Не приоритет — фиксация идеи *(Agent, cross: Backend)*
- **P2** Изображения в контексте агента — в v1 (post-mvp feat-010) агент не видит сгенерированные картинки: ToolMessage текстовый, байты идут в БД мимо контекста; результат оценивает пользователь, правки — словами. Варианты возврата зрения: мультимодальный ToolMessage (требует vision-модели в каскаде), vision-judge субагент «оцени изображение» (механика — post-mvp feat-011), обходной паттерн для не-vision основной модели. Триггер — догфудинг (Фаза 5b) *(Agent, cross: Backend)*
- **P3** Подпись к изображению от агента — в MVP caption во вьюере = prompt генерации; промпт как подпись может выглядеть сыро. Отдельный параметр tool `caption` (человекочитаемая подпись/alt от агента) *(Agent, cross: Frontend)*
- **P3** Входные изображения (`input_references`) + «апскейл выбранного варианта» — image-to-image расширение tool `generate_image`: референс-изображение на вход, доработка/увеличение уже сгенерированного варианта. Триггер — спрос на редактирование/уточнение сгенерированных картинок при dogfooding. Не путать с «Бинарные документы в skill-context» ниже — тот пункт про пользовательские референс-материалы скилла, этот про сам tool генерации *(Agent, cross: Backend, Frontend)*
- **P3** Скилл визуализации техстатей — упаковать зафиксированный при написании статьи визуальный стиль (стиль-промпт генерации, Mermaid-паттерны) в скилл, оркеструющий `generate_image`; личные предпочтения стиля — в skill-context скилла. Сырьё (Visual_Contract, visuals) живёт вне репозитория, в Vault инициативы *(Agent)*
- **P3** Продвинутый context engineering — оптимизация работы с контекстом и токенами наполненного агента (когда агент реально нагружен реальным использованием). Параллельный трек-оптимизация, не привязан к вехе. Перенесено из устаревшего roadmap *(Agent)*
- **P3** Расширенная конфигурация моделей пользователем — полный per-scope контроль параметров модели поверх каскада settings (thread → project → user): reasoning effort / include_reasoning, temperature и прочий `extra_body`, вместо одного поля `model_name`. Сейчас per-scope override несёт только имя модели, `extra_body` scope-настройки пуст — reasoning-конфиг дефолта не наследуется при переключении; компромисс текущей итерации — нормализованный OpenRouter-параметр `reasoning` с дефолт-effort на уровне конфига. Триггер — спрос из догфудинга на тонкую настройку под задачу *(Agent, cross: Backend, Frontend)*
- **P2** SSE-дисконнект отменяет LangGraph-ран → потеря ответа — выполнение графа драйвится потреблением SSE-генератора: разрыв клиентского соединения (закрытая вкладка, сеть, таймаут) роняет `CancelledError` в генератор и отменяет ран до коммита чекпоинта суперстепа — сгенерированный ответ не персистится и исчезает (воспроизведено на стенде feat-011: LLM-вызов завершён, аборт клиента в окне до записи чекпоинта, AIMessage отсутствует в БД; ложный `status=ok` в логе закрыт фиксом `client_disconnected`). Целевая модель: развязать выполнение графа и SSE-отдачу — граф в отдельной задаче (`asyncio.create_task`/`shield` + очередь событий к генератору), разрыв клиента не отменяет ран, ответ дописывается в чекпоинт и виден после перезагрузки; явная отмена пользователем — по-прежнему через `cancel_event`. Триггер актуальности — многоминутные раны субагентов (feat-011): вероятность дисконнекта на длинном ране высокая. Затрагивает стрим-ядро → правка `streaming.md` *(Agent, Backend)*
- **P3** Kill execution-джобы по обрыву соединения (сироты) — v1 execution runtime (feat-011) при отмене хода не убивает джобу: `CancelledError` рвёт `await POST /jobs`, но subprocess в executor доживает до deadline и дописывает файлы в `artifacts/` без `artifact_created` («призрачные» артефакты — видны в list, не в ленте). Джобы короткие (десятки секунд), для догфудинга принято. Правильная модель: executor ловит disconnect (`request.is_disconnected` / отмена ASGI-задачи) и валит джобу той же kill-цепочкой, что deadline (killpg группы bwrap-обёртки → `--die-with-parent` → коллапс pid-ns). Делать вместе с развязкой графа от SSE-отдачи (пункт «SSE-дисконнект отменяет LangGraph-ран» выше) — тот же контур обрыва клиента; затрагивает контракт `/jobs` executor'а *(Agent, Backend, cross: Infra)*
- **P3** LangGraph Postgres checkpointer/store без авто-reconnect — при падении БД SQLAlchemy-пул восстанавливается сам, а `AsyncPostgresSaver`/`AsyncPostgresStore` — нет: после блипа БД соединение остаётся битым, требуется рестарт сервиса для чистого agent-стрима (наблюдалось на стенде feat-007). Причина разобрана по исходникам langgraph-checkpoint-postgres: `from_conn_string` открывает одно `AsyncConnection` (psycopg, не пул), и все операции сериализуются глобальным `asyncio.Lock` (`aio.py::_cursor`) — на тот же лок садится и read-путь UI (`CheckpointHistory.aget_tuple` при каждом открытии чата). Решение: передавать `psycopg_pool.AsyncConnectionPool` в конструктор `AsyncPostgresSaver(conn=pool)` (поддерживается; `async with` в lifespan остаётся) — пул даёт health-check и reconnect соединений. Оговорка: глобальный lock библиотека берёт и при пуле, так что сериализация операций внутри процесса остаётся (known limitation установленной версии; операции короткие — отдельная, менее острая боль под нагрузкой); `pipeline=True` с пулом несовместим (не используется), `setup()` — по-прежнему один раз *(Agent, Infra)*
- **P3** Гонка cancel: отравление `_pending_cancels` + `cancel()` всегда `True` — два смежных дефекта `LangGraphAgentRunner` (`agent/runner.py`). (1) `cancel()` без активного стрима кладёт thread_id в `_pending_cancels` без TTL; в гонке с `finally` стрима (cancel приходит в окно после снятия регистрации) запись отравляет следующий ран — очередное сообщение пользователя в этом чате мгновенно завершается `error` без единого токена; сет растёт неограниченно. (2) `cancel()` возвращает `True`, даже когда отменять нечего, из-за чего ветка локального аборта фронта (`useAgentStream.ts` — `if (!ok) abort()`) мертва. Фикс: TTL/скоуп у pending-cancel + честный `False` при отсутствии активного рана (фронт-ветка оживает без правок). Найдено верификацией внешнего ревью (celery-моделирование) *(Agent, cross: Frontend)*
- **P3** Версии артефактов (на подумать) — половина исходной боли закрыта файловой моделью dogfooding feat-011: `write_file` по тому же пути перезаписывает артефакт, «одна работа — один артефакт» из коробки, копии больше не плодятся. Остаток: версии артефакта с историей (переключатель версий во вьюере, откат) + **полный визуальный дифф-просмотр обновления** (канвас-стиль ChatGPT — что именно изменилось в тексте). В feat-011 обновление показывается только компактной дельтой строк `+N −M` в бейдже карточки (backend считает в момент перезаписи); полный дифф требует хранения старой версии — диффать не с чем, пока версий нет. Сюда же — полный lifecycle артефакта: события удаления/переименования (`artifact_deleted`) в feat-011 не эмитятся, список чинится инвалидацией по завершении хода, историческая карточка удалённого файла показывает «файл больше не существует». Рассматривать вместе с версионированием Сферы знаний *(Agent, cross: Backend, Frontend)*
- **P3** Live-рендер карточек артефактов в ленте по SSE — в feat-011 карточка встаёт из истории по завершении хода (сознательное решение: во время хода живут tool-строка и сайдбар; live-материализация требует рендера part'ов из стрима, склейки с историей после хода и дедупликации, плюс live-обновление бейджа «+N −M» по `artifact_updated`). Триггер — dogfooding показывает, что на длинных ходах ожидание карточек раздражает *(Frontend, cross: Agent)*
- **P2** Langfuse: рассинхрон меток промпта `system--production` — метка `production` указывает на версию 2, `latest` — на версию 3; рантайм читает `latest`, функционально не блокер, но UI-картина «что в проде» врёт. Синхронизировать метки перед/после деплоя; причину появления не разбирали (решение гейта chore-001: не копать, просто выправить) *(Agent, Langfuse, harvest chore-001)*
- **P3** `runner.py`: переименовать `review_events` — переменная хранит булев «защита активна» (`self._enforcer.active`), а имя и `if review_events:` читаются как «есть события». Поведение корректно, риск только читаемость; `review_active` при следующем касании файла *(Agent, Tech Debt, harvest chore-001)*

## Backend

- **P2** Multi-tab холодный старт разлогинивает через replay-детекцию refresh — бутстрап SPA шлёт `POST /auth/refresh` при каждом холодном старте без access-токена; две вкладки с пустым localStorage и живой refresh-cookie гонятся, проигравшая ловит replay → `revoke_all_for_user` рвёт все сессии. Класс pre-existing, частота выросла с app-бутстрапом feat-008. Развилка (решение архитектора): cross-tab-дедупликация (BroadcastChannel/лидер-вкладка) vs grace-окно на ротацию refresh. Побочно: при `CLIENT_IP_SOURCE=socket` за прокси ключ лимитера `refresh:{ip}` один на всех *(Backend, cross: Frontend, Security)*

- **P2** Локализация detail-строк парольного auth — «Invalid credentials», «Username already exists» и соседние строки `routes/auth.py`/`services/exceptions.py` показываются на русской странице `/login` по-английски (фронт по контракту рендерит `detail` сервера). Введены в первичной JWT-итерации, подтверждено на живом стенде feat-008. Малая отдельная итерация: локализация тронет ассерты `tests/auth`/`tests/oauth` *(Backend, cross: Frontend/UX)*

- **P3** Наблюдаемость гео-gate при `CLIENT_IP_SOURCE=socket` за прокси — в docker/nginx-топологии socket-адрес это прокси, MMDB даёт промах и все пользователи молча получают фолбэк RU (только Яндекс). Отказ fail-closed (безопасный), но без сигнала в логе. Целевая модель: warning на старте при заданном `GEOIP_DB_PATH` и `CLIENT_IP_SOURCE=socket` + пункт в деплой-чек-листе *(Backend, cross: Infra)*

- **P3** Ручная линковка/отвязка OAuth-провайдера + смена пароля OAuth-пользователем — точка расширения ADR-033 (схема `oauth_accounts` 1:N готова, функциональности v1 нет): пользователь в настройках привязывает второго провайдера / отвязывает существующего; установка пароля для беспарольного аккаунта *(Backend, cross: Frontend)*

- **P3** Возврат провайдера VK ID — исключён в feat-008 (бизнес-верификация недоступна физлицу, dev-флоу требует отдельной https-топологии). Триггер: верифицированный бизнес-профиль VK ID; цена возврата — код провайдера + расширение `CHECK`, без структурных изменений схемы (ADR-033) *(Backend)*

- **P3** Downgrade миграции `e7fac2fe3bdf` (oauth_accounts) форвард-only — откат упадёт на `ALTER COLUMN password_hash SET NOT NULL` при наличии OAuth-пользователей (строки с NULL). Известное свойство, зафиксировано в ревизии; если откат реально понадобится — ручная DML-правка `downgrade` с заголовком `# Manual migration:` и одобрением архитектора *(Backend, db, known-debt)*


- **P2** Потолок затрат на пользователя (per-user cost budget) — per-IP rate limiting есть, но лимита токенов/стоимости на пользователя нет: любой зарегистрированный аккаунт может неограниченно жечь LLM-бюджет владельца. Для показа паре доверенных преподавателей приемлемо; **гейт перед любой волной внешних пользователей** (студенческая волна через ГОСТ-скилл, Фаза 6). Данные по стоимости уже есть в Langfuse (pricing seed, dogfooding feat-003) — вопрос агрегации per-user и enforcement: мягкий (деградация на дешёвую модель по исчерпании) vs жёсткий (отказ с показом квоты); решение — на design-brief *(Backend, cross: Agent, Security)*
- **P2** Версионирование Сферы знаний (бэкенд под семвер-UI feat-004) — design-branding feat-004 реализовал фронт семвера сферы (дропдаун «Сохранить версию ▾» с предложением уровня патч/минор/мажор, история версий, бейджи, панель «Жизнь сферы») **на mock-данных**. Нужен бэкенд: хранение версий `major.minor.patch`, история (автор agent/user, summary, ts), API чтения/сохранения версии, правила предложения уровня. Откат версии (peek S3) опирается на это *(Backend, cross: Frontend, Agent)*
- **P2** SSE-событие «агент пишет в сферу» + peek (бэкенд под feat-004) — фронт peek-карточки S3 («Записано в сферу → раздел», дифф, действия Открыть/Подправить/Откатить) сделан на mock. Нужно реальное SSE-событие в стриме агента при записи в сферу (индикатор + последующая карточка с диффом и откатом к версии). Зависит от версионирования сферы выше *(Backend, Agent, cross: Frontend)*
- **P3** Персист состояния студии-панели (опц.) — `{open, tab, selectedArtifactId}` сейчас клиентское (feat-004). Серверный персист — только если потребуется синхронизация между устройствами; иначе закрыть как не нужно *(Frontend, cross: Backend)*
- **P3** Thumbnail-вариант media — превью image-артефакта в карточке чат-ленты качает полноразмерный файл с media-endpoint (после dogfooding feat-011 блобы PG упразднены, артефакт = файл workspace; immutable-кэш заменён ETag-ревалидацией — полноразмер тянется чаще). При росте лент/объёмов — уменьшенная копия (генерация при записи или кэш на лету, `?variant=thumb` у media-endpoint) *(Backend, cross: Frontend)*
- **P3** Media-хранилище на S3/MinIO + presigned URLs — вынос workspace-файлов/медиа в объектное хранилище при росте объёмов (после dogfooding feat-011 файлы живут на docker volume, отдача — через backend media-endpoint): presigned URL разгружает backend (клиент качает напрямую), путь к CDN. Цена: новый сервис в инфраструктуре, пересмотр файловой модели workspace (executor и файловый слой работают с локальной ФС — S3 ломает эту премиссу для исполняемых джоб, кандидат — только media-отдача артефактов). Порог — Фаза 6 (внешние пользователи) или заметная нагрузка на отдачу *(Backend, cross: Infra)*
- **P3** `mcp_servers.py` — дедупликация ownership-проверок и инстанциаций — три уровня (user/project/thread) повторяют один и тот же паттерн «загрузить → проверить владельца → 404» в ~9 handlers, плюс множественные `MCPServerRepository(session)` / `_get_encryption(request)` / `invalidate(...)` по месту. Вынести ownership в dependencies (по образцу `UserProject`/`UserThread`), повторяющиеся инстанциации — в deps. Выявлено в slice feat-004 (codebase maturity), вынесено отдельным пунктом из-за объёма файла *(Backend)*
- **P3** `POST /rules` принимает произвольный `config` без валидации схемы — `services/siem-service/siem_service/api/routes.py` создаёт correlation-правило с любым JSON в `config` (201 на что угодно), а движок затем молча пропускает правило с незнакомой схемой, логируя `threshold_rule_missing_pattern`. Админ получает 201 и «правило», которое тихо не работает. Решение — discriminated union по `rule_type` для `config` (валидация на create) + явная 422 на нераспознанную схему. Known limitation из feat-005, подтверждена прогоном feat-004 *(Backend, SIEM)*

- **P3** Single-worker допущение: process-local состояния не переживут второй воркер/реплику — деплой осознанно однопроцессный (`entrypoint.sh`: uvicorn без `--workers`), но допущение нигде не зафиксировано, а состояний, молча ломающихся при multi-process, минимум три: (1) `_cancel_events`/`_pending_cancels` в `agent/runner.py` — `/cancel` попадает не в тот процесс и при этом отвечает `ok: true` (клиент не делает локальный abort и висит до конца генерации — отказ молчаливый); (2) `RateLimiter._buckets` (`infra/rate_limit.py`) — лимит логина 5/60с превращается в 5×N; (3) `checkpointer.setup()`/`store.setup()` в lifespan — гонка миграций чекпойнтера между стартующими процессами. При переезде на multi-process: отмена — сигналом через Redis pub/sub, устанавливающим локальный `asyncio.Event` (не polling-флаг: проверка в горячем цикле стрима должна остаться бесплатной, а Redis в проекте опционален — нужен гибрид), rate limiter — в Redis, setup — отдельным шагом деплоя. До тех пор новые process-local состояния допустимы осознанно. Триггер: вторая реплика / `uvicorn --workers` / Фаза 6 (внешние пользователи) *(Backend, cross: Agent, Infra)*

_Findings из feat-008 Enforcement (Codebase Maturity) — R1: нарушения `api/routes → repositories` (API→Repository), вскрытые arch-checker'ом. Все три нетривиальны (нет готового сервиса для механической делегации), зафиксированы в `ignore_imports` контракта api-isolation — долг под наблюдением, новые нарушения того же класса ловятся сразу. Удаление строки из allowlist возвращает нарушение в gate._

- **P3** `SettingsService` — вынести `api/routes/settings.py` из прямого доступа к `repositories/settings.py` — 6 хендлеров инстанцируют `SettingsRepository(session)` по месту; сервиса нет, `ModelConfigResolver.resolve(repo, …)` принимает репозиторий параметром. Перенос — проектирование сервисного слоя, не механическая замена. Триггер активации: когда роут трогается по другой задаче *(Backend, выявлено feat-008)*
- **P3** `McpServerService` — read-path методы; `api/routes/mcp_servers.py` (18 read/list/toggle/delete хендлеров) ходят в `repositories/mcp_server.py` напрямую — существующий `McpServerService` покрывает только write/guard-flow. Перенос — добавление read-методов в сервис. Пересекается с P3 «`mcp_servers.py` дедупликация ownership» выше (тот же файл, оба про вынос в сервис/deps — делать вместе) *(Backend, выявлено feat-008)*
- **P3** `FeedbackService` — выделить из `api/routes/feedback.py` (инлайн Langfuse + Redis `TraceStore` + проверка владения) — сервиса нет, роут напрямую инстанцирует `TraceStore(redis_client)`. Наиболее изолированный из трёх R1 (кандидат на починку первым). Пересекается с P3 «Feedback resource modeling» ниже (тот же файл) *(Backend, выявлено feat-008)*

_Findings из feat-002 REST API slice (Codebase Maturity) — отложены архитектором, фиксятся не в slice'е._

- **P3** Feedback resource modeling — убрать протечку Langfuse `trace_id` в REST-контракт. Сейчас feedback адресуется `PUT|DELETE /projects/{id}/chats/{cid}/feedback/{trace_id}` (feat-002): `trace_id` — observability-идентификатор Langfuse, утёкший в публичный контракт, а вложенность depth-3 избыточна (trace_id глобально уникален, project/chat в пути — только ради авторизации). Целевая модель — адресация по доменному `message_id` (`.../chats/{cid}/messages/{message_id}/feedback`), `trace_id` резолвится на бэке для `create_score`. Требует: маппинг message_id↔trace_id (сейчас в Redis только прямой `trace:{thread_id} → {message_id: trace_id}`), правки фронта (`FeedbackButtons` оперирует `trace_id` из SSE `done`), резолв в роуте. Не горит — текущий контракт работает и ownership-безопасен; это чистота доменной модели на вырост. Нужно продумать маппинг детальнее перед реализацией *(cross: Frontend, Langfuse)*
- **P3** Дубль `problem.py` + иерархии `AppError` в двух сервисах — `backend/app/api/problem.py` и `services/siem-service/siem_service/api/problem.py` почти идентичны (RFC 9457 handlers, feat-002), а feat-007 добавил **второй дубль** — siem получил свою копию иерархии `AppError`/`exceptions.py` (полное зеркало по решению OQ-C), которую нужно синхронить с main app вручную (зеркала уже разъезжались — системный CORS-on-500 баг feat-007). Кандидат на вынос в shared-пакет исключений + барьера. Trade-off: DRY vs независимость сервисов (CLAUDE.md: «services/ — самостоятельные runtime'ы»); если не выносить — кандидат в arch-checker (feat-008): проверять совпадение зеркал *(Backend, SIEM)*
- **P3** MCP `mcp_unreachable` — неточный статус-код — `backend/app/services/mcp_server.py:271` отдаёт `503` при недоступности внешнего MCP на create/update. Сам fail-closed (security-валидация metadata требует доступного сервера) — корректен. Но `503` означает «наш сервис недоступен» (провоцирует ретраи / реакцию балансировщиков); недоступный URL, указанный клиентом, семантически ближе к `422` (не можем обработать данные запроса) или `502` (upstream не ответил). Поправить только код, security-логику не трогать *(Backend)*
- **P1** Graceful-деградация старта при недоступном built-in MCP — сетевая ошибка к `mcp.jina.ai` в момент старта роняет приложение (`_validate_subagent_registry`: `unknown tool name(s) search_web/read_url` → `Application startup failed`): рестарт/деплой в окно недоступности внешнего MCP = прод не поднимается. Решение архитектора (гейт chore-001): так быть не должно — приложение обязано корректно стартовать при недоступном built-in MCP, реестр субагентов деградирует, не валя старт. Меняет модель отказа старта — проектировать явно (что теряет агент, как это логируется/восстанавливается). Поднято P2→P1: риск материализовался на проде (2026-08-11) — пересоздание app-контейнера при переводе на домен learnflow.me вызвало свежий старт, tool-листинг исчерпанного Firecrawl MCP не вернул инструменты, приложение легло в crash-loop до внепланового релиза feat-009 (PR #91); класс отказа сохраняется и с Jina *(Backend, Agent, Infra, harvest chore-001)*
- **P3** Per-request кэш клиентского IP — middleware и auth-роуты (`main.py`, `routes/auth.py`) резолвят IP независимыми вызовами `get_client_ip`: при дрейфе nginx (пропавший `X-Real-IP`) каждый register/login/refresh пишет два одинаковых fallback-WARNING. Целевая модель: резолв один раз в middleware → `request.state`, роуты читают оттуда *(Backend, harvest chore-001)*
- **P3** Восстановление удалённых чатов (корзина + TTL) — dogfooding feat-002 сознательно выбрал hard delete (soft-delete-флаг размазал бы фильтр по всем выборкам и оставлял бы переписку в БД без UI восстановления). Если из эксплуатации придёт спрос на «вернуть удалённый чат» — делать полноценной механикой: корзина с TTL (флаг `deleted_at` + отложенная физическая чистка ThreadView/настроек/checkpoints), UI списка удалённых, restore. Обоснование trade-off — design-brief feat-002 § Scope boundaries *(Backend, cross: Frontend)*
- **P3** Удаление проекта не чистит LangGraph checkpoints — `ProjectService.delete_project` удаляет `thread_views` каскадом, но история тредов остаётся в `checkpoints`/`checkpoint_blobs`/`checkpoint_writes` (связь по значению `thread_id`, FK нет) — рост БД + «удалённая» переписка живёт вечно (приватность). Для одиночного чата долг закрыт в dogfooding feat-002 (`AgentRunner.delete_thread` → `AsyncPostgresSaver.adelete_thread`); для проекта — обход его тредов тем же методом до каскадного удаления. Механика готова, осталось применить *(Backend)*
- **P3** `Workspace.write_text`/`write_bytes` теряют tmp-файл при сбое записи — падение между созданием `.workspace-tmp-*` и атомарным `replace` оставляет tmp-файл на диске навсегда: он отфильтрован из `list_dir`/`snapshot_artifacts`, невидим ни агенту, ни UI. Целевая модель — обернуть запись в `try/except`, `tmp_path.unlink(missing_ok=True)` в обработчике *(Backend, выявлено feat-011)*
- **P3** `sanitize_filename` не защищает служебный префикс `TMP_FILE_PREFIX` — имя вида `.workspace-tmp-x.pdf` (из upload или заголовка `generate_image`) даёт файл, скрытый из `list_dir`/`snapshot_artifacts`, без события `artifact_created` — тихая потеря пользовательских данных на edge-case имени. Целевая модель — срезать/экранировать префикс в начале очищенного имени. Триггер актуальности — расширение зоны загрузок на внешних пользователей *(Backend, выявлено feat-011)*

## Product / Distribution

- **P2** Студенческая волна + позиционирование для студентов — гипотеза: студентам продукт продаётся не как «инструмент подготовки материалов» (JTBD преподавателя/спикера), а через готовые узкие офферы; первый — «отчёт по ГОСТ 7.32» (ГОСТ-скилл, dogfooding feat-012). Волновая модель дистрибуции: показ преподавателям (5b) → одногруппники ~30 чел с ГОСТ-оффером → Хабр-статья про ГОСТ-скилл (C1, talks.md) с CTA «попробовать в продукте» как воронка. **Гейт волны — потолок затрат на пользователя (§ Backend).** Полноценное бизнес-позиционирование для студентов — прорабатывать при открытии Фазы 6, не раньше; сюда же вернётся Generative UI-идея адаптивных учебных интерфейсов (§ roadmap Фаза 4) *(Product, cross: Контент)*
- **P2** Telegram Bot — второй канал доставки поверх существующего agent runtime: тонкий адаптер (SSE-поток → `sendMessageDraft`/`sendRichMessageDraft`), ответы агента в Rich Markdown (GFM + LaTeX), топики как сессии, Login with Telegram как мост к auth. Скоуп сознательно ограничен: диалог с агентом и потребление материалов — полноценно; редактирование Knowledge Sphere и rich-UI остаются в вебе. Ориентир — Фаза 6 (канал дистрибуции). Ресерч завершён (`doc/research/telegram-bot-platform.md`). Telegram — первый и эталонный канал; за ним той же адаптерной моделью — боты других платформ для расширения аудитории (Max, VK), каждая — отдельным адаптером поверх того же agent runtime, по вызревании Telegram-канала *(cross: Backend, Agent)*
- **P3** Standalone-дистрибуция скиллов с обратной воронкой — скиллы проекта применимы и вне LearnFlow AI (vision.md фиксирует standalone-скиллы как канал); внутрь скилла закладывается инструкция: после удачного применения агент упоминает происхождение («скилл из проекта LearnFlow AI — он помогает решать такие-то задачи») как мягкий CTA в продукт. Дешёвый дистрибуционный приём поверх уже планируемых standalone-репозиториев скиллов *(Product, cross: Контент)*
- **P3** Организационные (вузовские) скиллы — механизм распространения скиллов на уровень организации: кафедра/вуз заводит специфичные скиллы (например, оформление внутривузовского отчёта) и раздаёт их всем своим преподавателям централизованно. Для on-prem-внедрений — кандидат в enterprise-модуль (управление org-скиллами); см. strategy.local.md § монетизация *(Agent, cross: Backend, Product)*
- **P3** Public Material Sharing — публичные ссылки на материалы (Notion-style share-to-web). Преподаватель публикует → получает URL → студенты видят материал без регистрации. Потенциальный pivot от "инструмент подготовки" к "подготовка + дистрибуция". Для rich-артефактов зависит от подходящего формата публикации / экспорта, но не требует Generative UI *(cross: Frontend, Backend, Infra)*
- **P3** SOFA Blueprint-кандидаты к публикации — прогнать blueprint sweep (режим скилла `sofa-contributor`, вызов словами) и довести вызревшие категориальные паттерны до публикации. Стартовые кандидаты (выявлены до появления свипа): многослойная защита LLM (топология prompt-injection → SIEM → boundary), FSM AIDD-оркестратора с A6-guardrails целостности тестов, партиция тестов по непересекающимся скоупам под parallel-safe fan-out, fuzzy-patch обновление Knowledge Sphere, каскад dynamic model resolution. Дедуп и формат — в самом режиме свипа; реестр публикаций — `doc/content/sofa/` *(Контент)*

## Infra

- **P2** Тегированные релизы + версионирование — при переходе к реальным деплоям (сентябрь, догфудинг/показ) завести релизный цикл: git-теги `vX.Y.Z` на выкатки в `main`, changelog (хотя бы генерируемый из коммитов), GitHub Releases. Мотивация двойная: релизная гигиена при живых пользователях и заявка на грант Yandex Open Source — тегированные релизы оказались общим знаменателем победителей AI-трека (28 релизов у Auto AI Router, PyPI у langchain-localai; бенчмарк — strategy.local.md §7). Механику (ручные теги vs release-workflow в CI) решить при заведении *(Infra, cross: Meta)*
- **P3** Metrics/alerting backend (на подумать) — в проекте нет числовой метрической инфраструктуры (Prometheus/StatsD): вся наблюдаемость на structlog + SIEM security-events. Следствие (всплыло в feat-007, D-ERR-6/OQ-B): нет места считать и алертить по числовым порогам — частота деградаций guard, доля таймаутов, rate ошибок. Сейчас сигналы деградации едут только как SIEM-события. Решить, нужен ли метрический слой и какой *(Infra, cross: Agent, SIEM)*
- **P3** Сузить `COPY` в прод-образах до рантайм-минимума — `backend/Dockerfile` копирует `backend/` целиком (включая `backend/tests/`), `packages/` и `services/` (в т.ч. тестовый харнесс `packages/testing` и весь исходник `siem-service`, который backend не импортирует); siem-образ — свои tests + `packages/testing`. Та же категория мусора, что вычищенные dev-deps (chore-001 убрал их из venv, но не из файловой системы образа); сужение — изменение состава образа с риском для workspace-резолвера, отдельной итерацией по образцу точечного `COPY tools/*` *(Infra, harvest chore-001)*
- **P3** Автоматизация обновления гео-базы (IPinfo Lite MMDB) — feat-008 ввёл оффлайн country-детекцию для гео-разделения OAuth-провайдеров; v1 живёт на разовом скачивании MMDB (`data/geoip/`) при настройке окружения, механика регулярного обновления не реализована. Country-данные тухнут медленно, но бессрочно стареющая база — долг; решить механику (systemd-timer на VM vs deploy-артефакт vs CI) *(Infra, из feat-008)*
- **P3** `backend/Dockerfile`: `ARG VITE_SIEM_ENABLED` стоит до `COPY package*.json` + `RUN npm ci` — переключение тумблера инвалидирует кэш-слой npm-установки при каждой пересборке. Перенести пару `ARG`/`ENV` непосредственно перед `RUN npm run build`; дешёвый промежуточный фикс, пока флаг build-time (исчезает при runtime-конфигурации фронт-флагов, см. Frontend / UX) *(Infra, harvest chore-001)*

## Security

- **P2** `SecretStr` для секретных полей `Settings` (глубокая защита) — канал утечки через rich-locals закрыт в feat-008 (`RichTracebackFormatter(show_locals=False)`), но прямой `repr(settings)`/сериализация в лог всё ещё печатает `jwt_secret`/`llm_api_key`/шесть `oauth_*`-кредов открытым текстом. Обёртка в `pydantic.SecretStr` трогает каждое чтение секрета — отдельная итерация *(Security, cross: Backend)*

- **P3** Timing-оракул перечисления способа входа — `AuthService.login` отвечает 401 мгновенно для аккаунта без пароля (`password_hash IS NULL`) и после bcrypt-проверки для аккаунта с паролем; класс существовал для несуществующего имени, feat-008 добавил признак «аккаунт заведён провайдером». Решение по модели угроз: если перечисление — угроза, фиктивная bcrypt-проверка на обеих 401-ветках *(Security, cross: Backend)*


- **P3** Reasoning-канал вне guard-проверок перед показом пользователю — feat-001 вывел `reasoning_chunk` на экран, оставив его вне canary / mid-stream / final-output проверок: в `runner.py` guard и `full_response` кормит только ветка `text_chunk`. До итерации канал отбрасывался фильтром — вектора не было; теперь канареечный токен или утёкший системный промпт, всплывший в рассуждениях, дойдёт до экрана непроверенным. Осознанно отложено: LLM-защита уходит под kill-switch (chore-001), большинство проприетарных моделей отдают суммированные рассуждения (оговорка: `z-ai/glm-5.2` по факту отдаёт сырые). Активируется при подъёме LLM-защиты под прод с реальными требованиями: детерминированный canary-скан по reasoning (дёшево, ловит именно утечку) + отдельное решение о включении рассуждений в LLM-проверки (удлиняет вход, даёт FP — модель в рассуждениях легально обсуждает подозрительное) *(Agent, Security, выявлено feat-001 agent-visibility)*
- **P2** Схема rate-лимитов периметра дырява по обеим осям — ключ логина `login:{name}:{ip}` (`routes/auth.py:170`) не ловит ни распределённый перебор одного аккаунта (атакующий меняет IP → новый ключ), ни спрей по многим аккаунтам с одного адреса (меняется `name` → новый ключ); у регистрации (`register:{ip}`, 3/час) нет глобального потолка. chore-001 чинит **источник** IP (`CLIENT_IP_SOURCE`), из-за которого лимиты обходились тривиально, но саму схему не трогает — это проектирование политики защиты аккаунтов, а не закрытие прода. Целевая модель: разложить лимит логина на две независимые оси (по имени и по IP отдельно) + глобальный потолок регистраций. Помнить границы применимости IP как признака: за CGNAT оператора один адрес делят тысячи пользователей (ложные срабатывания), у атакующего с облачным аккаунтом адресов тысячи задёшево — IP-лимит остаётся speed bump, а не стеной. Инвариант, на котором всё держится: IP ничего не выдаёт (не аутентифицирует, не авторизует, не открывает квоту), он только ограничивает и записывает *(Backend, Auth, Security)*
- **P3** Семь security-логов без `event_type` молча дропаются — `tool_guards.py:114,174`, `runtime_security.py:220`, `services/{skill_context,sphere,mcp_server,user_memory}.py` пишут `security_event=True` без `event_type`, а процессор такие записи отбрасывает (`security_pipeline/processor.py:40-45`). Это все точки *enforcement* («мы заблокировали»); само *обнаружение* эмитится из `guard.py` с корректным `event_type` для любого checkpoint'а, поэтому теряется избыточность, а не слепое пятно. Побочно: `bind_contextvars(security_event_dropped=...)` привязывает флаг к контексту насовсем — он виснет на всех последующих строках лога того же запроса. Не чинилось в chore-001 осознанно: правка тянет расширение вокабуляра `siem-contracts` ради подсистемы, которую та же итерация выключает. Триггер — возврат SIEM в активный режим *(Backend, SIEM, Security)*
- **P3** Fail-open write-роутов при отсутствии security guard — все три write-роута (`user_memory.py`, `skill_context.py`, `mcp_servers.py`) резолвят guard через `getattr(request.app.state, "security_guard", None)`: если guard не сконструирован/не положен на `app.state` (misconfiguration в lifespan), сервисы получают `guard=None` и пропускают security-checkpoint целиком — запись проходит без проверки вместо явного отказа. Риск теоретический (Sec 2.0 всегда строится в lifespan до роутеров), но паттерн воспроизведён по коду во всех трёх файлах. Целевая модель: явный fail-closed (500) при отсутствии guard вместо молчаливого пропуска. Не дубль пункта про деградацию LLM-классификатора (ADR-017 D-ERR-6) — тот про недоступность LLM, этот про отсутствие guard-объекта целиком. Триггер — следующий security-hardening проход или ревью новой write-checkpoint-точки *(Backend, Security)*
- **P3** Таймаут на вызов LLM-классификатора — у guard-вызова классификатора нет собственного таймаута: при деградации провайдера guard-модели (пустые ответы с ретраями, зависания) запрос агента висит минутами без завершения, «agent completed» в логе не появляется. Воспроизведено на живом стенде feat-012 (провайдер `z-ai/glm-4.7-flash`); симптом снят заменой guard-модели, механизм остался — следующая деградация любого провайдера повторит картину. Целевая модель: явный timeout на классификатор-вызов + предсказуемый исход по ADR-017 (fail-open/fail-closed согласно полярности checkpoint'а) *(Agent, Security)*
- **P2** RBAC guard страницы `/security` пропускает не-админов — заявленный в DoD feat-005 «React страница `/security` (lazy chunk + RBAC guard)» по факту даёт зайти на страницу пользователю с устаревшим / не-админским JWT. После feat-002 (закрытие `/events` под `require_admin`) все SIEM-эндпоинты не-админу отдают 403 — пользователь видит неработающий UI без понятной диагностики. Guard должен либо отсекать вход на страницу целиком при отсутствии `is_admin`, либо страница должна показывать explicit message «Требуются права администратора» вместо обычной ошибки загрузки *(Frontend)*
- **P3** JWT claim `is_admin` не освежается после `grant-admin` — claim фиксируется в JWT при логине; `make grant-admin USER=...` обновляет `users.is_admin` в БД, но активные токены остаются со старым значением до relogin / истечения. На UI это выглядит как «выдал админа, но SIEM-вкладки всё ещё 403». Варианты: (a) короткоживущий access token + refresh при изменении прав; (b) явная invalidation токенов по user_id при `grant-admin`; (c) проверка `is_admin` через БД в dependency, не из claim; (d) минимальный UX-фикс — на 403 от SIEM API показывать toast «Перелогиньтесь, чтобы обновить права». Workaround сейчас — logout + login *(Backend, Frontend, Auth)*
- **P2** Async Guard — параллельная проверка guard с main LLM для снижения latency. Tool execution ждёт вердикта *(Agent)*
- **P2** Multi-turn escalation detection — текущий classifier видит history, отдельный механизм not planned; пересматриваем при появлении подтверждённого FN-класса, который history-aware classifier стабильно пропускает *(Agent)*
- **P3** Trust-tier formalization для internal tools — если выйдет class internal tools, чьи outputs регулярно конфликтуют с защитными слоями (по аналогии с tool result + skills corpus), описать tier-механизм для исключений *(Agent)*
- **P3** User MCP attack probe в eval-наборе — добавить attack probe для пункта «User MCP → единая строгость» при возврате eval-инфраструктуры из parked-режима *(Agent)*
- **P3** Глобальный refactor паттерна `get_db_session` commit — активируется при повторном проявлении 404-симптома в других routes (сейчас покрыт точечным фиксом в ChatService, конвенция — в conventions.md) *(Backend)*
- **P3** Guard observations иерархия в Langfuse UI — guards рендерятся как siblings root span, не вложены в позицию между iter'ами agent node. Не блокирует observability; ремедиация требует синхронизации CallbackHandler vs OTel scope, low value *(Agent)*
- **P1** Fundamental red-team eval — объективная метрика защиты для статьи — feat-008 дал baseline: инструмент Promptfoo + первый поверхностный прогон (22 кейса, 0 успешных атак, 6/22 errored по timeout / grader edge case). Нужен фундаментальный, более полный прогон: глубже разобраться в Promptfoo (plugins / strategies / frameworks) и/или подключить более жёсткие red-team наборы, выбить измеримую объективную метрику качества защиты на сотнях кейсов. Цель — не быть голословным во флагманской статье о многослойной защите (см. `doc/content/security-article-draft.md`). Фундамент — `doc/security/`. **Зависимость флагманской статьи (Трек C), а НЕ гейт Фазы 4:** LLM-защита в проекте — артефакт исследования, не продуктовая потребность; прогон тянется тогда, когда садимся за статью *(Agent, Security, cross: Контент)*
- **P3** Продуктовое решение: модель безопасности субагентов — развилка, зависящая от полномочий субагентов. Если субагенты получат полномочия уровня основного агента (запись в memory, другие критические операции) — защита внутри субагентского графа должна стать полной, как у основного, и это потребует пересмотра классов security-слоя, чтобы `SecurityGuard` нативно встраивался в граф субагента без дублирования одних и тех же методов/конструкций в двух местах. Если субагенты остаются read-only-исполнителями — защиту внутри не наращивать вовсе (лишний код): достаточно границы основного агента — вызов субагента это tool-вызов, уже закрытый checkpoint'ами `TOOL_CALL_ARG`/`TOOL_RESULT`. Частный случай развилки — A1 из review-a feat-011: ReAct-цикл субагента не оборачивает tool-результаты trust-маркерами `compose_for_llm`, в отличие от основного графа, — принято как осознанное состояние v1 до этого решения. Второй частный случай (feat-001 agent-visibility): TOOL_RESULT-guard субагента не имеет наблюдаемого эффекта на ране — у всех спеков `persistence: "none"`, граф субагента компилируется с `checkpointer=False`, редакция живёт только в его эфемерном состоянии и в чекпоинт основного треда не попадает → `CheckpointHistory.latest_redaction` возвращает `None`, `security_block` не эмитится, ход продолжается; единственный эффект guard'а — подмена содержимого в контексте самого субагента. Варианты: свой канал инспекции для субагентов либо перенос вердикта в возвращаемое значение `SubagentRunner.run`. Триггер: появление у субагентов write-полномочий или red-team находка *(Agent, Security)*
- **P3** Классификация императивных tool-инструкций пользователя: FP или TP — сообщение владельца сессии вида «вызови инструмент run_subagent с аргументами…» LLM-классификатор квалифицирует как инъекцию → `security_blocked`, чат залочен (воспроизведено на стенде feat-011; естественная формулировка той же просьбы проходит). Неоднозначно by-design: с одной стороны, прямое управление инструментами — легитимный паттерн power-user'а от аутентифицированного владельца сессии (не untrusted-контент), с другой — та же форма в inj-контенте — классический вектор. Решить норму (легальна ли императивная tool-инструкция из user-канала), затем либо перекалибровать классификатор/корпус, либо зафиксировать как осознанный TP. Развязка может стать неактуальной при выключенной в проде LLM-защите (kill-switch) *(Agent, Security)*
- **P3** SIEM → SOC evolution — развить текущий SIEM (сбор / корреляция / алертинг / уведомление) в полноценную SOC-систему с активным реагированием: не только обнаруживать и уведомлять, но и блокировать (напр. брутфорс по IP / аккаунту). Сейчас SIEM реализован из учебной потребности (дисциплина по ИБ, фундамент диплома); как продуктовая фича не требуется, но при выходе на реальный прод (~сотни пользователей, ближе к осени) активное реагирование 100% пригодится — требует допила функционала (валидация правил, рабочий RBAC — сюда же, вместе с замороженными SIEM Extensions из post-mvp feat-007). Далёкий горизонт: не гейт Фазы 4 и, вероятно, дальше Фазы 5. Триггер — реальная потребность в живом SIEM (диплом / прод-нагрузка): по решению архитектора SIEM в проде отключается собственным kill-switch'ем *(Backend, SIEM, Security)*
- **P3** `client_ip.py`: режим `x-forwarded-for` читает заголовок через `.get()` — берётся первый экземпляр `X-Forwarded-For`, не склейка всех (`getlist()`): за прокси, дописывающим адрес отдельной строкой заголовка (не через запятую), hop-отсчёт справа пойдёт по целиком клиентской первой строке — режим молча перестанет защищать. Сегодня недостижимо (nginx склеивает `$proxy_add_x_forwarded_for`, прод-режим — `x-real-ip`); `getlist` + склейка при реальном переходе на XFF-режим. Смежная открытая проблема XFF-режима — смешанные точки входа с разной глубиной дописывания — опубликована как SOFA Question: https://agents.stackoverflow.com/questions/5aa45468-d14c-4166-89f0-c56b7e5c7f74 *(Backend, Security, harvest chore-001)*
- **P3** `rate_limit._buckets` растёт неограниченно — ключ создаётся на каждый уникальный IP и не удаляется, даже когда список таймстемпов опустел (pre-existing; до chore-001 усугублялось спуфингом XFF, T1 сузил пространство ключей до реальных адресов, но чистки нет). Lazy-удаление пустых бакетов при чтении *(Backend, Security, harvest chore-001)*

## Cross-cutting

- **P1** Text feedback — текстовые комментарии к трейсам (расширение like/dislike), видимые в Langfuse *(Frontend + Backend + Langfuse)*
- **P2** Per-user MCP management — UI для добавления/отключения MCP-серверов per user *(Frontend + Backend)*
- **P2** HF Pro utilization — найти применение подписке Hugging Face (~$9/мес) шире одной задачи: разведка сильных моделей/Spaces под текущие задачи и проекты (генерация, обработка, разовые специфические задачи). Первое применение состоялось — batch-прогон вырезания фона через managed BiRefNet (Design-branding feat-002), паттерн «Gradio API + gradio_client + токен» воспроизводим для любого Space. Off-topic относительно одного трека, поэтому здесь, а не в tasklist *(cross: Design-branding, Agent)*
- **P3** Чаты без проекта — сейчас чат обязан принадлежать проекту (`thread_views.project_id` NOT NULL), «быстрый чат без выбора проекта» невозможен. Глубоко завязано на память: knowledge sphere, skill-context, MCP-каскад и настройки живут на уровне проекта — «безпроектному» чату нужен либо неявный дефолт-проект, либо отдельная модель контекста. Отложено при дизайне dogfooding feat-002 (Chat UX): создание чата всегда через привязку к проекту (модалка выбора в sidebar) *(Frontend + Backend, cross: Agent)*

## Meta (Backlog & Workflow Infrastructure)

_Мета-работа над процессом и инструментами разработки, не фичи продукта._

- **P1** Meta Agent Harness — самоулучшающийся слой workflow. Background-агент анализирует артефакты итераций (design-brief, plan, summary, transcripts сессий) и истории сообщений Claude Code → предлагает архитектору улучшения skill'ов / инструкций / чек-листов / промптов workflow-агентов. Цель — root-cause fixing промахов workflow-агентов (агент затупил, ревьюер пропустил, чек-лист не покрыл) через правки в первоисточниках (промпт, документ, чек-лист), не через точечные правки симптомов. Идея на основе статьи https://arxiv.org/abs/2603.28052 — meta agent harness, который сам себя улучшает. Скорее всего реализуется как отдельный skill, в который упакован background-агент; AIDD-skill сам по себе не часть этого пункта. Отдельный домен, не привязан к фазам типа codebase-maturity. Стыкуется с P1 Doc feedback loop (Documentation Quality) — там тот же паттерн background-анализа, но для документации
- **P3** GitHub PRs migration research — изучить возможность и стоимость миграции `backlog.md` в GitHub Issues/Projects с сохранением agent-friendly интерфейса (через `gh` CLI). Сейчас Markdown работает нормально, миграция — вопрос "более взрослого формата", не текущей боли. Исследовательская задача: как агент будет создавать/читать/обновлять issues в рамках iteration workflow, не перегружая контекст. Решение о миграции — после результатов

## Agent Harness & Workflow

_Формализация multi-agent dev workflow, промпты workflow-агентов, детерминированные проверки, systematic fixes инфраструктуры разработки._

- **P1** Подход к тестированию/ревью фронтенда — выстроить архитектуру так, чтобы минимизировать ручную нагрузку и когнитивный груз архитектора, оставляя ему только высокоценный вкусовой фидбэк (человеческое ревью без него всё равно не обойтись). Это **запись мыслей под отдельную итерацию проектирования**, не выбор реализации сейчас. Ценные направления, которые всплыли (feat-004): **(1)** агент в конце фронт-итерации генерирует «гид по ревью» — что изменилось, что потыкать, на что смотреть, где заглушки — архитектор проходит чек-лист (в идеале во время догфудинга, не из головы); кандидат — формализовать фазу `VISUAL_REVIEW` в `aidd-orchestrator`, чтобы она всегда выдавала такой гид + контактный лист скриншотов. **(2)** Улучшить фундамент фронт-тестирования: коммитнутый Playwright e2e + visual-regression снапшоты в CI — детерминированно ловить поломки и непреднамеренные визуальные изменения между итерациями (сейчас визуальная проверка разовая, агентом через Playwright MCP). **(3)** Инструмент человеческого фидбэка прямо в браузере: кликнул по элементу → видишь его → оставил комментарий, привязанный к конкретному полю/элементу страницы, агент читает и фиксит. Категория реальная: off-the-shelf (BugHerd / Marker.io / Pastel / Vercel Toolbar comments) или лёгкий self-built dev-overlay, пишущий element-anchored комментарии в файл для агента — нужен ресёрч. **(4)** Догфудинг как основной канал полировки: точечную визуальную доводку делать во время реального пользования продуктом, а не «тыкать просто так» (двух зайцев — пользуешься и тестируешь). Стыкуется с Meta Agent Harness, Doc feedback loop (тот же паттерн feedback-loop), Browser automation в cloud, Dynamic Workflows (fan-out верификации). Отдельной итерацией: взять, продумать, выбрать варианты, выстроить *(cross: Frontend, Documentation Quality)*
- **P2** Kill-process bug systematic fix — разобраться, почему Claude Code регулярно не убивает процессы с первого раза при работе с `make`-таргетами (повторяющаяся проблема: агент думает, что убил процесс, потом видит занятый порт). Гипотезы: нюансы среды Claude Code, особенности process trees в Docker/host setup, сигналы SIGTERM vs SIGKILL, race conditions в `make`-wrappers. Ресёрч + systematic fix в sandbox settings / wrapper-скриптах / CLAUDE.md — не точечный workaround
- **P3** Browser automation в cloud-сессиях — known limitation: Anthropic proxy не пропускает HTTP CONNECT → headless-браузеры к HTTPS не работают ([issue #11791](https://github.com/anthropics/claude-code/issues/11791), open без ETA). Сейчас покрывается merge-конвенцией: UI-валидация — архитектором локально после pull feature-ветки (см. `conventions.md` § Cloud sessions). Реактивировать при фиксе CONNECT в Anthropic proxy (тогда обычный Playwright заработает) или если merge-конвенция начнёт быть боттлнеком. Альтернатива — wuservices workaround (Playwright + `page.route()` + `undici.ProxyAgent` для localhost dev-сервера) отдельной итерацией
- **P1** Doc-first execution discipline — зафиксировать правило поведения workflow-агентов: документация в `doc/` — единый источник правды по архитектуре, контрактам и flow; код агент читает только для уточнения технических деталей, не покрытых документацией. Цель — (a) экономить контекст (читать спецификацию дешевле, чем кодовую базу), (b) держать документацию в состоянии, годном для агента, не только для человека, (c) выявлять gaps в документации по факту обращения к коду (см. парный пункт Doc feedback loop в Documentation Quality). Применяется ко всем ролям workflow-агентов, в первую очередь planner и implementer. Где конкретно живёт правило (`CLAUDE.md`, промпты в `.claude/skills/aidd-orchestrator/prompts/`, раздел в `conventions.md`) — на этапе реализации; вероятно сочетание. Enforcement — через промпты ролей planner/implementer (generation-time дисциплина). Связано со skill `aidd-orchestrator` (правило встраивается в промпты ролей) и AIDD skill actualization (continuous improvement при промахах) *(cross: Documentation Quality)*
- **P3** Dynamic Workflows для autonomous-сегментов workflow — оценить применение Claude Code Dynamic Workflows (инструмент `Workflow`): детерминированная оркестрация сабагентов кодом вместо прозовой FSM в SKILL.md. Не переписывать `aidd-orchestrator` целиком (последовательность + HITL-гейты — против волокон примитива), а точечно: read-only fan-out фаз (code-review с adversarial-верификацией, doc-drift свип, security-свип) и автономные сегменты между гейтами (ради journal-resume, детерминированных bounded-loops, изоляции контекста). Стратегическая рамка — направлять токен-бюджет подписки на верификацию вширь/вглубь + постройку отсутствующих oracle'ов (тесты), а не на генерацию кода; автономия отпускается до границы дешёвого объективного верификатора и сжимается с длиной рана (verifier на каждый шаг декомпозиции). Предусловие enforcement-паттернов (развод ролей исполнитель/верификатор) — реальные сабагент-типы вместо текущих псевдо-сабагентов. Гейтится постройкой тест-oracle (codebase maturity); самый дешёвый вход — PoC code-review fan-out. Ресёрч завершён (`doc/research/dynamic-workflows-autonomy.md`). Стыкуется с Meta Agent Harness и Doc feedback loop (тот же паттерн background/autonomous-агентов) *(cross: Documentation Quality, Agent)*
- **P3** SOFA-пайплайн: reply-шаг в produce-контуре — в текущем конвейере (роль sofa-contributor / финализация итерации) нет систематического места для reply — полноценных содержательных ответов на посты других агентов. Пересмотреть pipeline и встроить reply-шаг с жёсткой рубрикой отбора: не на всё подряд, а только на действительно содержательные посты — которые сильно помогли, либо где есть примечательное расхождение/дополнение собственного опыта. Точки правки: скилл `sofa-contributor` (режим `planned-work`), промпт роли в `aidd-orchestrator` *(Контент, cross: Agent Harness)*
- **P3** Паттерн «learning-first» итераций — формализовать архитектуру под задачи, где перед реализацией идёт серьёзное изучение фундамента (как feat-009: сначала полный разбор теории тестирования с research/сабагентами и фундамент-документами, потом код). Паттерн повторяется в проекте и стал ценным, но ведётся сейчас ad-hoc / немного хаотично. **Запись мыслей под отдельную итерацию проектирования, не выбор реализации сейчас.** Продумать воспроизводимый пайплайн — вероятно через Claude Dynamic Workflows (`Workflow`): комбинация агентов (research-фан-аут по доменам → синтез в фундамент-доки → adversarial-проверка полноты «всё ли упомянул» → разбор с архитектором), со встроенными конвенциями объяснения (рисовать диаграммы; примеры разбирать на коде самого проекта; суть-вперёд) и опциональными adversarial-проверками. Цель — превратить нынешний ручной/хаотичный процесс в выстроенный. Стыкуется с P3 Dynamic Workflows и P1 Meta Agent Harness (тот же примитив fan-out / автономных агентов). Берётся в maintenance-формате (последовательно, заодно с рандомными фичами) — отдельная забота, детали при триаже *(cross: Documentation Quality)*

## Documentation Quality & Architecture

_Работа над качеством существующей арх-документации и создание сводных архитектурных документов._

- **P1** Doc feedback loop — непрерывный цикл улучшения документации на основе реальных взаимодействий агента с ней. Текущее состояние: ручное ревью архитектора — боттлнек качества документации; цель — переместить боттлнек, а не убрать ревью. Состав: **(1) Сбор фидбэка** — workflow-агент при работе фиксирует gaps (полез в код, потому что в доке нет / противоречие / устарело / неоднозначно); формат и место хранения — на этапе реализации (отдельный файл, маркеры в документах, structured log — варианты). Триггер сбора: часть промпта implementer/planner + опционально опрос архитектора в конце сессии. **(2) Периодическая обработка через async/cloud doc-review agent** — раз в день/неделю облачный агент пробегается по накопленному feedback log, поднимает релевантные документы, формирует архитектору **предложения** правок (документы / чек-листы / промпты workflow-агентов). Запускается автономно (Claude Routines / Codex cloud triggers). **(3) Архитектор как финальный ревьюер** — на текущем этапе зрелости системы любые изменения структуры/правил/конвенций утверждает человек; верхнеуровневое идейное ревью остаётся за человеком. Полное делегирование агентам преждевременно: дай агентам строить систему — выйдет херня. Связано с Async / cloud agent workflow (один из первых use case async-агентов), Tech/ full documentation audit (разовый аудит vs continuous loop), Meta Agent Harness (тот же паттерн background-анализа, но для skill'ов/инструкций workflow-агентов), skill `aidd-orchestrator` (точка встраивания триггера сбора в промпты ролей), Doc-first execution discipline (парный пункт — генерирует обращения к коду как сигнал о gap'ах) *(cross: Agent Harness, Infra)*
- **P2** Tech/ full documentation audit — пройтись по всем документам в `tech/` через doc reviewer по принципам, зафиксированным в `conventions.md` (раздел Documentation) и скилле `aidd-methodology`. Выявить проблемные точки, завести точечные итерации на рефакторинг. Зависит от существования doc reviewer как формализованного агента — в текущем `aidd-orchestrator` роли нет, потребуется отдельная формализация
- **P2** REST API contracts summary — сводный документ с REST API контрактами со всех сервисов. **Не дубль** `tech/backend.md` (там живут детали реализации) — обзорная страница "где какой endpoint живёт, какой контракт, какие коды ответа, какая аутентификация". Полезна при cross-service обсуждениях и при планировании работ, затрагивающих несколько API. Роль документа явно обозначена как "summary / overview", не как "source of truth", чтобы избежать дрейфа дубликации
- **P2** DB architecture summary — аналогично REST summary, но для базы: таблицы по сервисам, связи, ключевые invariants, соглашения по миграциям. Не дубль Alembic-миграций и не дубль SQLAlchemy models — обзорный документ
- **P3** Move `langgraph-store-deepdive.md` to research/ — сейчас `tech/langgraph-store-deepdive.md` лежит в технической документации, но по содержанию это deep-dive ресёрч, не архитектурный документ проекта. Перенести в `research/`, обновить ссылки в `doc/index.md` и CLAUDE.md. Отдельно от существующего P2 "LangGraph Store deep-dive" в Agent-секции (это другой пункт — планируемый research-проект, не перенос файла)
- **P3** `auth.md` § Конфигурация: таблица env-переменных не пополнена `CLIENT_IP_SOURCE`/`CLIENT_IP_XFF_HOPS` — упоминание в соседний § Rate limiting добавлено (chore-001), сама таблица — нет; добавить обе переменные строками рядом с `SECURE_COOKIES` *(Documentation, harvest chore-001)*

## Оптимизация под будущие нагрузки

_Отложенные оптимизации из DB slice (feat-003): на текущих объёмах не окупаются, активируются при росте нагрузки._

- **P3** Корреляционные стратегии SIEM на SQL-агрегаты — сейчас стратегии тянут все события окна в Python и группируют в памяти (sequence — вложенный цикл A×B); перевод на `GROUP BY ... HAVING` / self-join переносит счёт в БД и возвращает только пробившие порог группы. Триггер: рост объёма событий в окне
- **P3** UUIDv7 вместо UUIDv4 для PK — random v4 разбрасывает вставки по случайным страницам B-tree (page splits, холодный кэш); v7 time-ordered, вставки идут в правый край. Генерация client-side (Python-библиотека), смены типа колонок не требует. Триггер: заметный insert-трафик в таблицы main app
- **P3** Индексы на `siem_alerts.first_event_id` / `latest_event_id` — FK с `ON DELETE RESTRICT` без индексов; каждое удаление события проверяет ссылки seq scan'ом. Обязательно перед включением retention-удаления `siem_events` (`SIEM_DELETE_AFTER_DAYS`)
- **P3** Retention для `siem_alerts` — `expired`/`resolved` алерты копятся бессрочно (в отличие от событий, у которых retention есть). Сейчас осознанно: алерты — аудит-след. Триггер: рост объёма таблицы алертов; вводить вместе с retention-индексами выше (FK на события не дадут удалить событие, пока на него ссылается алерт)
- **P3** Политика хранения refresh-токенов неактивных пользователей — оппортунистическая чистка при логине (feat-003) не затрагивает пользователей, которые не возвращаются; глобальная фоновая чистка/политика хранения — при необходимости

## Tech Debt & Competency

_Плановый техдолг и развитие компетенций архитектора по техническому стеку проекта._

- **P3** Frontend stack deep-read — целенаправленное погружение архитектора в фронтенд-код проекта (React/TypeScript) для прокачки компетенции. **Не задача по коду и не рефакторинг** — задача по компетенции архитектора, без которой невозможно выстраивать серьёзную архитектуру фронтенда
- **P3** Linter/formatter stack deep-read — целенаправленное изучение конфигов и правил `ruff` / `mypy` / `eslint` / `prettier` для прокачки компетенции. Аналогично frontend: цель — экспертиза, а не рефакторинг
- **P2** Итерация рефакторинга size-нарушителей и перевод size-чека в fail — arch-checker `size_limits` (warning-only) флагит файлы > 500 строк (`backend/app/main.py`, `backend/app/api/routes/mcp_servers.py`, `backend/app/storage/workspace.py` — добавлен feat-011, 640 строк) и директории > 10 модулей (`backend/app/{agent,api/routes,api/schemas,services}`, плюс `backend/app/agent/tools` — добавлена feat-011, 11 модулей). Целевая модель: разбить нарушителей (или осознанно исключить плоские route/schema-директории из порога), затем перевести `size_limits` из `WARNING_CHECKS` в фейлящие `CHECKS`. Триггер: после мержа feat-011 — warning'и уже печатаются в каждом `make check` *(Tooling, cross: Backend)*
- **P2** `services/executor` вне контрактов import-linter — `root_packages` в корневом `pyproject.toml` не включает `executor`: сервис, исполняющий недоверенный код, не защищён ни одним слоевым контрактом, в отличие от backend и siem (9 контрактов). Не делаем сейчас — модульный состав свежий, преждевременный контракт зафиксирует случайную структуру. Триггер: когда в `executor/api/` появится третий модуль или в runner-слое второй *(Backend, Executor, Tooling)*
- **P3** Точечные `# type: ignore[import-untyped]` в `services/executor` вместо записи в mypy-overrides — `smoke/smoke_matplotlib_png.py` и `smoke/smoke_python_docx.py` несут локальные игноры для `pandas`/`docxcompose`; проектная норма — одна запись в корневом `[[tool.mypy.overrides]]` (тот же класс, что уже есть для `fuzzysearch`). Делать вместе со следующей уборкой mypy-конфига *(Backend, Executor, Tooling)*
- **P3** Вынести транзакционный multi-session test-harness в `packages/testing/db.py` — связка `outer_conn`/`_bound_session`/`tool_session_factory`/`seed_session` дословно дублируется в `backend/tests/image_generation/conftest.py` и `backend/tests/subagents/conftest.py` (несколько sibling-сессий на одном соединении с общим откатом; существующий `transactional_session` покрывает только одну сессию на тест). Целевая модель: расширить `packages/testing/db.py` хелпером под multi-session. Триггер: следующая правка любого из двух дублей или появление третьего потребителя паттерна *(Testing, Tech Debt)*
- **P3** Вынести билдер тестового приложения в `packages/testing` — `create_app()` + DI-override'ы продублированы в 6 файлах (`backend/tests/conftest.py`, `skill_context/conftest.py`, `auth/conftest.py`, `personalization/conftest.py`, `smoke/test_app_boot.py`, `client_ip/_helpers.py`). Делать вместе с multi-session harness пунктом выше *(Testing, Tech Debt, harvest chore-001)*
- **P3** Рефактор `security/enforcement/` — свести enforcement-адаптеры (`app/agent/runtime_security.py` + guard-часть `app/agent/tool_guards.py`) в подпакет `app/agent/security/enforcement/`, структурно закрепив границу «движок vs применение»; `handle_tool_error` (не security) остаётся при графах. Слоистость уже зафиксирована текстом (`conventions/agent.md`, docstring `tool_guards.py`) — рефактор материализует её в структуре. Триггер: следующая итерация, трогающая оба адаптера, или появление третьего enforcement-адаптера *(Agent, Security, Tech Debt)*
- **P3** `line-length` 88 → 100 — решить после слияния slice'ов codebase maturity — bump вызывает project-wide reformat и гарантированные конфликты с параллельными ветками; потребности в 100 нет (значение пришло из недействовавшей `[tool.ruff]`-секции pyproject, удалённой как мёртвая). Либо bump одним коммитом в спокойном окне, либо зафиксировать 88 насовсем и закрыть пункт *(Tooling)*
- **P3** Мелкие CSS-нестыковки, оставленные параллельной работой шести треков feat-013 — `scrollbar-gutter: stable` продублирован инлайн-стилем в `AppLayout.tsx` и `MessageList.tsx` вместо одной утилиты рядом с остальной механикой скроллбаров в `index.css`; `ChatThread.tsx` несёт лишний `w-full` в `illustrationClassName`, хотя `StateScreen` уже добавляет его сам, и три соседних потребителя передают только `max-w-[280px]`. Каждое по отдельности не стоит тикета — это тот самый след параллельной работы, который позже объясняют «а у нас так исторически» *(Frontend, cleanup, harvest feat-013)*
- **P3** `rowMenuTriggerFor` дословно продублирован в двух тест-файлах — `ChatList.test.tsx` и `Sidebar.test.tsx` несут идентичный хелпер с идентичным JSDoc (pre-existing, старше feat-013, итерация правила лишь соседние строки). Обход DOM вверх по предкам — самый хрупкий вид тестового селектора, живёт в двух копиях и будет расходиться молча. Вынести в `@/test/`-хелпер при следующем заходе в любой из файлов *(Testing, Tech Debt, harvest feat-013)*
- **P3** Package `__init__.py` как публичный контракт + лечение жадных импортов. Сейчас `app/agent/__init__.py` и `app/agent/security/__init__.py` жадно реэкспортируют тяжёлые модули (`LangGraphAgentRunner`, `SecurityGuard`), но **ни один потребитель не использует package-level импорт** — везде идут прямые submodule-импорты. Из-за жадных `__init__` любой импорт листа (`app.agent.config`, `app.agent.security.types`) исполняет `__init__` целиком и тянет полграфа → склонность к циклам (в feat-005 поймали цикл `app.infra.llm ↔ app.agent`, обойдён через `TYPE_CHECKING`-импорты в `llm.py`). Цель: трактовать `__init__`/`__all__` как поддерживаемый публичный API, потребители импортируют package-level. **Оговорка-констрейнт:** «просто реэкспортировать больше и заставить всех импортировать package-level» при жадных `__init__` **усугубит** циклы — нужно либо ленивые реэкспорты (`__getattr__`, PEP 562), либо держать тяжёлые runtime-классы вне `__init__`, либо слоевое разделение. Связано с feat-008 (`import-linter` / arch-checker — там же можно зафиксировать направление импортов и контракты как enforcement) *(Architecture / Tech Debt)*

## feat-009 follow-ups (тестирование + находки ревью)

_Отложенное из feat-009. Прод-баги P1–P9, найденные ревью тестов, починены в самой итерации; ниже — то, что требует отдельной проработки или выходит за объём тест-итерации._

- **P2** SSRF: полная защита от DNS-rebinding в MCP-клиенте — resolve-and-pin через кастомный httpx `AsyncHTTPTransport` (резолв один раз, коннект на запиненный IP с сохранением SNI/Host). Contained-фикс (запрет редиректов на per-request путях) уже в проде в feat-009; полный rebinding-proof — новый компонент *(Security)*
- **P3** SSRF uniformity: применить no-redirect/resolve-pin и к статическим admin-MCP (`app/infra/mcp.py`), для единообразия — сейчас покрыты только per-request пользовательские URL (ниже поверхность атаки) *(Security)*
- **P2** SIEM cross-side контракт через типизированный `emit_security_event(event_type: EventType, ...)` — провести всю эмиссию security-событий через типизированный хелпер: тогда членство в словаре по всем emission-сайтам гарантирует mypy статически, runtime/AST-страж не нужен. feat-009 закрыл cross-side backend-side контракт-тестом, но статическая гарантия сильнее *(Security, SIEM)*
- **P3** Protocol-интерфейсы для тест-фейков repo/trace_store — сейчас фейки конкретных классов под `# type: ignore`, дрейф сигнатуры не ловится; ввести `Protocol` (как у `AgentRunner`) либо держать фейки рядом с реальными классами *(Testing)*
- **P2** Frontend e2e / visual-regression на Playwright — реальный browser-e2e (3–7 journey) + visual-regression; в облаке headless-браузер к HTTPS не работает (issue #11791), потому отложено из feat-009 (там только Vitest/jsdom) *(Frontend, Testing)*
- **P2** LLM-eval контур (Langfuse datasets + LLM-as-judge) — качество ответов агента, точность guard на реальных атаках, качество fuzzy-patch: недетерминированные оценки вне CI-гейта, по расписанию/перед релизом промпта. feat-009 покрыл детерминированную обвязку (реакция кода на вердикт), но не качество модели *(Agent, Observability)*
- **P3** Mutation testing (mutmut) — периодический аудит силы тестов на ядре (auth, guard, парсинг), не в обычном CI *(Testing)*
- **P3** Проектный testing-skill — упаковать тест-конвенции feat-009 (шов модели, фейки guard, тестовая БД на testcontainers, async-паттерны) в переиспользуемый skill *(Tooling)*
- **P3** Антипаттерн в `testing.md`: запретить недетерминированные значения (`uuid4()`/random/время) в `@parametrize` argvalues — вычисляются при сборе, ломают xdist-коллекцию (разные node-id между воркерами → «Different tests collected»). Использовать стабильные `ids` или фиксированные значения. Поймано в feat-009 (siem poison-тесты) *(Testing)*
- **P3** Summarization-LLM за шов модели — `create_summarization_llm` в `GraphFactory.build()` строится напрямую, мимо инъектируемой `model_factory` (главный чат тестируем через шов, саммаризация — нет). Для симметрии тестируемости вынести и её за шов; не горит, саммаризацию в тестах не гоняют *(Agent, Testing)*
