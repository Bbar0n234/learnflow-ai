# Test Cases: <feat-XXX — название> / трек <T1>

> Шаблон. Скопируй в `tracks/<id>/test-cases.md` трека, заполни плейсхолдеры `<…>`,
> удали этот блок-цитату. Конвенции ниже **остаются в документе** — тестировщик читает их здесь,
> а не в `conventions.md`.
>
> Единый дом тестового трека — три секции: **Дизайн автотестов** (пишет test-author), **Ручные
> кейсы + статусы** (кейсы формулирует test-author, run-log флипов ведут tester и fixer),
> **Находки ревью [severity+owner]** (пишет test-reviewer). Файл авторит автономно роль
> `test-author` из design-brief **после IMPLEMENT** трека — не архитектор и не «до реализации».

<1-2 абзаца: что за итерация/трек, под что страхуют кейсы (поведение-сохраняющий рефактор / новая
фича), что именно подтверждаем. Если правки меняют поведение — явно перечислить ожидаемые
изменения.>

## Конвенции прохождения (инлайн — это рамка тестировщика)

**Статус и run-log.** У каждого кейса — текущий статус плюс опциональный run-log, если кейс прогонялся не раз:

- `- [x]` + лаконичный результат: что проверялось, что получилось, значимые нюансы. По заполненному чек-листу должно быть видно, что всё работает, без перепрохождения.
- `- [ ] ⚠️` + причина, если кейс не пройден или требует отдельного внимания.
- Кейсы с 👤 — требуют ручного действия / решения архитектора (UI, браузер); тестировщик помечает и эскалирует.
- **Доменные маркеры** (применять, если итерация их касается): `📊` — проверка наблюдаемости (структура БД, метрики, Redis state, Langfuse); `🔴` — проверка реальных инъекций / атак / security-событий; `[auto]` — кейс закрыт автотестом (живёт в `tests/<scope>/`); `*(регресс)*` — кейс страхует «поведение не сломалось» (отделяет регресс от проверки нового поведения).
- **run-log** (только у перепрогнанных кейсов) — строка-история флипов с причиной:
  `runs: r1 ✅ → r2 ❌ (после фикса review #3: регрессия инвалидации) → r3 ✅`.
  Один прогон — run-log не нужен. Перепрогон после правки кода обязателен (см. ре-верификацию).

**Ре-верификация.** Правка кода аннулирует прошлый зелёный статус затронутого. После фиксов: детерминированный гейт (`make check`/`make check-fe`/`make test`) — перепрогон всегда; ручные/UI-кейсы — перепрогон только затронутой области. Каждый перепрогон → запись в run-log.

**Диагностика — через наблюдаемость, не догадки.** Один кейс — одна попытка диагностики: не сошлось — повтори (мог быть транзиент); не сошлось второй раз — fail + эскалация, без долгой отладки. Инструменты: structlog (JSON stdout), Langfuse traces, состояние БД, Redis Streams (`XINFO`/`XLEN`). Код читать только там, где поведение иначе не наблюдаемо (явно отмечать). Код тестировщик не правит: прод-баги, вскрытые кейсом, чинит **fixer** (A6: fixer ≠ автор теста), не сам тестировщик.

**Скоуп по трекам.** Кейсы с префиксом трека (`{T1.1}`) гоняются на своём треке + Layer 0; cross-cutting (Layer 2/3 без префикса) — в INTEGRATION_TEST (`{track_id}=final`). Не пропускать кейсы молча — неприменимый помечать причиной.

### Процесс (тестировщик поднимает стенд сам)

1. Инфраструктура: `make docker-up-db` (Postgres + Redis), `make migrate` (+ siem-миграции), backend `make dev`, siem-service, фронт `make dev-fe` — либо `make docker-up` целиком. <уточнить под итерацию>
2. Акторы через UI register / `/api/auth/register`: <перечислить нужных, напр. **user-a** обычный с данными, **admin** через `make grant-admin USER=<name>`>.
3. Прогон сверху вниз; каждый failed-кейс — повторная попытка, затем фиксация в run-log + `## Решения и обоснования` summary трека.
4. Реальное тестирование через UI/API. После прогона — сводка (pass / failed / **deferred**). Deferred — кейсы 👤/заблокированные (нет ключей, занят стенд): отдельным счётчиком + причина по каждому, чтобы архитектор видел хвост на ручную добивку.

### Где смотреть состояние

| Что | Место |
|-----|-------|
| Фронт | `http://localhost:5173` (Vite) |
| Main app | `http://localhost:8000`, structlog stdout |
| siem-service | `http://localhost:8001` |
| Сеть / SSE | DevTools → Network |
| Логи фронта | DevTools → Console (через `@/shared/lib/logger`) |

---

## Дизайн автотестов

> Пишет **test-author**. Секция **human-facing**: архитектор читает её на гейте PR полностью
> (`doc/tech/conventions/review.md` § Гейт) — пиши для человека, не конспектом. Автотесты живут в
> `tests/<scope>/`; общие утилиты/фейки — из `packages/testing`. Граница авто/ручное — из
> design-brief и `testing.md` § Граница авто/ручное.

**Покрываем автотестом** — по одной записи на суиту (тест-файл), формат фиксированный:

### `<test_file.py>` — <объект под тестом, по-русски>

1. **Файл**: `tests/<scope>/<test_file.py>` — <слой/тип теста>, <шов: MockTransport / реальный Postgres / ASGI-клиент / …>
2. **Тестирует**: `<prod-модуль :: функция/класс>`
3. **Суть**: <1–3 полных предложения с глаголами: что суита гарантирует и от чего страхует. Без номинализаций («проброс», «парсинг») — это слой скимминга: по одним «Сутям» архитектор видит карту покрытия трека, не читая кейсы.>
4. **Кейсы**: <список поведений, по одному на строку; телеграфный стиль здесь допустим — справочный уровень для выборочного нырка>

**Осознанно не покрываем автотестом** — список, каждый пункт триадой: *что — почему — куда уехало* (ручной кейс `{Tx.y}` / другая суита / мягкий гейт glue):

- <что> — <почему> → <куда уехало>

**Замеченные прод-баги (для fixer'а, сам не чиню):** <…>


### Layer 0: Automated gate

- [ ] `make check` — ruff + mypy + **arch-checker** (import-linter + AST-ассерты) → **0 ошибок**.
- [ ] `make check-fe` — ESLint (+ FSD-boundaries) + Prettier `--check` + `tsc -b` strict → **0 ошибок**.
- [ ] `make test-scope P=...` / `make test-fe` — автотесты скоупа зелёные.
- [ ] <grep-проверки на мёртвые ссылки / старые пути, если применимо>

---

## Ручные кейсы + статусы

> Кейсы формулирует **test-author** (узкий ручной хвост — то, что не закрыто автотестом);
> статусы и run-log флипов ведут **tester** и **fixer**. Fixer сюда — только статус-флип и
> максимум одна тезисная фраза; содержательный контекст фикса — в `## Решения и обоснования`
> summary трека.

### Layer 1: <Трек T1 — название>

- [ ] `{T1.1}` <предусловие → действие → ожидаемый результат>
- [ ] `{T1.2}` <…>

### Layer 2: Integration (cross-cutting, в INTEGRATION_TEST)

- [ ] `<описание>`

### Layer 3: E2E (cross-cutting, в INTEGRATION_TEST)

- [ ] `<описание>` 👤 <если требует браузера/ручной проверки>

---

## Находки ревью [severity+owner]

> Пишет **test-reviewer** (adversarial-ревью тестов против контракта, read-only). Каждая находка —
> severity (**blocker** / **major** / **minor**) + владелец фикса: `[test]` (test-author) /
> `[prod]` (fixer) / `[infra]` (`packages/testing`) / `[doc]`. На фазе GREEN fixer чинит `[prod]`,
> test-author — `[test]`; закрытую/эскалированную находку помечают здесь же. Чисто — секция пустая.

- [ ] `<severity>` `<owner>` <находка: что не так, против какого контракта/чек-листа>

---

## Покрытие (опционально)

<Трассируемость кейс ↔ риск/finding: каждый кейс привязан к согласованной правке или findings слайса. Таблица `finding/риск → закрывающие кейсы` — чтобы видеть, что ничего не осталось без проверки. Уместно для рефакторингов и slice-аудитов; для простой фичи можно опустить.>
