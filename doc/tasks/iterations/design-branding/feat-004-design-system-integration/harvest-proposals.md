# Harvest proposals: feat-004 — Design-system integration

Фаза HARVEST. Источник кандидатов: `summary.md` (секция `## Follow-ups`), diff пост-приёмочного гейтинга, итоги разведки недизайненных экранов, backlog. Проверка «не закрыто ли уже» — grep по символам + текущее состояние `backlog.md`.

Особенность итерации: harvest проведён **постфактум** (фаза не отрабатывала в основном прогоне). Большинство хвостов уже занесены в `backlog.md` руками при сворачивании итерации — для них здесь фиксируется статус «уже в backlog» со ссылкой, чтобы не плодить дубли.

## В backlog

| Кандидат | Действие | Статус |
|---|---|---|
| Предпродакшн-гейт заглушек группы B | Переформулировать `backlog.md:15`: часть «скрыть» закрыта в feat-004 фиче-флагом `SHOW_GROUP_B_STUBS`; остаётся «свап на реальные данные» по мере появления бэкенда (un-gate per feature) | **Landing сейчас** (правка существующего пункта) |
| Бэкенд версионирования сферы | — | Уже в backlog (Backend P2) |
| SSE-peek «агент пишет в сферу» | — | Уже в backlog (Backend P2) |
| Rich Artifact UI (реальные типы артефактов) | — | Уже в backlog (Frontend P3) |
| Дизайн экранов auth (login/register) | — | Уже в backlog (Frontend P2) |
| 404 / Not-Found экран | — | Уже в backlog (Frontend P2) |
| Дизайн SIEM/security-экранов | — | Уже в backlog (Frontend P3) |
| Loading-скелетоны + per-query error-состояния | — | Уже в backlog (Frontend P3) |
| SIEM: kill-switch или допил до прод | — | Уже в backlog (Security P2) |
| Подход к тестированию/ревью фронтенда | — | Уже в backlog (Agent Harness & Workflow P1) |

## В конвенции (conventions/frontend.md)

| Кандидат | Действие | Статус |
|---|---|---|
| Паттерн stub-gating незрелых фич | Зафиксировать приём: незрелые/безбэкендные фичи гейтятся за `import.meta.env.DEV` через единый флаг в `shared/config/feature-flags.ts`; в прод-сборке ветка статически мертва (DCE убирает и мок-данные). Точка рендера раз-гейтится индивидуально по мере появления бэкенда | **Landing сейчас** |

## known-trivial

- **Инвентаризация заглушек — свипом, не grep'ом мок-файлов.** Первичная инвентаризация искала по именам `MOCK_*` в `model/mock-*.ts` и пропустила два инлайн-стаба (`ProjectLayout` чип `v2.4.1 · растёт`, `ChatList` контрибьюшн-чипы из индекса строки). Полный охват дал только сплошной свип по маркерам (`mock`/`stub`/`заглушк`/хардкод версий/индекс-производные значения) + grep собранного `dist/`. Урок для фронт-итераций с заглушками; в отдельный пункт backlog не выносится.

## Отсеяно как шум

- **`dist/` в дереве после verify-сборки** — артефакт сборки, gitignored, в коммит не идёт. Не долг.
- **«Редактировать» как `disabled`-кнопка в `ArtifactView`** — это намеренный visual-stub группы A (не fake-данные, а отключённое действие); под гейтинг не подпадает, в проде выглядит корректно как «недоступно».
- **Размер бандла (chunk > 500 kB warning при build)** — общий пункт оптимизации фронта, не порождён этой итерацией; vite-warning стоит давно, не follow-up feat-004.
