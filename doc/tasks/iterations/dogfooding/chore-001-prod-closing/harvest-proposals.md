# Harvest proposals — chore-001-prod-closing

Свод фазы HARVEST. Источники: `tracks/{T1,T2,T3,T4}/summary.md` § Follow-ups (T1–T3 — пусто, T4 секции нет — факт, не ошибка), anytime-кандидаты оркестратора (вошли в этот свод, консолидированы ниже — не дублируются отдельным списком), ADR-029 (отложенных развилок сверх зафиксированного в тексте не найдено), `review-a.md`/`review-b.md` (часть nit'ов уже исправлена фиксами code review, коммит `6e97e04`, и docs-актуализацией, коммит `17dd1e4` — проверено по коду, ниже только оставшееся), `sofa-proposals.md` (сверено, не дублируется — отдельный сток).

## В backlog

| Кандидат | Тип | Приоритет | Целевая модель / триггер | Проверка «не закрыто» |
|----------|-----|-----------|--------------------------|------------------------|
| `backend/Dockerfile` копирует `packages/` и `services/` целиком в прод-образ (тестовый харнесс `packages/testing`, весь исходник `services/siem-service`, который backend не импортирует) — тот же класс мусора, что трек вычищал из venv, но не из `COPY` | tech-debt | P3 | Сузить `COPY` до pyproject.toml/минимума по образцу `tools/*` (`:32-36` уже так делают для tools) | `backend/Dockerfile:41-42`, `git log -- backend/Dockerfile` — строки не менялись треком; подтверждено чтением файла |
| `backend/app/infra/client_ip.py` читает `X-Forwarded-For` через `request.headers.get()` (первый экземпляр), не `getlist()`+склейка — за прокси, дописывающим адрес отдельной строкой заголовка (не через запятую), hop-отсчёт справа пойдёт по целиком клиентской строке | contract/security | P3 | `getlist` + склейка в `client_ip.py`, когда режим `x-forwarded-for` реально понадобится (сегодня прод — `x-real-ip`, недостижимо) | `client_ip.py:51,57` — `.get()`, подтверждено чтением; факт уже зафиксирован в `production.md` |
| Middleware и auth-роуты (`main.py`, `routes/auth.py:122,162,201`) резолвят клиентский IP независимыми вызовами `get_client_ip` — при дрейфе nginx (пропавший `X-Real-IP`) каждый `register`/`login`/`refresh` пишет **два** одинаковых fallback-WARNING вместо одного | tech-debt/observability | P3 | Резолвить IP один раз в middleware, класть в `request.state`, роутам читать оттуда | `main.py:680`, `auth.py:122,162,201` — по-прежнему независимые вызовы, подтверждено чтением; nit review-A/B, не исправлен фиксами `6e97e04` |
| Билдер тестового приложения (`create_app()` + override'ы) продублирован в 3+ `conftest.py` (`backend/tests/conftest.py`, `skill_context/conftest.py`, `auth/conftest.py`, `personalization/conftest.py`, `smoke/test_app_boot.py`, `client_ip/_helpers.py`) | tech-debt | P3 | Вынести общий билдер в `packages/testing` | `grep -rl "def create_app"` — 6 файлов, подтверждено |
| Fail-fast старта на недоступности built-in MCP: `_validate_subagent_registry`/`main.py` роняет приложение при сетевой ошибке к внешнему MCP (`mcp.firecrawl.dev`) в момент старта — рестарт/деплой в окне недоступности = прод не поднимается | tech-debt/reliability | P2 | Graceful-деградация реестра субагентов при отсеве built-in MCP; нужна санкция архитектора — меняет модель отказа старта | `main.py:130-200` — поведение не изменено треками (не регрессия chore-001), подтверждено чтением |
| Langfuse: метка `production` промпта `system--production` указывает на версию 2, `latest` — версия 3. Рантайм читает `latest` (не функциональный блокер), но UI-вид «что в проде» врёт | ops-checklist | P2 | Синхронизировать метки в Langfuse перед деплоем | Внешнее состояние Langfuse, не в коде — проверка недоступна из worktree; факт зафиксирован tester'ом T3 (эскалация № 2, кейс {T3.6}) |
| `backend/app/agent/runner.py:283` — `review_events = self._enforcer.active` хранит булев «защита активна», но имя и `if review_events:` (`:284,:305`) читаются как «есть события». Поведение корректно, риск — только читаемость | tech-debt | P3 | Переименовать в `review_active`/`announce_review` | `runner.py:283-305` — не изменено фиксами review-A/B, подтверждено чтением |
| `frontend/src/app/router.tsx` не имеет catch-all роута: при `SIEM_ENABLED=false` закладка на `/security` рендерит пустой DOM вместо экрана ошибки (заодно закрывает пре-существующее отсутствие 404 для опечаток в URL) | contract/UX | P3 | **Решение архитектора (гейт chore-001)**: паттерн — любой URL, не попадающий в легитимные роуты, обрабатывается общим 404-экраном («страница не найдена»), не редиректом: catch-all `path="*"` → компонент NotFound внутри layout-роута | `router.tsx` — catch-all отсутствует, подтверждено чтением и диффом `develop...HEAD` |
| `SIEM_ENABLED` парсится двумя разными правилами: pydantic принимает `0`/`False`/`no` как выключено, фронт (`feature-flags.ts:38-39`) гасит UI только на литеральном `"false"` через `build.args` passthrough — `SIEM_ENABLED=0` в проде даёт погашенную эмиссию при живой кнопке «Безопасность» и `502` на `/siem/` | contract | P2→P3 | **Решение архитектора (гейт chore-001): пока живём с build-time флагом и дисциплиной «писать именно `false`» (задокументировано в `production.md`); структурное закрытие — кандидатом «runtime-конфигурация фронтенд-флагов» (строка ниже), рассинхрон парсинга исчезает там by design** | `feature-flags.ts:38-39` — правило не изменено; `docker-compose.yml:47` — passthrough не изменён; подтверждено чтением |
| Runtime-конфигурация фронтенд-флагов: Vite SPA статически подставляет `import.meta.env.VITE_*` при `npm run build` — серверного процесса у фронта нет, смена флага требует пересборки образа. **Решение архитектора (гейт chore-001): сейчас не критично (переключения ~раз в месяц), делаем при реальной потребности в операционном тумблере без пересборки.** Два рабочих варианта: **(A) config endpoint** — фронт при старте запрашивает `GET /api/config` из `Settings`; плюсы: единственный источник истины — pydantic; минусы: флаг становится асинхронным (задержка первого рендера либо перестройка после ответа), новый публичный контракт. **(B) инжект в `index.html`** — FastAPI при отдаче `index.html` подставляет `<script>window.__CONFIG__ = {...}</script>` из `Settings`; плюсы: флаг синхронный, роутер строится как сейчас, замена источника в `feature-flags.ts` минимальна; минусы: FastAPI шаблонизирует `index.html` (аккуратность с кэшированием), неявный контракт «бандл ждёт `window.__CONFIG__`». Оба работают по рестарту app; оба структурно закрывают рассинхрон парсинга `SIEM_ENABLED` (фронт получает распарсенный boolean) и снимают `build.args`-провод (вместе с ним — кандидата про npm-кэш). Предпочтение при реализации — B | tech-debt/contract | P3 | Триггер активации: реальная потребность переключать фронтенд-флаги без пересборки (частые тумблеры / несколько флагов). Целевая модель — вариант B (инжект `window.__CONFIG__`) | Новый кандидат с гейта chore-001, решение архитектора; кода нет — закрывать нечего |
| `backend/Dockerfile:4-5` — `ARG`/`ENV VITE_SIEM_ENABLED` стоят до `COPY frontend/package*.json`+`RUN npm ci`, поэтому переключение тумблера инвалидирует кэш-слой npm-установки при каждой пересборке | optimization | P3 | Перенести пару `ARG`/`ENV` вниз, непосредственно перед `RUN npm run build`. Дешёвый промежуточный фикс, пока build-time флаг остаётся (см. кандидата «runtime-конфигурация фронтенд-флагов» — при варианте B провод исчезает вместе с проблемой) | `Dockerfile:4-9` — порядок не изменён, подтверждено чтением |
| `backend/app/infra/rate_limit.py` — `_buckets` растёт неограниченно: ключ создаётся на каждый уникальный IP и не удаляется, даже когда список таймстемпов опустел (pre-existing, до T1 эксплуатировалось тривиально через спуфинг левого `X-Forwarded-For`; T1 сужает пространство ключей до реальных адресов, но не добавляет очистку) | tech-debt (pre-existing) | P3 | Lazy-удаление пустых бакетов при чтении | `rate_limit.py:8,17-20` — `git log develop..HEAD` по файлу пуст, файл треками не тронут, подтверждено чтением |
| `doc/tech/auth.md` § Конфигурация (`:167-175`) — таблица env-переменных домена всё ещё без `CLIENT_IP_SOURCE`/`CLIENT_IP_XFF_HOPS`. Упоминание добавлено в соседний § Rate limiting (`:95`, коммит `17dd1e4`), но сама таблица не пополнена | doc-gap | P3 | Добавить обе переменные строками рядом с `SECURE_COOKIES` | `auth.md:167-175` — таблица не содержит `CLIENT_IP_*`, подтверждено чтением после коммита `17dd1e4` |

## В конвенции (conventions.md)

Кандидатов нет. Единственная норма, всплывшая по ходу итерации (парность флагов `uv sync` в обоих вызовах `backend/Dockerfile`), уже внесена в `conventions.md` § Dockerfile фиксом review A/B (коммит `6e97e04`) — повторно заводить нечего.

## known-trivial (не в backlog, фиксируем как известное)

- Write-tool guard дважды отказал сабагентам в создании легитимных артефактов трека (`tracks/*/summary.md`) как «report-файлам»; обход — Bash/heredoc. Если повторится за пределами итерации — кандидат на правку промптов оркестратора (явная пометка «артефакт, не отчёт»).
- Описание маркера `integration` в `backend/pyproject.toml:42` разошлось с фактическим применением (маркер ставится и на HTTP-интеграцию без Postgres); сами тесты корректны. Находка T1-R4.
- Четыре кейса `backend/tests/security/test_llm_defense_toggle.py:237,250,264,286` дёргают приватную `_validate_builtin_mcp` без докстринг-обоснования (соседний `siem_toggle/test_emission_startup.py` для той же задачи объясняет свой выбор). Находка review-B.
- Verification-критерий плана T4.3 («`siem_pgdata` по-прежнему объявлен в `volumes:` под `docker compose config` в обоих случаях») не подтверждается буквально под Compose v5.3.1: без активного профиля `siem` резолвер вычищает из рендера всё дерево, ссылающееся только на исключённые сервисы, включая volume. Объявление в самом `docker-compose.yml` (source of truth) не тронуто. Implementer решение не принимал самостоятельно — факт зафиксирован в `tracks/T4/summary.md` § Эскалации (T4.3), архитектору на решение: считать пункт закрытым по духу (source-level) или переформулировать критерий.

## Отсеяно как шум (с причиной — для прозрачности гейта)

- `test_active_model_prices_within_drift_tolerance` (живой прайсинг `z-ai/glm-5.2` разошёлся с `configs/pricing.yaml`) — **закрыто в итерации по поручению архитектора**: `configs/pricing.yaml` актуализирован (коммит `03bdf09`), тест зелёный. Review-B nit о том, что обоснование жило только в незакоммиченном `harvest-proposals.md`, снимается тем, что этот файл коммитится вместе с итерацией.

### Проверено отдельно — уже закрыто фиксами ревью, не заводится повторно

Ниже — nit'ы review-A/B, для которых проверка «не закрыто ли уже» дала да; перечислены для прозрачности гейта, действий не требуют.

- review-A: `main.py` — `classifier`/`guard_observer` могли остаться несвязанными при `LLM_DEFENSE_ENABLED=false` (NameError-риск) — закрыто `6e97e04` (guard-фабрика определяется в своей ветке).
- review-A: `prompt_provider.py` — нет детектора нерастворённых `{{ }}` в промпте — закрыто `6e97e04` (детектор добавлен).
- review-B: `CLIENT_IP_XFF_HOPS` отсутствовал в `.env.local.example` — закрыто `6e97e04`.
- review-B: метапометка «зафиксировать дисциплину синхронизации nginx-конфига при следующей правке» в `production.md` — закрыто `6e97e04` (заменено констатацией факта).
- review-B: `conventions.md` § Dockerfile не фиксировал инвариант парности флагов `uv sync` в обоих вызовах — закрыто `6e97e04`.
- review-B «Незамеченный дрейф документации»: `siem-service.md` (kill-switch не упомянут), `streaming.md`, `agent-runtime.md`, `security/architecture.md`, `ADR-017`, `backend.md`, `observability.md`, `conventions/agent.md` — все закрыты `17dd1e4`.
