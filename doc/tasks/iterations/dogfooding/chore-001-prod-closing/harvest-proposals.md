# Harvest proposals — chore-001-prod-closing

Anytime-записи оркестратора по ходу итерации; harvester на фазе HARVEST сводит их с `## Follow-ups` треков, не теряя и не дублируя.

## Anytime-кандидаты (оркестратор)

- **[backlog, P3, Infra]** Исходники и тесты в прод-образах: `backend/Dockerfile` копирует `backend/` целиком (включая `backend/tests/`, ~704 KB), `packages/` и `services/`; siem-образ — `services/siem-service/tests/` и `packages/testing/`. Та же категория, что вычищенные dev-deps (тестовый и чужой код в прод-образе), но сужение `COPY` — изменение состава образа с риском для workspace-резолвера; в chore-001 осознанно не делалось (Open Question № 1 плана T2, резолюция «строго бриф»). Кандидат: сузить `COPY` до рантайм-минимума отдельной итерацией. Источник: `tracks/T2/plan.md` § Open Questions, находка R6 test-review T2.
- **[доработка скилла/харнесса, known-trivial?]** Write-tool guard дважды отказал сабагентам в создании легитимных артефактов трека (`tracks/*/summary.md`) как «report-файлов»; обход — Bash/heredoc. Если повторится за пределами итерации — кандидат на правку промптов оркестратора (явная пометка «артефакт, не отчёт»).
- **[backlog, P3, Security]** Режим `x-forwarded-for` читает заголовок через Starlette `.get()` — первый экземпляр, не склейка всех (`getlist`). За прокси, дописывающим адрес отдельной строкой заголовка (не через запятую), отсчёт hops справа пойдёт по целиком клиентской первой строке — режим молча перестанет защищать. Сегодня недостижимо (nginx склеивает `$proxy_add_x_forwarded_for`, прод-режим — `x-real-ip`). Кандидат: `getlist` + склейка в `client_ip.py` при реальном переходе на режим; факт зафиксирован в `production.md` (DOC_UPDATE chore-001). Источник: находка T1-R6 test-review T1 (подтверждена измерением).
- **[doc, known-trivial]** Описание маркера `integration` в `backend/pyproject.toml:42` разошлось с фактическим применением (маркер ставится и на HTTP-интеграцию без Postgres). Находка T1-R4; сами тесты корректны.
- **[конвенции/infra, P3]** Билдер тестового приложения (`create_app()` + override'ы) дублируется третий раз по скоупам — кандидат на вынос в `packages/testing`. Находка T1-R5.
- **[backlog, P2, Backend/Agent]** Fail-fast старта на недоступности built-in MCP: при отсеве сервера (сетевая ошибка к `mcp.firecrawl.dev` в момент старта) `_validate_subagent_registry` роняет приложение (`unknown tool name(s) firecrawl_*` → `Application startup failed`). На проде это значит: рестарт/деплой в момент недоступности внешнего MCP = прод не поднимается. Не регрессия chore-001 (воспроизводится при обоих положениях тумблера, код треками не менялся). Кандидат: graceful-деградация реестра субагентов при отсеве built-in MCP (санкция архитектора нужна — меняет модель отказа старта). Источник: эскалация № 1 tester T3.
- **[ops/prod-checklist, P2]** Langfuse: метка `production` промпта `system--production` указывает на версию 2 (2026-04-14), тогда как `latest` — версия 3. Рантайм читает `latest`, функционально не блокер, но расхождение меток стоит разрешить перед деплоем (иначе UI-вид «что в проде» врёт). Источник: эскалация № 2 tester T3, кейс {T3.6} 👤.
- **[backlog, P3, Backend]** Middleware и auth-роуты резолвят клиентский IP независимо — при дрейфе nginx (пропавший X-Real-IP) каждый auth-запрос даёт два одинаковых fallback-WARNING. Кандидат: per-request кэш результата `get_client_ip` (request.state). Источник: nit review-A/B chore-001.
- **[test, known-trivial]** Четыре кейса `test_llm_defense_toggle.py` дёргают приватную `_validate_builtin_mcp` без комментария-обоснования (соседний siem-тест той же задачи объясняет выбор lifespan-прогона). Кандидат: строка-обоснование при следующем касании файла. Источник: nit review-B.
- **[ops, P3, Agent]** `test_active_model_prices_within_drift_tolerance` (`@pytest.mark.external`) стабильно красный: живой прайсинг `z-ai/glm-5.2` разошёлся с `configs/pricing.yaml` на 12.6% при допуске 10%. Не связано с chore-001 (наблюдалось во всех полных прогонах итерации). Кандидат: обновить pricing seed по методике `reference/model-selection.md`. — **Закрыто в итерации по поручению архитектора**: `configs/pricing.yaml` (`z-ai/glm-5.2`) актуализирован точными значениями живого каталога OpenRouter (input 6.72e-07 → 7.686e-07, output/output_reasoning 2.112e-06 → 2.4156e-06, input_cache_read 1.248e-07 → 1.4274e-07); тест зелёный.
