## Code Review Report — режим B (соответствие контракту)

Итерация: chore-001 (B) «Prod-closing». Диапазон: `git diff develop...HEAD` (61 файл). Сверялся с `design-brief.md` (вкл. § Партиция треков), `tracks/T1..T4/{plan,summary}.md`, `doc/tech/conventions.md` и доменными `conventions/{api,agent,frontend,testing}.md`, затронутыми документами `doc/`.

### Summary
- blocker: 0
- nit: 7
- pre-existing: 0

Контракт брифа выполнен по всем четырём трекам. Проверено адресно и совпадает: env-правило четырёх мест (`LLM_DEFENSE_ENABLED`, `SIEM_ENABLED`, `CLIENT_IP_SOURCE` — все четыре места; исключение по `CLIENT_IP_XFF_HOPS` — nit 1); вокабуляр `siem-contracts` не тронут (`packages/` без диффа); `packages/testing` не тронут; CI (`.github/workflows/`) не тронут; `configs/security.yaml` (`classifier_enabled`) не тронут; список гасимых ключей `prompt_fragments.yaml` (`_SECURITY_HEADER_KEYS`/`_SECURITY_WRAPPER_KEYS`, `agent/config.py:69-74`) совпадает с брифом § 1 «Гасимые ключи» шесть в шесть, структурные ключи остаются в обоих режимах; `_validate_builtin_mcp` не скипается целиком (`main.py:137-160`) — fetch и отсев по сетевой ошибке безусловны; раннер не читает `Settings` и не лезет в приватные поля enforcer'а (`runner.py:283`, `runtime_security.py:60-68`); обе ветки сборки guard'а согласованы одной проверкой флага в фабрике (`main.py:415-417`); мёртвый `get_security_guard` удалён; INFO-строки выключенных состояний присутствуют ровно там, где требует бриф § 1 «Принятые следствия» (`main.py:326`, `main.py:412`); оба `uv sync` в обоих Dockerfile'ах несут `--no-dev --package`, оба `entrypoint.sh` — `UV_NO_SYNC=1`; `docker-compose.yml` объявляет каждую переменную явно в `environment:`, без `env_file:`.

Два места, где буква брифа фактически неточна (сужение runbook-команды `docker compose --profile siem down siem-service siem-db`; формулировка «финальный sync их не удаляет» против «слои остаются в образе»), проверены: реализация и комментарии в `backend/Dockerfile:19-25` / `production.md:148-154` содержат корректную версию, отступления зафиксированы в `tracks/T4/summary.md` и `tracks/T2/summary.md`. Как договорено — не блокеры реализации, идут архитектору правкой брифа.

Осознанное изменение рендера промпта (canary-строка после `</system_instructions>`) зафиксировано в `tracks/T3/summary.md` и подтверждено по диффу `configs/prompts/system.txt` + `configs/prompt_fragments.yaml`: кроме позиции этой строки, перекладка блока текстуально идентична.

### Замечания

| Severity | Намерение | Файл:строка | Норма (ссылка) | Замечание | Предложение |
|---|---|---|---|---|---|
| nit | Завести env-поверхность источника клиентского IP | `.env.local.example:8-9`, `backend/app/config.py:26` | conventions.md § Docker → Что попадает в env: «При добавлении env-переменной — одновременное обновление `.env.example`, `.env.local.example`, `docker-compose.yml` и `Settings`. Все четыре места — один atomic change» | `CLIENT_IP_XFF_HOPS` заведён в трёх местах из четырёх: в `.env.local.example` его нет, хотя соседний `CLIENT_IP_SOURCE` там есть закомментированной строкой. Отступление осознанное и записано (`tracks/T1/summary.md` § Решения — уточнение № 3 PLAN_REVIEW + принцип «только отличия от `.env`»), поэтому не blocker; но правило конвенции сформулировано без исключений, и следующий читатель `.env.local.example` увидит асимметрию без объяснения | Либо добавить `# CLIENT_IP_XFF_HOPS=1` для симметрии, либо внести в conventions.md § Docker явное исключение: параметр, не читаемый при дефолтном режиме local dev, в local-шаблон не выносится |
| nit | Единая точка чтения клиентского IP | `backend/app/main.py:680` + `backend/app/api/routes/auth.py:122,162,201` | conventions.md § Logging → Антипаттерны (шум); § Обработка ошибок → Восстановление (наблюдаемая, но читаемая деградация) | Middleware и auth-роут резолвят IP независимыми вызовами `get_client_ip`. При дрейфе nginx (`CLIENT_IP_SOURCE=x-real-ip`, заголовка нет) каждый `register`/`login`/`refresh` даст **две** одинаковые строки `client ip fallback to socket` вместо одной. Сигнал корректен, но удваивается ровно на тех путях, где он важнее всего | Либо принять как есть (сигнал всё равно читаем, объём ограничен auth-роутами), либо класть резолвленный IP в `request.state` в middleware и читать его в роутах — тогда fallback-WARNING остаётся один на запрос |
| nit | Зафиксировать дисциплину работы с nginx-конфигом | `doc/tech/setup/production.md:130` | CLAUDE.md § Документация: «Документация описывает текущее состояние — без временных метапометок… Планы живут в тасклистах» | Фраза «Выбранную дисциплину синхронизации (единственный файл в `sites-available` + симлинк из `sites-enabled`) зафиксировать здесь же при следующей правке этого документа» — это отложенная задача внутри архитектурного документа. Такой пункт гниёт: следующая правка документа его не обязательно заметит | Сформулировать дисциплину прямо сейчас как факт («конфиг живёт в `sites-available`, `sites-enabled` — симлинк») либо вынести пункт в `doc/backlog.md` |
| nit | Привести § Dockerfile к целевым флагам | `doc/tech/conventions.md:124` | conventions.md § Documentation → Принципы (конвенция — источник истины, grep-абельная норма); design-brief § 4 | Норма задаёт целевую форму `uv sync --locked --no-dev --package <пакет>`, но теряет инвариант, который бриф § 4 называет обязательным условием эффективности правки: **оба** вызова `uv sync` в файле (кэш-слой и финальный) обязаны нести одинаковые флаги, потому что кэш-слой физически остаётся в образе. Сегодня это знание живёт только в комментариях самих Dockerfile'ов (`backend/Dockerfile:23-25`) — конвенция не спасёт следующий сервис от ровно той же ошибки | Дописать одну фразу в § Dockerfile: «флаги дублируются во всех вызовах `uv sync` внутри файла — то, что установил кэш-слой, остаётся в образе даже если финальный sync это подрежет» |
| nit | Покрыть startup-валидацию MCP при выключенном guard'е | `backend/tests/security/test_llm_defense_toggle.py:237,250,264,286` | conventions/testing.md § Антипаттерны → «Тестирование приватного»; § Чек-лист автора («не приватные методы и внутренние вызовы») | Четыре кейса вызывают приватную `main_module._validate_builtin_mcp` напрямую. Смягчающее: публичного шва вокруг startup-валидации нет (composition root), а соседний `siem_toggle/test_emission_startup.py` для аналогичной задачи выбрал другой путь — прогон реального `lifespan` с обрывом на инфраструктурном вызове, и объяснил выбор в докстринге. Здесь выбор не объяснён, хотя в этом же файле подробно обоснованы другие решения | Дописать в докстринг файла, почему приватный вызов предпочтён прогону lifespan (цена/хрупкость), либо согласовать с архитектором как явное исключение из нормы |
| nit | Закрыть красный `test_pricing_external` по поручению архитектора | `configs/pricing.yaml:46-49` (коммит `03bdf09`) | CLAUDE.md § Документация → «Исправляй дрейф на месте… исправления идут тем же коммитом или отдельным, но **всегда упоминаются в отчёте**» | Правка вне скоупа брифа и вне файловых скоупов всех четырёх треков. Обоснование существует и корректное (`harvest-proposals.md:14` — «Закрыто в итерации по поручению архитектора»), но сам файл не закоммичен (untracked), то есть в истории ветки след решения — только сообщение коммита | Закоммитить `harvest-proposals.md` вместе с итерацией либо перенести пункт в summary трека / отчёт итерации, чтобы обоснование пережило сессию |
| nit | Довести build-time флаг SIEM до фронта | `frontend/src/shared/config/feature-flags.ts:39-40` ↔ `backend/app/config.py:56`, `docker-compose.yml:46-47` | conventions.md § Docker → Что попадает в env (одно значение — один смысл); design-brief § 2 «Две строки в `.env` — один смысловой переключатель» | Один и тот же `SIEM_ENABLED` парсится двумя разными правилами: pydantic принимает `0`/`False`/`no` как выключено, фронт гасит UI только на литеральном `"false"` (сырой passthrough через `build.args`). При `SIEM_ENABLED=0` прод получает погашенную эмиссию при живой кнопке «Безопасность» и `502` на `/siem/`. Не blocker: расхождение найдено самим треком, задокументировано предупреждением оператору (`production.md:146`), закреплено тестами с обеих сторон и **эскалировано архитектору** (`tracks/T4/summary.md` § T4.7, фикс R1) | Решение за архитектором. Отмечаю, что на сегодня корректность прода держится на дисциплине оператора («писать именно `false`»), а не на коде — если ответ будет «оставить как есть», это стоит зафиксировать в `production.md` как принятое следствие, а не только как предупреждение |

### Blocker без прецедента в conventions

Нет. Блокеров не найдено — ни таких, для которых норма существует, ни таких, для которых её пришлось бы изобретать.

### Незамеченный дрейф документации

Для docs-updater. Бриф § Партиция треков выносит актуализацию архитектурной документации в фазу DOC_UPDATE после барьера и перечисляет адресатов: `doc/security/architecture.md`, ADR-017/022/023/024, `doc/tech/agent-runtime.md`, `doc/tech/conventions/agent.md`, `doc/tech/backend.md`, `doc/tech/streaming.md`, `doc/tech/observability.md`. Ниже — конкретные места в этих документах, чтобы фаза не искала их заново, **плюс два документа, которых в списке брифа нет** (это и есть незамеченная часть).

**Не покрыто списком DOC_UPDATE — требует решения, а не просто правки:**

- `doc/tech/siem-service.md` — документ описывает подсистему, которую итерация выключает, но kill-switch в нём не упомянут вовсе: ни `SIEM_ENABLED`, ни `profiles: ["siem"]`, ни `VITE_SIEM_ENABLED`. Трек T4 правил здесь только префикс `/api` (`:149-156`). Читатель документа сегодня не узнает, что подсистема в проде выключена и как её вернуть. Минимум — абзац с отсылкой на `setup/production.md` § SIEM.
- `doc/tech/auth.md` — § Конфигурация (`:167-172`) даёт исчерпывающую по форме таблицу env-переменных домена (`JWT_SECRET`, `ACCESS_TOKEN_*`, `SECURE_COOKIES`), а `:93` объясняет составной ключ rate-лимита «username + IP». Именно этот IP теперь определяется `CLIENT_IP_SOURCE`/`CLIENT_IP_XFF_HOPS`, которых в таблице нет. Прямого противоречия тексту нет (`X-Forwarded-For` в auth.md не упоминается), поэтому это дрейф по полноте, а не по факту.

**Покрыто списком DOC_UPDATE — точные места:**

- `doc/tech/streaming.md:25-26, 38, 65-70` — `final_output_review_started`/`_complete` описаны как безусловная пара вокруг end-of-stream проверки, включая sequenceDiagram. При `LLM_DEFENSE_ENABLED=false` события не эмитятся (`runner.py:283-306`). Контракт SSE читает фронт (`shared/api/sse.ts:15-16`, `useAgentStream.ts:200-203`) — формулировку про «индикатор проверки» надо оговорить режимом.
- `doc/tech/agent-runtime.md:90` — раскладка system message показывает `<system_instructions> ← hardening preamble + canary token` как часть `system.txt`; текст переехал в `configs/prompt_fragments.yaml`, а в шаблоне остался слот `{{ security_preamble_section }}`. Там же `:383` (таблица роли `prompt_fragments.yaml` — теперь ещё и преамбула) и `:408` (`CANARY_SECRET`: «пустой = canary отключён + warning» — WARNING больше не эмитится при выключенной защите, `main.py:592`).
- `doc/security/architecture.md:92` (тот же блок `<system_instructions>`), `:230` (таблица `prompt_fragments.yaml`), `:237` (env-таблица без `LLM_DEFENSE_ENABLED`), `:65` (canary как безусловный элемент composition).
- `doc/tech/adr/ADR-017-prompt-injection-defense.md:61` — «hardening-секции (`<system_instructions>`, `<instruction_reminder>`) структурированы отдельно [в system.txt]»: решение ADR в силе, но место жительства текста изменилось.
- `doc/tech/backend.md:582-586` — «Ключевые env vars» без `LLM_DEFENSE_ENABLED`, `SIEM_ENABLED`, `CLIENT_IP_SOURCE`, `CLIENT_IP_XFF_HOPS`. Список выборочный, но два операционных kill-switch'а прода — ровно тот случай, ради которого он существует.
- `doc/tech/observability.md:113, 115-125, 138` — score `security_verdict`, guardrail-observation'ы `guard-<checkpoint>` и изолированный cost tracking guard-модели описаны как всегда присутствующие; при defense-off их не будет ни одного. Бриф § 1 «Принятые следствия» это предсказывает, документ — нет.
- `doc/tech/conventions/agent.md:15, 24` — `RuntimeSecurityEnforcer` описан четырьмя чекпоинтами без упоминания состояния «защита не построена»; у класса появился публичный признак `active`, а сам enforcer штатно живёт с `guard=None`.
