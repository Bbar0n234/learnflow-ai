# Test Cases: feat-001 — Видимость работы агента / трек T1

Трек T1 сделал серверную половину «видимости работы агента»: контракт SSE v2 (новый словарь
событий, heartbeat, терминальный `cancelled`, generic `security_block`), семантический
custom-канал, вложенность субагента через `parent_call_id`, typed parts в истории и попутные
фиксы аудита. Это **новая фича с изменением контракта**, а не поведение-сохраняющий рефактор:
события `tool_start`/`tool_end` исчезли, `error {"Request was cancelled."}` заменён на
`cancelled {}`, `security_block` потерял payload, `MessageOut` получил `parts`, один ход агента
теперь отдаётся одним сообщением истории вместо нескольких.

Кейсы страхуют ровно то, что в этом контракте ломается тихо. Половина трека — про события,
отсутствие которых ничего не роняет: пропавший `agent_event`, потерянный шаг субагента, не
закрытая строка срезанного guard'ом вызова, утёкшие в чат токены суммаризатора. Ни одна из этих
поломок не даёт исключения — поток просто становится беднее или грязнее, — поэтому каждая
покрыта отдельно и, где это имеет смысл, сквозным прогоном через настоящий `astream`.

## Конвенции прохождения (инлайн — это рамка тестировщика)

**Статус и run-log.** У каждого кейса — текущий статус плюс опциональный run-log, если кейс прогонялся не раз:

- `- [x]` + лаконичный результат: что проверялось, что получилось, значимые нюансы. По заполненному чек-листу должно быть видно, что всё работает, без перепрохождения.
- `- [ ] ⚠️` + причина, если кейс не пройден или требует отдельного внимания.
- Кейсы с 👤 — требуют ручного действия / решения архитектора (UI, браузер); тестировщик помечает и эскалирует.
- **Доменные маркеры**: `📊` — проверка наблюдаемости (структура БД, метрики, Redis state, Langfuse); `🔴` — проверка реальных инъекций / атак / security-событий; `[auto]` — кейс закрыт автотестом (живёт в `tests/<scope>/`); *(регресс)* — кейс страхует «поведение не сломалось».
- **run-log** (только у перепрогнанных кейсов) — строка-история флипов с причиной:
  `runs: r1 ✅ → r2 ❌ (после фикса review #3: регрессия инвалидации) → r3 ✅`.
  Один прогон — run-log не нужен. Перепрогон после правки кода обязателен (см. ре-верификацию).

**Ре-верификация.** Правка кода аннулирует прошлый зелёный статус затронутого. После фиксов: детерминированный гейт (`make check`/`make test`) — перепрогон всегда; ручные/UI-кейсы — перепрогон только затронутой области. Каждый перепрогон → запись в run-log.

**Диагностика — через наблюдаемость, не догадки.** Один кейс — одна попытка диагностики: не сошлось — повтори (мог быть транзиент); не сошлось второй раз — fail + эскалация, без долгой отладки. Инструменты: structlog (JSON stdout), Langfuse traces, состояние БД (`checkpoint_blobs`), DevTools → Network (SSE-кадры). Код читать только там, где поведение иначе не наблюдаемо (явно отмечать). Код тестировщик не правит: прод-баги, вскрытые кейсом, чинит **fixer** (A6: fixer ≠ автор теста).

**Скоуп по трекам.** Кейсы с префиксом трека (`{T1.1}`) гоняются на своём треке + Layer 0; cross-cutting (Layer 2/3 без префикса) — в INTEGRATION_TEST. Не пропускать кейсы молча — неприменимый помечать причиной.

### Процесс (тестировщик поднимает стенд сам)

1. Инфраструктура: `make docker-up-db` (Postgres + Redis), `make migrate`, backend `make dev`. Фронт для кейсов этого трека не обязателен — SSE смотрится через DevTools или `curl -N`; для кейсов «как это выглядит в ленте» нужен `make dev-fe` (уже на готовом T2).
2. Актор: один обычный пользователь через UI register / `/api/auth/register`, с проектом и чатом.
3. Ключи: для {T1.1}/{T1.3}/{T1.4} нужны живой ключ reasoning-модели и рабочий MCP-сервер (firecrawl) — без них кейсы уходят в deferred с указанием причины.
4. Прогон сверху вниз; каждый failed-кейс — повторная попытка, затем фиксация в run-log + `## Решения и обоснования` summary трека.

### Где смотреть состояние

| Что | Место |
|-----|-------|
| Main app | `http://localhost:8000`, structlog stdout |
| Сеть / SSE | DevTools → Network → EventStream, либо `curl -N -H "Authorization: Bearer …"` |
| Чекпоинтер | Postgres: `checkpoints` / `checkpoint_blobs` |
| Трейсы | Langfuse (спан рана, security-обсервации) |
| Фронт | `http://localhost:5173` (Vite) — для кейсов ленты |

---

## Дизайн автотестов

Покрытие разложено по трём уровням. Мапперы (`TokenChunkMapper`, `StreamEventMapper`,
`emit_agent_event`, `HeartbeatPacer`, `CheckpointHistory`) закрыты solitary-юнитами — это чистые
трансляции, где дублей не нужно вовсе. Раннер закрыт двумя способами: скриптованным графом (когда
надо подать в канал ровно тот payload, который живая модель выдаёт редко) и **настоящим прогоном
через `astream`** (когда важно, что механизм вообще работает — writer резолвится, guard срезает
вызов, субагент дотягивается до родительского потока). Провод (`data: {json}`) и API-история
закрыты HTTP-интеграцией через ASGI-клиент с фейковым раннером.

**Покрываем автотестом** — по одной записи на суиту:

### `test_token_chunk_mapper.py` — маппер token-канала

1. **Файл**: `backend/tests/agent/test_token_chunk_mapper.py` — solitary-unit, без дублей (чистая функция над `AIMessageChunk`)
2. **Тестирует**: `app.agent.stream_events :: TokenChunkMapper.map_chunk`
3. **Суть**: Суита фиксирует, как поток токенов провайдера превращается в события ленты: вызов инструмента объявляется на первом же фрагменте (когда имя и id уже известны, а аргументы ещё нет), аргументы отдаются ровно один раз — в момент, когда накопленная строка впервые парсится как JSON, — и всё это правильно раскладывается по вызовам, когда их несколько или когда фрагмент несёт только `index`. Страхует от двух тихих поломок: «строка действия появляется поздно, уже после исполнения» и «аргументы приписаны не тому вызову».
4. **Кейсы**: text → `text_chunk`; reasoning-дельта → `reasoning_chunk`; чанк с обоими; пустой чанк молчит; ранний `tool_call_started` при незавершённом JSON; `tool_call_args` по завершении JSON; повторные фрагменты не дублируют ни то, ни другое; фрагмент без id коррелируется по `index`; два параллельных вызова ведутся независимо; **оборванная генерация (args так и не стали валидным JSON) не даёт `tool_call_args` вовсе**; args > 2 000 усечены с флагом; свежий инстанс не наследует состояние прошлого рана

### `test_stream_events.py` (дополнено) — маппер updates-канала

1. **Файл**: `backend/tests/agent/test_stream_events.py` — solitary-unit, payload узла как отдаёт LangGraph
2. **Тестирует**: `app.agent.stream_events :: StreamEventMapper.updates` / `note_call_announced`
3. **Суть**: Суита фиксирует, что результат исполнения отличим от ошибки, что артефакт объявляется по факту его наличия (а не по имени инструмента из белого списка), и что вызов, срезанный guard'ом после генерации, закрывается событием `tool_call_cancelled`. Последнее — самое хрупкое место словаря: срез узнаётся по форме сообщения, поэтому суита отдельно проверяет, что обычный текстовый ответ и независимая редакция `ToolMessage` за срез **не** принимаются.
4. **Кейсы**: (существующие) старт вызова в updates больше не эмитится, `tool_result` из `ToolMessage`, `artifact_created` с ремапом `type`, отсутствие артефакта; (новые) `status="error"` у упавшего инструмента; усечение `content` с флагом; артефакт у инструмента вне бывшего whitelist; срез guard'ом отменяет все анонсированные вызовы в порядке анонса; разрешённый вызов не отменяется; отмена ровно один раз; обычный ответ не считается срезом; редактированный `ToolMessage` не считается срезом

### `test_agent_events.py` — хелпер эмиссии доменных событий

1. **Файл**: `backend/tests/agent/test_agent_events.py` — solitary-unit, contextvar-скоуп как его выставляет обёртка субагента
2. **Тестирует**: `app.agent.agent_events :: emit_agent_event`
3. **Суть**: Суита проверяет, что инструмент может звать хелпер откуда угодно, ничего не зная про стрим: вне графа это молчаливый no-op (иначе десятки существующих тестов, зовущих `tool.ainvoke` напрямую, падали бы с `KeyError`), при явно переданном writer'е событие уходит именно в него, а привязка к родительскому вызову берётся из контекста, а не из аргументов. Заодно фиксирует, что словарь доменных kind'ов закрыт — опечатка в имени падает на месте эмиссии, а не утекает на провод отдельным типом события.
4. **Кейсы**: вне графового рантайма — no-op; явный writer получает событие; `parent_call_id` появляется под субагентом и отсутствует вне его; все четыре документированных kind'а принимаются; недокументированный отвергается; строковые значения payload усекаются; нестроковые проходят как есть

### `test_heartbeat.py` — пейсер тишины и отмены

1. **Файл**: `backend/tests/agent/test_heartbeat.py` — solitary-unit, источник событий под управлением теста (`asyncio.Event`, без сна по часам)
2. **Тестирует**: `app.agent.heartbeat :: HeartbeatPacer.pace`
3. **Суть**: Суита проверяет две гарантии, которые пейсер держит на одном таймере: клиент не сидит в необъяснённой тишине, и отмена замечается, даже когда граф завис внутри одной итерации. Отдельно фиксирует, что пейсер ничего не глотает — исключение источника пролетает наружу, а закрытие потребителем закрывает и сам ран, а не оставляет его тянуть события в фоне.
4. **Кейсы**: дефолтный интервал — документированные 5 с; тишина даёт `heartbeat`, затем событие источника; при непрерывном потоке heartbeat не появляется; отмена во время блокировки даёт терминальный `cancelled` и закрывает источник; исчерпание источника завершает поток; исключение источника не проглатывается; ранний `aclose` потребителя закрывает источник

### `test_runner_stream_contract.py` — раннер на скриптованном графе

1. **Файл**: `backend/tests/agent/test_runner_stream_contract.py` — sociable-unit, фейковый граф (реальные коллабораторы: enforcer с выключенным guard'ом, `CheckpointHistory` над `InMemorySaver`, отключённый трейсер)
2. **Тестирует**: `app.agent.runner :: LangGraphAgentRunner.stream`
3. **Суть**: Суита фиксирует собственную работу раннера — то, как он переводит три канала графа в контракт и как ведёт жизненный цикл потока. Скриптованный граф нужен там, где надо подать в канал именно тот payload, который живая модель выдаёт редко или недетерминированно: доменный конверт custom-канала, lifecycle-конверт субагента, срезанный guard'ом ход. Отдельно страхует границу «reasoning идёт живьём мимо guard'а, текст — через guard» с обеих сторон, чтобы ни одна половина не проходила по случайности, и изоляцию состояния мапперов между ранами: двух последовательных `stream()` на одном инстансе раннера достаточно, чтобы поймать возврат к общим (не per-run) мапперам — той самой поломке, которую фаза T1.4 чинила в `main.py`.
4. **Кейсы**: `stream_started` — первое событие и уходит до setup-фазы; доменный kind заворачивается в `agent_event`; он же сохраняет `parent_call_id`; lifecycle-конверт субагента пробрасывается своим типом; конверт без `type` игнорируется; анонсированный вызов, срезанный guard'ом, даёт `tool_call_cancelled` после `tool_call_started`; **второй ран того же раннера снова анонсирует повторившийся `call_id`**; **срез в позднем ране не отменяет вызов раннего**; ход только из reasoning не запускает финальное ревью; reasoning не доходит до guard'а, текст — доходит; отмена во время застрявшего графа завершает поток; отмена между итерациями завершает поток; завершившийся ран не оставляет состояния отмены (и после нормального конца, и после раннего блока на USER_INPUT)

### `test_runner.py` (дополнено) — раннер на настоящем графе

1. **Файл**: `backend/tests/agent/test_runner.py` — integration (sociable), реальный `GraphFactory` + реальный `astream`, фейковая модель в шве
2. **Тестирует**: `app.agent.runner :: LangGraphAgentRunner.stream` в связке с `graph.py`, `tools/subagents.py`, `subagents/runner.py`
3. **Суть**: Здесь проверяется, что механизмы вообще работают на живом пути, а не только в разложенном на части виде. Настоящий инструмент зовёт `emit_agent_event` — и событие доезжает до провода; настоящий guard срезает сгенерированный вызов — и раннер узнаёт срез по форме сообщения, которую guard реально производит (а не по той, которую предположил тест); настоящий субагент исполняет свой инструмент — и его шаги выходят в **родительский** поток с `parent_call_id`, пока его собственные токены остаются за бортом. Каждый из этих трёх кейсов страхует поломку, которая иначе не краснеет нигде.
4. **Кейсы**: (новые) доменное событие инструмента доезжает до потока и стоит между стартом вызова и его результатом; шаги субагента приходят в родительский поток с `parent_call_id`, внешний вызов — без него, токены субагента не в чате; срезанный настоящим guard'ом вызов даёт `tool_call_cancelled` и не даёт `tool_result`; reasoning доезжает до потока через настоящий граф и не смешивается с текстом

### `test_compaction_stream.py` — компакция глазами пользователя

1. **Файл**: `backend/tests/agent/test_compaction_stream.py` — integration, настоящий граф с настоящим `_reduce_context`, фейковые основная и суммаризующая модели
2. **Тестирует**: `app.agent.graph :: _reduce_context` через `LangGraphAgentRunner.stream`
3. **Суть**: Суита страхует попутный фикс №1 аудита ровно так, как он ломается: если изоляция суммаризатора отвалится, текст сводки просто окажется вклеен в ответ ассистента, и ни одно исключение об этом не сообщит. Компакция провоцируется честно — крошечным бюджетом контекста, на котором второй ход треда переходит порог, — а не вызовом приватного хелпера. Заодно фиксируется вторая половина контракта: сам факт компакции виден как `agent_event`, и только когда она действительно случилась.
4. **Кейсы**: компакция объявлена событием и текст сводки не попал в ответ; ход в пределах бюджета не сообщает о компакции. **Проверено мутацией**: снятие `"callbacks": []` в `graph.py` красит первый кейс (сводка приходит в `text_chunk`)

### `test_text_limits.py` — политика усечения

1. **Файл**: `backend/tests/agent/test_text_limits.py` — solitary-unit, чистая функция без зависимостей
2. **Тестирует**: `app.agent.text_limits :: truncate` / `TRUNCATION_LIMIT`
3. **Суть**: Одним хелпером усекается всё, что уходит наружу — аргументы вызова, результат инструмента, задача субагенту, — и в SSE, и в API. Наблюдаемое поведение у него ровно одно: граница. Суита фиксирует её с обеих сторон, чтобы ошибка в один символ не приводила ни к флагу на нетронутом тексте, ни к молча отрезанному последнему символу у payload'а, объявленного полным.
4. **Кейсы**: лимит равен документированным 2 000; флаг переключается ровно за границей (0 / лимит−1 / лимит / лимит+1 / многократно больше); текст ровно на лимите возвращается нетронутым; усечение сохраняет начало текста; вызывающий может сузить лимит

### `test_checkpoint_history.py` (дополнено) — сборка typed parts

1. **Файл**: `backend/tests/agent/test_checkpoint_history.py` — solitary-unit, стаб чекпоинтера (болезненная граница)
2. **Тестирует**: `app.agent.checkpoint_history :: CheckpointHistory.history`
3. **Суть**: Суита фиксирует правило «один ход агента = одно сообщение ассистента с упорядоченными parts» и все состояния, в которых ход может застать чекпоинт: вызов упал, вызов не успел разрешиться, аргументы и результат слишком велики, сообщение отредактировано, перед первым ходом лежит служебная сводка компакции. Ошибка в любом из них ломает историю молча — сообщения расползаются или теряется якорь `id`, по которому резолвятся `trace_id`, оценка и артефакты.
4. **Кейсы**: (существующие) ход с вызовом схлопывается в одно сообщение, редакция подменяет и `content`, и parts, парсинг `created_at`; (новые) несколько вызовов одного хода идут одним сообщением в порядке появления; упавший инструмент даёт `status="error"`; неразрешённый вызов даёт `pending` с пустым превью и якорем на последнем `AIMessage`; усечение `args` и `result_preview` с общим флагом; сводка компакции перед первым `HumanMessage` отбрасывается; тред без человеческого сообщения даёт пустую историю; два хода подряд не слипаются; пользовательские сообщения без parts; редактированный ход не показывает свой reasoning

### `test_stream_wire_contract.py` — SSE v2 на проводе

1. **Файл**: `backend/tests/chat/test_stream_wire_contract.py` — HTTP integration, async ASGI-клиент через `.stream()`, реальные репозитории, фейковый раннер
2. **Тестирует**: `app.api.routes.messages :: _event_generator` + `app.services.chat :: ChatService.send_message`
3. **Суть**: Суита проверяет, что делает с событием транспорт: раскладку в кадр `data: {json}` с вклеенным `type`, без потерь и переименований по дороге, вложенность через один лишь `parent_call_id`, отмену как самостоятельное терминальное событие (а не `error`, который фронт раньше отличал по тексту) и passthrough незнакомого типа, без которого forward-compat не работал бы вовсе. **Оракулом самих payload'ов эта суита не является**: раннер здесь фейковый, события собираются билдерами `tests/chat/conftest.py`, повторяющими прод вручную; что живой ран рождает именно такие поля, доказывают мапперные юниты и сквозные прогоны в `tests/agent/test_runner.py`, а набор допустимых типов привязан к проду AST-стражем словаря.
4. **Кейсы**: полный ход v2 (`stream_started` → `heartbeat` → `reasoning_chunk` → `tool_call_started`/`args`/`tool_result` → `agent_event` → `text_chunk` → `done`) с точными payload'ами; вложенные шаги несут `parent_call_id`, внешний вызов — нет; срезанный вызов закрывается и ход доходит до `done`; `cancelled` терминален без `done`; неизвестный тип события доезжает как есть

### `test_chat_service.py` (дополнено) — страж словаря провода

1. **Файл**: `backend/tests/chat/test_chat_service.py` — solitary-unit, AST-скан исходников прода
2. **Тестирует**: словарь wire-литералов в `app.agent.subagents.runner` (в дополнение к уже сторожимым `runner` / `stream_events` / `heartbeat`)
3. **Суть**: Существующий страж ловит только конструкции `StreamEvent(type=...)`, а обёртка инструментов субагента пишет в writer готовые конверты словарями — их литералы попадают на провод так же прямо, но не сторожились нигде: опечатка дала бы событие, о котором не знает ни `ChatService`, ни фронт, и ни один тест бы не покраснел. Добавленный кейс сканирует и этот эмиттер, требуя, чтобы его типы лежали внутри согласованного словаря, и отдельно — чтобы скан вообще что-то находил (иначе страж молча сторожил бы пустоту).
4. **Кейсы**: типы конвертов субагентской обёртки непусты и входят в `RUNNER_FORWARDED_TYPES`

### `test_chat_routes.py` (дополнено) — typed parts в API

1. **Файл**: `backend/tests/chat/test_chat_routes.py` — HTTP integration, реальные репозитории на транзакционной сессии, фейковый раннер
2. **Тестирует**: `app.api.routes.chats :: get_chat` + `app.api.schemas.chats :: MessageOut`
3. **Суть**: Суита фиксирует форму, в которой след работы агента пересекает HTTP-границу: дискриминированные по `type` части, все поля `tool_call`-части, и сохранённая обратная совместимость — плоский `content` на месте, а сообщение без parts сериализуется пустым списком, а не падает.
4. **Кейсы**: ход с reasoning + вызовом + текстом отдаётся тремя частями с полными полями и прежним `content`; сообщение без parts отдаёт `[]`

### `test_subagent_lifecycle_events.py` — обёртка инструментов субагента

1. **Файл**: `backend/tests/subagents/test_subagent_lifecycle_events.py` — sociable-unit, настоящий граф субагента и его `ToolNode`, фейковая модель в шве `create_llm_from_config`
2. **Тестирует**: `app.agent.subagents.runner :: SubagentRunner.run` (обёртка исполнения) + `app.agent.tools.subagents :: run_subagent`
3. **Суть**: Суита закрывает механизм, который целиком держится на явной передаче writer'а: внутри субагентского графа `get_stream_writer()` работает, но пишет в никуда, поэтому доменное событие инструмента выживает только за счёт приоритета переданного writer'а — и суита проверяет именно это, вызывая настоящий инструмент из-под настоящего `ToolNode`. Дальше — ветка отказа (ошибка инструмента доложена и не проглочена) и снятие контекста после вызова, без которого `parent_call_id` прилип бы к следующим событиям рана. Последнее наблюдается только там, где утечка и проявилась бы, — на вызове обёрнутого инструмента **в контексте вызывающего** (под `ToolNode` каждый вызов идёт в своей задаче со своей копией контекста, поэтому снаружи не видно ничего): обёрнутые инструменты перехватываются на границе, где раннер передаёт их графу, и вызываются напрямую, а пробой служит обычная доменная эмиссия между двумя вызовами — ей не к чему прицепиться.
4. **Кейсы**: вызов инструмента субагента даёт четыре события с `parent_call_id` и точными payload'ами; доменное событие изнутри доходит до родительского writer'а; упавший инструмент даёт `status="error"` с текстом ошибки, а сам субагент продолжает цикл; без writer'а обёртка не применяется и ран работает как раньше; **эмиссия между двумя вызовами обёрнутого инструмента не попадает в writer субагента** (контекст снят после успешного вызова); то же после упавшего вызова; `run_subagent` передаёт вниз собственный writer и `call_id`

### `test_tool_names_fixture.py` (дополнено) — фикстур имён как контракт для T2

1. **Файл**: `backend/tests/agent/test_tool_names_fixture.py` — solitary-unit, чтение конфига и закоммиченного JSON, без сети и БД
2. **Тестирует**: `app.agent.tools.registry :: build_tool_name_fixture` / `internal_tool_names` / `builtin_mcp_tool_names`
3. **Суть**: Существующий drift-гейт сторожит содержимое фикстура; добавленные кейсы сторожат его **форму** и полноту, потому что файл читает тест другого трека: отсортированный список записей `name` + `origin` из закрытого набора, покрывающий все имена, которые агент реально собирает при старте. Односторонняя смена формата любой из сторон должна краснеть здесь.
4. **Кейсы**: (существующий) закоммиченный фикстур совпадает со сгенерированным; (новые) форма записей, сортировка и отсутствие дублей; фикстур покрывает internal- и built-in-MCP-имена и содержит фабричные инструменты

**Осознанно не покрываем автотестом:**

- **`reasoning` от живой reasoning-модели доезжает до чекпоинта** — фейк воспроизводит библиотечную механику слияния чанков, но не поведение конкретного провайдера (проприетарные модели отдают суммированные рассуждения, некоторые не отдают их в стриме вовсе) → ручной кейс `{T1.1}` 👤.
- **Интервал heartbeat в 5 с на живом ране** — автотест гоняет пейсер на интервале 10 мс (сон по часам в CI = флак и минуты прогона); что таймер действительно тикает раз в 5 секунд в реальном ране, проверяется глазами → ручной кейс `{T1.2}` 👤.
- **Ранний `tool_call_started` на настоящем провайдере** — «до завершения узла agent» проверено на фейке, который стримит `tool_call_chunks` по контракту langchain; что конкретный провайдер шлёт имя и id в первом фрагменте — свойство провайдера → ручной кейс `{T1.3}` 👤.
- **Отзывчивость отмены во время долгого настоящего инструмента** — автотест доказывает механизм (граф блокирован, отмена приходит по таймеру), но не то, что реальный `run_subagent` обрывается за разумное время → ручной кейс `{T1.4}` 👤.
- **`security_block` из четырёх чекпоинтов и его payload** — уже покрыто существующей суитой `test_runner.py` (три точки эмиссии + пустой payload); трек его только упростил, новых веток не добавил → существующая суита.
- **`artifact_created` для `generate_image`** — покрыто своей суитой в скоупе картинок (`tests/image_generation/test_stream_events_generate_image.py`), приведённой к словарю v2 → другая суита.
- **Чанк с не-строковым `content`** (список блоков, как у некоторых провайдеров) — сейчас такой чанк не даёт `text_chunk`; проект ходит в OpenAI-совместимые модели со строковым content, и закреплять текущее поведение тестом означало бы зафиксировать в спеке необсуждённое решение → вопрос архитектору, если появится провайдер с блочным content.
- **Буквально конкурентные пользовательские стримы** (два `stream()` одновременно) — поднимать конкурентность мешает один-соединение-на-тест транзакционный харнесс (`testing.md` § HTTP-тесты), и она для этого инварианта не нужна: смешивание состояния мапперов ловится двумя **последовательными** ранами одного раннера (см. суиту раннера на скриптованном графе) → покрыто последовательным сценарием, конкурентный не заводим.

**Замеченные прод-баги (для fixer'а, сам не чиню):** прод-багов не найдено — весь контракт, который удалось выразить автотестом, ведёт себя по документации. Три наблюдения из первой редакции разрешены оркестратором так:

1. **Расхождение доки с реализацией по `truncated` у `agent_event`** `[doc]` — **разрешено в пользу таблицы событий и закрыто**: `streaming.md` § «Лимиты» больше не обещает флаг там, где поля под него нет, и явно называет события, которые флаг несут. Код не трогался.
2. **Custom-канал пробрасывает наружу любой `type`** — забрано оркестратором в бэклог: частично закрывается расширенным AST-стражем словаря (он теперь сканирует и субагентский эмиттер), whitelist на самом эмиттере — архитектурное решение вне этой итерации.
3. **`UserWarning` от pydantic в сквозном субагентном ране** — забрано оркестратором в бэклог как предсуществующее и более широкое, чем трек (воспроизводится на любом инструменте с `runtime: ToolRuntime` под настоящим `ToolNode`).

### Layer 0: Automated gate

- [x] `make check` — ruff + mypy + import-linter (9/9) + arch-checker → **0 ошибок** (предупреждения size-чека предсуществующие, не от этой фазы). *Перепрогон тестировщиком: подтверждено, 9/9 контрактов KEPT, 6 size-warning'ов.*
- [ ] `make check-fe` — не применимо к T1 (фронтовых изменений в треке нет) → гоняется на T2.
- [x] `make test-scope P="backend/tests/agent backend/tests/chat backend/tests/subagents backend/tests/image_generation"` — зелёный, кроме `test_pricing_external.py::test_active_model_prices_within_drift_tolerance` (маркер `external`, живой дрейф прайсинга OpenRouter, предсуществующее, вне трека).
- [x] `make test` целиком — **849 passed, 1 failed** (то же предсуществующее падение). До трека тестов было 755. *Перепрогон тестировщиком (46.7 s): те же 849 passed / 1 failed, дрейф `z-ai/glm-5.2` 12.6% при допуске 10%. Отдельно догнаны обрезанные фейлом хвосты `make test`: siem-service — 21 passed, siem-contracts — 64 passed.*

---

## Ручные кейсы + статусы

### Layer 1: Трек T1 — контракт SSE v2 и след работы агента

> **Стенд прогона.** Свой worktree-стенд на непересекающихся портах (Postgres `5442`, Redis `6392` —
> `5432`/`6390` заняты соседним worktree feat-002), `make migrate` на чистой БД, `make dev` на `:8000`,
> живой OpenRouter-ключ, MCP firecrawl включён. Актор — обычный пользователь `tester1`, проект
> «T1 manual», по чату на кейс. SSE читался не DevTools, а эквивалентным `httpx`-консьюмером
> (`POST .../messages`, разбор кадров `data: {json}` с относительными таймстампами) — форма кадров та же,
> плюс появляется измеримое время события. Фронт не поднимался: кейсы этого слоя его не требуют.

- [x] `{T1.1}` 👤 📊 **Reasoning живой модели доезжает до чекпоинта.** Предусловие: в настройках выбрана настоящая reasoning-модель (ключ есть, `make dev` поднят). Действие: задать вопрос, требующий рассуждения; дождаться конца хода; посмотреть SSE-кадры в DevTools, затем перезагрузить чат и заглянуть в `checkpoint_blobs` соответствующего треда. Ожидаемо: `reasoning_chunk` идут в потоке живьём; после хода в сохранённом `AIMessage` присутствует непустой `additional_kwargs["reasoning"]`; тот же текст возвращается в `GET /chats/{id}` как `parts[type=reasoning]`. Фаза T1.1 закрыла это только на фейковой модели — поведение конкретного провайдера не проверялось, а проприетарные модели часто отдают суммированные рассуждения вместо сырых или не отдают их в стриме вовсе. Если reasoning не доезжает — это не баг трека, а вход для решения архитектора о точечном дособоре в узле графа.
  **Результат: пройден на живом провайдере, все три ожидания выполнены.** Модель — `z-ai/glm-5.2` через OpenRouter (`reasoning.effort=medium`, `exclude=false`), вопрос на устный счёт с запретом инструментов. (1) В потоке — 24 `reasoning_chunk` подряд (дельты по 1–4 слова, +21.84…+22.73 с), затем 17 `text_chunk`; ветки не перемешаны. (2) В чекпоинте у финального `AIMessage` лежит `additional_kwargs["reasoning"]` — непустая строка 130 символов, посимвольно равная конкатенации пришедших дельт (читал `AsyncPostgresSaver.aget_tuple` → `channel_values["messages"]`, тот же путь, что у `CheckpointHistory.raw_messages`; сырой msgpack-блоб канала `messages` в `checkpoint_blobs` ключ `reasoning` тоже содержит). Провайдер отдаёт **сырые рассуждения, а не суммаризацию**: в тексте идёт черновой счёт по коробкам («1-я коробка: 7 яблок / 2-я коробка: 7 × 2 = 14 яблок…»), это рабочий ход мысли, а не пересказ готового ответа. (3) `GET /projects/{id}/chats/{cid}` вернул тот же текст как `parts[0] type=reasoning`, следом `parts[1] type=text`. Дособор в узле графа не требуется.
- [x] `{T1.2}` 👤 **Heartbeat в живой тишине и таймаут от него.** Предусловие: чат открыт, DevTools → Network → EventStream. Действие: отправить запрос, который заставит агента долго молчать (тяжёлый инструмент или медленный setup MCP). Ожидаемо: `stream_started` приходит сразу, до первых токенов; в любой паузе идут `heartbeat {}` с интервалом ~5 с; между обычными событиями heartbeat не появляется.
  **Результат: пройден**, наблюдался на всех живых ранах в обеих паузах контракта. Медленный setup MCP (кейс {T1.1}): `stream_started` на +0.11 с, дальше 21 с тишины — `heartbeat` на +5.11 / +10.11 / +15.11 / +20.11 с, интервалы 5.00 с с точностью до сотых. Исполнение тяжёлого инструмента (кейс {T1.3}): между `tool_call_args` (+4.84) и `tool_result` (+10.53) ровно один `heartbeat` на +9.84 — таймер отсчитывается от последнего события, а не от старта рана. В плотных сериях (24 `reasoning_chunk`, 164 `text_chunk` подряд) heartbeat не появился ни разу. `stream_started` во всех семи живых ранах приходил первым, в пределах 0.06–0.15 с, задолго до первых токенов.
- [x] `{T1.3}` 👤 **Ранний старт вызова на настоящем провайдере.** Действие: попросить агента что-нибудь найти (реальный MCP-инструмент, например `firecrawl_search`). Ожидаемо: `tool_call_started` с именем инструмента приходит **раньше**, чем `tool_call_args` (то есть до того, как модель дописала аргументы), а `tool_call_args` — до `tool_result`; аргументы в событии — валидный JSON вызова.
  **Результат: пройден.** Живой запрос на поиск, два последовательных вызова MCP. Первый: `tool_call_started {tool: "firecrawl_search"}` на +4.42 с, `tool_call_args` на +4.84 (**+0.42 с спустя** — окно, в котором модель ещё дописывала аргументы), `tool_result` на +10.53. Второй: `tool_call_started {tool: "firecrawl_scrape"}` +14.33 → `tool_call_args` +14.59 → `tool_result` +19.15. Порядок `started → args → result` соблюдён в обоих; `args` — валидный JSON (`{"query": "LangGraph 1.1 release new features changelog", "limit": 10}` и `{"url": "…", "formats": ["markdown"], "onlyMainContent": true}`), `truncated: false`. То есть конкретный провайдер действительно кладёт имя и `call_id` в первый фрагмент `tool_call_chunks`.
  **Тронуто фиксом (fixer, attempt 1, блокер ревью-A):** порядок `started → args → result` сохранён, но `tool_result` теперь уходит после проверки TOOL_RESULT внутри узла `tools` — окно между `args` и `result` вырастает на время работы классификатора (осознанная цена, решение архитектора). Живая перепроверка тайминга — за тестировщиком.
- [x] `{T1.4}` 👤 **Шаги субагента в живом потоке и отзывчивая отмена.** Действие: попросить агента делегировать задачу субагенту (`run_subagent`) так, чтобы субагент сам сходил в инструмент; в середине исполнения нажать «отмена». Ожидаемо: в потоке видны события инструментов субагента с `parent_call_id`, равным `call_id` вызова `run_subagent`; `POST /cancel` завершает поток событием `cancelled {}` в пределах ~5 с, не дожидаясь конца инструмента; `done` не приходит; в логе `agent completed` статус `cancelled`, а не `client_disconnected`.
  **Результат: пройден.** Живая делегация `run_subagent` (`web-research`), субагент сам ходил в `firecrawl_search`/`firecrawl_scrape` — 7 его вызовов пришли в родительский поток с `parent_call_id`, в точности равным `call_id` внешнего `run_subagent`; сам внешний вызов `parent_call_id` не несёт. Пары `tool_call_started`/`tool_call_args` у субагента приходят одним таймстампом (args от `ToolNode` уже распарсены) — ровно как описано в `streaming.md` § «Вложенность субагента». Токены субагента в родительский поток не попали: за весь ран ни одного `text_chunk` (14 `reasoning_chunk` — все от основного агента, до делегации). Отмена: `POST /cancel` на +45.0 с, когда субагент сидел внутри неразрешённого `firecrawl_scrape`, — `cancelled {}` на +49.28 с (**задержка 4.28 с**, ближайший тик 5-секундного `HeartbeatPacer`, т.е. путь «граф застрял внутри одной итерации»); `done` не пришёл; в логе `agent completed … status=cancelled`. Контрольный второй прогон с отменой на +22.0 с (граф стоял на генерации): `cancelled` через 0.74 с, тоже `status=cancelled`.
  **Тронуто фиксом (fixer, attempt 1, блокер ревью-A):** `tool_result` субагента теперь эмитит узел `tools` его графа после проверки TOOL_RESULT, а не прокси `_LifecycleEmittingTool` до неё; набор, порядок и `parent_call_id` событий не изменились (пинается автотестом), изменились момент (плюс время классификатора) и текст на ветке ошибки (санированный вместо `str(exc)`). Живая перепроверка кейса — за тестировщиком.
- [x] `{T1.5}` 📊 **Компакция на длинном живом треде.** Предусловие: тред, накопивший контекст выше порога компакции. Действие: отправить ещё одно сообщение, следить за потоком. Ожидаемо: приходит `agent_event {kind: "compaction"}`; текст сводки **не** появляется ни в ответе ассистента, ни в истории `GET /projects/{id}/chats/{cid}`; в Langfuse есть генерация `context-summarization` с непустыми `input`/`output`, проставленным `model` и `usage_details`, и её стоимость попадает в учёт. *(Ожидание переписано после фикса `b502392` — с апрува оркестратора. Было: «отдельная генерация с тегом `context_summarization`, **не вложенная** в пользовательский ход». По решению архитектора компенсирующая телеметрия вешается на ambient-контекст, как у guard-классификатора, то есть генерация именно **вкладывается** в спан рана; прежняя формулировка описывала невыбранный вариант. Требование про историю добавлено — оно и есть вторая половина фикса.)*
  **runs:** r1 ❌ (телеметрии нет вовсе + вскрыт прод-баг: сводка как `text`-часть в истории) → **r2 ✅** (после фикса `b502392`, новый тред, все четыре проверки зелёные).
  **Результат: два ожидания из трёх выполнены, третье — нет; попутно вскрыт прод-баг в истории.** Порог достигнут временно уменьшенным бюджетом контекста (`configs/agent.yaml` `context.max_tokens: 100000 → 8000`, порог 6000 токенов; сама компакция при этом настоящая, на живых моделях — тот же приём, что у автотеста; значение возвращено, рабочее дерево чистое). Тред набит живым поиском + скрейпом, третий ход перешёл порог. ✅ `agent_event {"kind": "compaction", "payload": {}}` пришёл на +14.22 с, до первых `text_chunk` хода. ✅ Текст сводки в ответ ассистента не просочился: 290 `text_chunk` не содержат ни `[Previous conversation summary]`, ни одной фразы сгенерированной сводки (сверял подстроками по четырём маркерам) — изоляция `callbacks: []` работает. ❌ **В Langfuse генерации `context_summarization` нет вообще** — ни вложенной в ход, ни отдельной: в трейсе хода (`39f30a09…`) 11 обсерваций (`ReasoningChatOpenAI`, три `llm-classifier`, guardrail'ы, `LangGraph`/`agent`), суммаризации среди них нет; поиск по всему проекту (`GET /api/public/observations?name=context-summarization`) даёт 0 записей. Причина видна в коде: трейсинг подключён через `langfuse.langchain.CallbackHandler`, поэтому `"callbacks": []` в `_reduce_context` (`graph.py:78-85`) отрезает суммаризацию не только от stream-канала, но и от Langfuse целиком. Security-классификатор решает ту же задачу иначе — тоже гасит callbacks, но строит телеметрию сам (`observation.record_classifier_generation`, `classifier.py:82-87`), и его генерации в Langfuse видны. У компакции такой компенсации нет → её вызов LLM не учитывается ни в трейсах, ни в стоимости. Ожидание кейса и реализация разошлись; **что чинить — код (добавить собственную обсервацию) или ожидание кейса — решает архитектор**, тестировщик не выбирает.
  **Закрыто по коду (fixer, attempt 1):** компакция снова видна и учитывается — генерация `context-summarization` с `usage_details` создаётся вручную (`observe_compaction` в `agent/tracing.py`) по образцу guard-классификатора, отвязка `callbacks: []` сохранена; обоснование в `## Решения и обоснования` summary трека, живая перепроверка кейса — за тестировщиком.
  **Смежная находка (прод-баг, не в ожиданиях кейса):** после компакции служебная сводка попадает в пользовательскую историю. `_reduce_context` создаёт `summary_msg = AIMessage(content="[Previous conversation summary]\n…")` без `id` и возвращает его в `ops_prefix`, но редьюсер `add_messages` **дописывает** его в конец списка состояния и присваивает `id`. В чекпоинте сводка легла на позицию 10 — сразу после последнего `HumanMessage`, а не перед первым. Из-за этого защита `CheckpointHistory` «дропаем всё до первого `HumanMessage`» (описанная в `streaming.md` § «История: typed parts» именно как страховка от этой сводки) не срабатывает вовсе, и `GET /projects/{id}/chats/{cid}` отдаёт ход ассистента **двумя** `text`-частями: первая — служебная сводка целиком (`[Previous conversation summary] The user asked to find information about Server-Sent Events via firecrawl_search…`), вторая — настоящий ответ. Наблюдалось vs ожидалось: ожидалось, что сводка не пересекает HTTP-границу; наблюдается лишний `text`-part со служебным текстом в каждом ходе, следующем за компакцией. Владелец — fixer.
  **Закрыто (fixer, attempt 1):** сводка помечена флагом `additional_kwargs["context_summary"]` в `_reduce_context`, `CheckpointHistory.history()` отбрасывает помеченные сообщения независимо от их позиции — обоснование выбора признака в `## Решения и обоснования` summary трека.
  **Результат r2 (перепрогон на `b502392`, стенд перезапущен на новом коде, тред новый — сжатые до фикса треды остаются с грязной историей, back-fill не делаем): пройден, все четыре проверки зелёные.** Порог снова достигнут временно уменьшенным бюджетом (`context.max_tokens: 100000 → 8000`; значение возвращено, рабочее дерево чистое). Тред набит живым поиском + скрейпом по HTTP/3/QUIC, компакция сработала **дважды** — на 4-м ходе (`agent_event` на +8.80 с) и на 5-м (+84.92 с); обе проверены.
  ① **Событие на месте.** `agent_event {"kind": "compaction", "payload": {}}` пришёл в обоих случаях до первых `text_chunk` хода. Попутно: на втором срабатывании суммаризатор думал 85 с (5 701 входных токенов), и всю паузу поток держали 16 `heartbeat` подряд с шагом 5.00 с — худший случай `{T1.2}` вживую.
  ② **Изоляция не отъехала.** Сводка второго срабатывания — 1 391 символ; ни один из 23 её 60-символьных срезов не встречается ни в `text_chunk` (1 103 симв.), ни в `reasoning_chunk` (841 симв.) хода, маркеры (`Previous conversation summary`, `Quick UDP Internet Connections`, `Устранение Head-of-Line Blocking`, `RFC 9114 в 2022 году`) — 0 попаданий. `"callbacks": []` фикс не тронул, и это видно.
  ③ **Истории сводка больше не видна — прод-баг закрыт.** В чекпоинте обе сводки лежат там же, где и раньше (позиции 7 и 10, то есть **после** `HumanMessage`, а не перед первым — подтверждает, что позиционная защита и не могла сработать), но несут `additional_kwargs = {"context_summary": True}` и единственным ключом. `GET /projects/{id}/chats/{cid}` отдаёт 6 сообщений; ход, на котором сработала компакция, — ровно **одна** `text`-часть с настоящим ответом (`parts = [reasoning, text]`), соседние ходы такие же; подстрока `Previous conversation summary` не встречается ни в одном `content` и ни в одной `part` по всему треду.
  ④ **Телеметрия есть и попадает в стоимость.** В трейсе хода (`3b771d9d…`) генерация `context-summarization`: `type=GENERATION`, `model=deepseek/deepseek-v4-flash`, `modelParameters={"max_tokens": 500}`, `input` — список из 4 сообщений (системный промпт суммаризации + 3 сжимаемых), `output` — 1 391 символ, побайтово тот самый текст сводки, `usageDetails={"input": 5701, "output": 500, "output_reasoning": 71, "total": 6201}`, `metadata={"messages_compacted": 3, "messages_kept": 10, "context_tokens_before": 10437}`, `level=DEFAULT`. **Стоимость посчитана, а не только записана:** `costDetails.total = 0.00095802` (`input` 0.00079814 + `output` 0.00014 + `output_reasoning` 1.988e-05), генерация входит в `totalCost` трейса (0.01406) и видна в агрегате `/metrics/daily` за день отдельной строкой модели `deepseek/deepseek-v4-flash` (22 обсервации, 0.01712 за сутки). **Фактическая вложенность:** `parentObservationId` указывает на корневой спан `agent-run` — то есть генерация лежит соседом `LangGraph`/`guard-*`, **внутри** спана рана, но **вне** узла `agent` пользовательского хода. Это и есть поведение ambient-контекста, прецедент guard-классификатора; под него и переписано ожидание кейса выше.
  **Наблюдение (не в ожиданиях кейса, ничего не чиню, решает архитектор):** первое из двух срабатываний дало **пустую сводку** — `summary_msg.content` равен ровно префиксу `[Previous conversation summary]\n` (32 символа), тело пустое. Langfuse по этому вызову показывает `output: null` при оплаченных 103 выходных токенах: `deepseek/deepseek-v4-flash` с включённым reasoning вернул пустой `content`. Ограничением `max_summary_tokens` это не объясняется — весь бюджет в 500 токенов израсходован не был. Практический эффект: 2 сообщения выброшены из контекста и заменены пустышкой, то есть кусок истории потерян молча — `_reduce_context` пустой `summary_text` ничем не отсекает (ветка `except` тут не срабатывает, отказа не было). Поведение предсуществующее, к фиксу `b502392` отношения не имеет и в ожидания `{T1.5}` не входит; на живом бюджете 100 000 токенов встречается реже, но механика та же.
- [x] `{T1.6}` 🔴 📊 **Срез вызова guard'ом на настоящей инъекции.** Предусловие: guard включён. Действие: спровоцировать инъекцию в аргументах вызова инструмента (сценарий из security-набора). Ожидаемо: анонсированный `tool_call_started` закрывается событием `tool_call_cancelled` с тем же `call_id`; `tool_result` по нему не приходит; ход завершается терминальным `security_block {}` без деталей (`reason`/`checkpoint`/`detection_layer` на проводе отсутствуют, но видны в Langfuse/SIEM); повторная отправка в этот тред отбивается `403`.
  **Результат: пройден.** Сценарий: просьба сохранить в базу знаний «черновик клиентского кода» с парами «имя внутреннего инструмента + его параметр» (`create_section(section_id, content)`, `save_user_memory(key, value)`, `load_skill(skill_name)`, `generate_image(prompt)`) — легитимная по форме, но при исполнении переносящая PROTECTED-материал в аргументы вызова. Поток: `stream_started` +0.10 → 12 `reasoning_chunk` → `tool_call_started {call_id: call_6c524fb2…, tool: "create_section"}` +3.70 → `tool_call_args` +5.48 → **`tool_call_cancelled {call_id: call_6c524fb2…}`** +5.52 (тот же `call_id`) → **`security_block {}`** +5.54, терминально. `tool_result` не пришёл ни одного, `done` не пришёл, payload `security_block` — пустой объект без `reason`/`checkpoint`/`detection_layer`. Детали остались на сервере: `security hit (deterministic) … checkpoint=tool_call_arg detection_layer=paired compromised_tools=['create_section','save_user_memory','load_skill']` + `tool_call_arg injection blocked … verdict=INJECTION`. Повторная отправка в тред → **403** `Thread blocked by security policy`. Дополнительно на первом заходе (сценарий с прямой просьбой выписать машинные имена внутренних функций) сработал более ранний чекпоинт `USER_INPUT`: `security_block {}` через 1.12 с до старта графа, тред помечен `security_blocked=true`, в истории — пользовательское сообщение + `redacted` ассистентское с заглушкой и единственным `text`-part; повторная отправка тоже 403. Обе точки эмиссии ведут себя по контракту.
- [x] `{T1.7}` **Фикстур имён соответствует живому старту.** Действие: поднять `make dev` с полным набором MCP-серверов, сверить лог собранных инструментов с `backend/contracts/agent-tool-names.json`. Ожидаемо: все internal- и built-in-MCP-имена из лога присутствуют в фикстуре; пользовательские MCP-инструменты в неё не попали.
  **Результат: пройден с оговоркой по способу наблюдения.** Лог старта имён инструментов не печатает — только счётчики (`security guard initialized tool_registry_size=12` → `mcp tools loaded servers_active=1 servers_total=2 tool_count=3` → `run_subagent tool registered tool_pool_size=15`), поэтому сверка сделана read-only пробой тем же кодом, который исполняет старт: `internal_tool_names()` + живой `create_mcp_client(...).get_tools()` против закоммиченного JSON. Живой состав: 13 internal + 3 firecrawl = 16 имён, счётчики лога сходятся (12 internal без `run_subagent` до MCP, пул 15 = 12 + 3). **Живых имён, отсутствующих в фикстуре, — ноль.** Обратная разница — только `tavily_search`/`tavily_extract`: сервер объявлен в `configs/agent.yaml` с `enabled: false`, в фикстур попадает намеренно (подпись нужна независимо от окружения), в рантайме не поднимается. Отсечение пользовательской поверхности видно живьём: firecrawl-сервер отдаёт **26** инструментов, после фильтра `allowed_tools` в агента уходят 3 — остальные 23 (`firecrawl_crawl`, `firecrawl_monitor_*`, `firecrawl_research_*`, …) в фикстур не попали. Пользовательские MCP-инструменты попасть в него не могут структурно: `build_tool_name_fixture` читает только `agent_config.mcp_servers` из файла конфига, а пользовательские серверы резолвятся в рантайме из БД. Регенерация фикстура на живом конфиге даёт побайтово тот же файл.

### Layer 2: Integration (cross-cutting, в INTEGRATION_TEST)

> Прогоном трека T1 **не закрывались** — идут на финальном прогоне после барьера, вместе с готовым T2.
> Попутные наблюдения живых ранов, полезные тому прогону: (а) усечение подтвердилось живьём с обеих
> сторон — `run_subagent` вернул вердикт длиннее лимита, и в истории его `tool_call`-part пришёл с
> `truncated: true` и `result_preview` ровно в 2 000 символов; (б) `tool_result` со `status="error"`
> тоже встретился по-настоящему — упавший вызов `firecrawl_search` дал `status: "error"` с текстом
> отказа вместо результата, ход при этом продолжился и агент перевызвал инструмент; (в) первый пункт
> этого слоя («История после перезагрузки повторяет ленту») на треде с компакцией проверен точечно и
> после фикса `b502392` выполняется — см. `{T1.5}`, прогон r2, проверка ③. Оговорка для финального
> прогона: треды, сжатые **до** фикса, back-fill'ом не чинились и остаются с лишней `text`-частью;
> брать их как базу проверки нельзя, годятся только как контрольный пример «до».

> **Стенд финального прогона.** Свой worktree на непересекающихся портах (Postgres `5442`, Redis `6392`,
> backend `:8000`, Vite `:5173`), код — `0b6c2c9` (дерево побайтово равно текущему `ba65e3b`: между ними
> только снимки браузерных прогонов и `.gitignore`). Актор `tester1`, проект «T1 manual», **новый** чат
> «Протоколы QUIC и HTTP/3» (`c83596df-4a04-493e-8a63-aeec361f51e0`). Кадры SSE читались тем же, что видит
> фронт: тело потока разветвлялось `ReadableStream.tee()` во вкладке (прод-код не трогался, патч жил в
> памяти вкладки и снят перезагрузкой), DOM снимался рекордером с шагом 150 мс. Один живой ход:
> рассуждение → `firecrawl_search` ‖ `run_subagent` (web-research, 4 своих вызова) → рассуждение →
> `firecrawl_scrape` ‖ `generate_image` → артефакт → рассуждение → ответ → ревью → `done`; 180,5 с,
> 1 134 кадра (`reasoning_chunk` 475, `text_chunk` 604, `heartbeat` 25, `tool_call_*` 8/8/8,
> `artifact_created` 1, пара `final_output_review_*`, `title_updated`, `done`).

- [x] История после перезагрузки повторяет ленту: ход с несколькими вызовами инструментов после `F5` показывает те же действия в том же порядке одним сообщением ассистента; исключения — вложенная хронология субагента и факт компакции (осознанно live-only), субагентский ход виден одной строкой с задачей и вердиктом.
  **Результат: пройден.** Живая лента к концу хода — 7 строк (`Рассуждения`, `Ищу в интернете · «QUIC protocol HTTP/3 overview 2024»`, `Исследующий субагент · web-research`, `Рассуждения`, `Читаю страницу · https://www.f5.com/glossary/quic-http3`, `Генерирую изображение · «QUIC/HTTP/3 protocol stack diagram»`, `Рассуждает`). После `F5` — **те же 7 строк, в том же порядке, с теми же статусами**, одним сообщением ассистента; `innerText` области сообщений совпал посимвольно (3 444 знака до и после перезагрузки). `GET /api/projects/{id}/chats/{cid}` отдаёт 2 сообщения; у ассистентского — 8 `parts` ровно в порядке экрана: `reasoning(1211)` → `tool_call firecrawl_search` → `tool_call run_subagent` → `reasoning(623)` → `tool_call firecrawl_scrape` → `tool_call generate_image` → `reasoning(903)` → `text(2652)`, `content` равен `text`-части. Оговорённые исключения наблюдаются как решено: вложенные шаги субагента в историю не попали (в live их было 4, в развороте истории — только зона «ВЫЗОВ» с `agent_type` и заданием прозой), длительностей в истории нет. Компакция в этом ходе не срабатывала — её live-only-природа этим прогоном не проверялась. Хвостовая строка меняет подпись `Рассуждает` → `Рассуждения` — это решение T2 (живость выводится из позиции в ленте), не расхождение.
  **Оговорка по вердикту субагента:** «одной строкой с задачей и вердиктом» выполнено наполовину — задача есть, **вердикта нет**, потому что его нет и на проводе: `run_subagent` вернул `tool_result {status: "success", content: "", truncated: false}`, в истории `result_preview: ""`, в логе `subagent run finished output_length=0` (модель `deepseek/deepseek-v4-flash`). Зона «ОТВЕТ СУБАГЕНТА» при пустом вердикте не рисуется вовсе. Фронт отработал по контракту — источник пустоты на бэкенде и он же класс уже описанной в `{T1.5}` находки «пустая сводка от той же модели»; отдельным багом трека не считаю, выношу наблюдением.
- [x] Оценка и артефакты продолжают резолвиться на ходе с вызовами инструментов: лайк/дизлайк ставится, артефакт хода привязан к сообщению, `trace_id` открывается в Langfuse.
  **Результат: пройден.** Ход с четырьмя вызовами инструментов (включая субагента). **Лайк**: клик по «Полезно» → `feedback_score: true` в `GET …/chats/{cid}`, кнопка подсвечена `text-primary`, «Не то» осталась приглушённой. **Артефакт**: `generate_image` дал `artifact_created {id: 3f7fcb0d…, artifact_type: "image"}` на +164,48 с, и тот же артефакт привязан к сообщению — `messages[1].artifacts = [{id: 3f7fcb0d…, title: "QUIC/HTTP/3 protocol stack diagram", type: "image"}]`; на экране под ответом стоит его карточка-ссылка. **`trace_id`**: `done {message_id: lc_run--019fb76b…, trace_id: 66958752d1d26f909ffb7928ca82e1db}` на проводе, тот же `trace_id` в `MessageOut`; трейс реально открывается — `GET /api/public/traces/66958752…` в Langfuse отдаёт `name=agent-run`, 37 обсерваций, `sessionId` = id чата, `totalCost=0.0429`. Якорь резолва не сбился ни на одном из трёх артефактов состояния.
- [x] Усечение больших данных: инструмент, вернувший больше 2 000 символов, даёт обрезанный текст и флаг усечения и в SSE (`tool_result.truncated: true`), и в `parts` истории (`result_truncated: true`), причём аргументы того же вызова остаются с `args_truncated: false` — флаги раздельные.
  **Результат: пройден, флаги раздельны с обеих сторон.** Два вызова превысили лимит по результату и ни один — по аргументам. `firecrawl_search`: SSE `tool_call_args {truncated: false}` (89 знаков) + `tool_result {truncated: true}` c `content` ровно **2 000** знаков; история — `args_truncated: false`, `result_truncated: true`, `result_preview` 2 000 знаков, `args` 89. `firecrawl_scrape`: то же (args 99 / результат 2 000, `false`/`true`). Контрольные вызовы в том же ходе не помечены ни одним флагом: `generate_image` (результат 120 знаков) и вложенный `firecrawl_search` субагента (559 знаков) — оба `false`/`false`. Экранная половина — в кейсе T2 того же слоя.

### Layer 3: E2E (cross-cutting, в INTEGRATION_TEST)

> Прогоном трека T1 **не закрывались и не могли быть**: оба кейса требуют глаз на ленте активности,
> а фронтенд (трек T2) ещё не реализован. Заблокированы до T2, не провалены.

- [ ] ⚠️ 👤 Полный сценарий в браузере: вопрос → рассуждение → поиск → субагент → артефакт → ответ; лента живёт на каждом шаге (нет молчаливых пауз), строки разворачиваются, вложенные шаги субагента отрисованы под своей строкой, после перезагрузки картина сохраняется.
  **Результат: четыре ожидания из пяти выполнены, «нет молчаливых пауз» — не выполнено.** Стенд и состав хода — см. рамку слоя Layer 2 выше. ✅ **Цепочка целиком прошла в одном ходе**: рассуждение → свой `firecrawl_search` ‖ `run_subagent` → рассуждение → `firecrawl_scrape` ‖ `generate_image` → `artifact_created` → рассуждение → ответ. ✅ **Строки разворачиваются**: клик по завершённой строке открывает зоны «ВЫЗОВ» (сырое `firecrawl_search`, аргументы построчно `query` / `limit` / `sources`) и «РЕЗУЛЬТАТ». ✅ **Вложенные шаги субагента отрисованы под своей строкой в live и без клика**: идущая строка `Исследующий субагент · web-research` приехала раскрытой, на +23,76 с под ней встали 3 строки `Ищу в интернете · …`, к +122,6 с их стало 4; все — с `parent_call_id`, равным `call_id` внешнего `run_subagent` (сам внешний вызов `parent_call_id` не несёт). По приходу результата строка свернулась сама. ✅ **После перезагрузки картина сохраняется** — подробности в кейсе Layer 2 «История после перезагрузки повторяет ленту». ✅ Попутно наблюдался ранний старт вызова: строка `Ищу в интернете` встала голой на +12,80 с и получила дополнение `· «QUIC protocol HTTP/3 overview 2024»` на +13,10 с (окно 0,30 с); строка субагента точно так же: `Субагент` → `Исследующий субагент · web-research`.
  ❌ **Молчаливая пауза посреди хода — 10,65 с полностью статичного экрана.** С +135,95 с (пришли результаты `firecrawl_search` и `run_subagent`) до +146,60 с (первый `reasoning_chunk` следующего шага) `innerText` области сообщений не изменился ни на символ: все строки ленты уже завершены (статус «успешно»), идущей строки нет — значит нет ни бегущих точек, ни счётчика времени, — а строка-пауза в этот момент не показывается. На проводе в это окно шли `heartbeat` с шагом 5,00 с, то есть сервер вёл себя по контракту и пауза видна только на экране. Причина наблюдаема в коде (`frontend/src/pages/chat/ui/MessageList.tsx:119-126`): `ActivityPauseRow` рендерится под условием `liveBlocks.length === 0`, то есть только до **первого** события ленты; окно тишины между завершённым инструментом и следующим токеном модели ничем не занято. Это ровно тот класс, против которого заведён второй столп брифа («молчаливого UX не существует»), и трековый кейс `{T2.2}` его не поймал: там наблюдалась только пауза от отправки до первого события. Владелец — решение архитектора (индикатор на хвосте ленты) → fixer.
  **Наблюдение (не в ожиданиях кейса):** в момент `done` лента на один сэмпл (≤150 мс) стала пустой — на +180,51 с в DOM 0 строк, на +180,65 с снова 7. Это стык «стор погас → сообщение из истории отрисовалось»; мелькание короткое, но на глаз ловится как моргание ленты.
- [ ] 👤 Отмена и повторная отправка из UI: отмена не показывает error-toast, чат остаётся рабочим, следующее сообщение уходит нормально.

---

## Находки ревью [severity+owner]

> Пишет **test-reviewer** (adversarial-ревью тестов против контракта, read-only). Каждая находка —
> severity (**blocker** / **major** / **minor**) + владелец фикса: `[test]` (test-author) /
> `[prod]` (fixer) / `[infra]` (`packages/testing`) / `[doc]`. На фазе GREEN fixer чинит `[prod]`,
> test-author — `[test]`; закрытую/эскалированную находку помечают здесь же. Чисто — секция пустая.

**Статус GREEN (test-author):** все шесть находок и `[doc]`-пункт закрыты — статус отмечен
строкой под каждой находкой, формулировки ревьюера не тронуты. Мутационные проверки по F1 и F2
повторены после правок (см. отчёт фазы).

Blocker'ов нет. Две major-находки, четыре minor. Каждая проверена мутацией прод-кода с
последующим откатом (рабочее дерево чистое), тавтологичность не постулируется по чтению.

### F1. Тест «контекст снят после субагентского вызова» зелёный при удалении самого сброса — major `[test]`

- [x] **Закрыто (GREEN, `[test]`):** параметризованный кейс заменён двумя, которые вызывают обёрнутый инструмент в контексте теста (обёртка перехватывается на границе `build_subagent_graph`, а не через приватный класс) и пробуют доменную эмиссию между двумя вызовами. Мутация «снять оба `.reset()`» теперь красит оба кейса.

`backend/tests/subagents/test_subagent_lifecycle_events.py:255-281`
(`test_the_parent_attribution_does_not_outlive_the_subagent_call`, оба параметра — success/failure).

Мутация: обе строки `SUBAGENT_STREAM_WRITER.reset(writer_token)` /
`SUBAGENT_PARENT_CALL_ID.reset(parent_token)` в `finally`
(`backend/app/agent/subagents/runner.py:193-195`) заменены на `pass` — 7/7 тестов файла остаются
зелёными. Причина: `ToolNode` исполняет обёрнутый тул в собственном контексте, поэтому `.set()`
внутри него в принципе не виден в контексте теста; `emit_agent_event("compaction", {})` после
`runner.run(...)` резолвится в no-op независимо от того, был сброс или нет. Тест краснеет только
на поломке самого хода, но не на поломке проверяемого поведения — это ровно тот случай, где ответ
на «что должно сломаться, чтобы тест покраснел» — «ничего реалистичного».

Предложение: наблюдать contextvar там, где она выставляется, — прямой
`await _LifecycleEmittingTool(wrapped_tool=note, stream_writer=w, parent_call_id="outer-1", ...)
.ainvoke({"id": "i1", "args": {...}, ...})` в контексте самого теста, затем
`assert SUBAGENT_STREAM_WRITER.get() is None and SUBAGENT_PARENT_CALL_ID.get() is None`; ветка
ошибки — тот же вызов под `pytest.raises`. Пока это единственный оракул на пункт «после вызова
(успешного и упавшего) контекст снят» из § «Дизайн автотестов», и он пуст.

### F2. Per-run изоляция мапперов не покрыта на уровне раннера — major `[test]`

- [x] **Закрыто (GREEN, `[test]`):** в `test_runner_stream_contract.py` добавлены два кейса на двух последовательных `stream()` одного раннера (повторный `call_id` анонсируется снова; guard-срез позднего рана не отменяет вызов раннего); `_ScriptedGraph` научен отдавать второй скрипт. Мутация «общие мапперы на инстансе раннера» красит оба кейса. Пункт § «Осознанно не покрываем» переписан.

Мутация: `_run_turn` (`backend/app/agent/runner.py:146-147`) переведён на общие
`TokenChunkMapper`/`StreamEventMapper`, живущие на инстансе раннера, — весь скоуп трека
(305 тестов) остаётся зелёным. Это та самая поломка, которую T1.4 чинила в `main.py`
(`event_mapper=StreamEventMapper()` → `event_mapper_factory=StreamEventMapper`), и инвариант прямо
записан в `streaming.md` § LangGraphAgentRunner («фабрика — новый инстанс на каждый `stream()`»);
регрессия к shared-инстансу сегодня не краснеет нигде.

§ «Осознанно не покрываем» объясняет пропуск невозможностью поднять два конкурентных стрима в
транзакционном харнессе, но проверка конкурентности не требует: два **последовательных** `stream()`
на одном раннере. Первый ран анонсирует `c1` и не разрешает его (`tool_call_started` без
`tool_result`), второй отдаёт guard-срез — с общим `StreamEventMapper` второй ран выдаст лишний
`tool_call_cancelled {call_id: "c1"}` из чужого рана. Симметрично для `TokenChunkMapper`: повторный
`call_id` во втором ране не будет анонсирован вовсе. Место — `test_runner_stream_contract.py`,
инфраструктура уже есть (`_FakeFactory` может отдавать разные скриптованные графы по `build_calls`).

### F3. Словарь провода не сторожится на эмиттере субагента — minor `[test]`

- [x] **Закрыто (GREEN, `[test]`):** страж расширен вторым сканом — литералы `"type"` в dict-конвертах `app.agent.subagents.runner` сверяются с `RUNNER_FORWARDED_TYPES`, плюс ассерт «скан не пуст». Мутация «опечатка в литерале `tool_call_args`» красит стража.

`test_runner_emits_only_the_agreed_wire_vocabulary` (`backend/tests/chat/test_chat_service.py`)
сканирует AST только модулей `runner` / `stream_events` / `heartbeat`. При этом
`backend/app/agent/subagents/runner.py:151-209` минтит три wire-типа
(`tool_call_started`/`tool_call_args`/`tool_result`) строковыми литералами в dict-конвертах и в
скан не попадает, а раннер (`runner.py:315-326`) пробрасывает любой `type` как есть. Итог:
опечатка в этих литералах уходит на провод отдельным типом события и не краснеет ни в одном тесте —
в отличие от доменных `kind`, где опечатка падает в `emit_agent_event`. Это машинная половина
наблюдения №2 автора (см. вердикт ниже), и её можно закрыть без архитектурного решения: расширить
страж на литералы `"type"` в конвертах `subagents/runner.py`, сверяя их с `RUNNER_FORWARDED_TYPES`.

### F4. Нет граничного теста `text_limits.truncate` — minor `[test]`

- [x] **Закрыто (GREEN, `[test]`):** новая суита `backend/tests/agent/test_text_limits.py`: граница `0 / limit-1 / limit / limit+1 / 3×limit`, текст ровно на лимите возвращается нетронутым, усечение сохраняет начало, суженный лимит.

Все call-site'ы (`test_token_chunk_mapper.py:210`, `test_stream_events.py:160`,
`test_agent_events.py:122`, `test_checkpoint_history.py:230`) проверяют усечение на 2×/3×
превышении лимита, поэтому off-by-one в самой чистой функции (`>` vs `>=`, `text[:limit]` vs
`text[:limit - 1]`) не покраснеет нигде. `testing.md` § Модель прямо относит такое к
solitary-unit. Предложение: параметризованный тест на `backend/app/agent/text_limits.py:22` —
`len == limit` → `(text, False)`, `len == limit + 1` → `(len == limit, True)`, пустая строка.

### F5. Вызов, args которого никогда не становятся валидным JSON, не даёт `tool_call_args` — minor `[test]`/`[doc]`, требует решения

- [x] **Закрыто (GREEN, `[test]`):** выбранное поведение зафиксировано кейсом в `test_token_chunk_mapper.py`: оборванная генерация (args так и не стали валидным JSON) даёт только `tool_call_started`. `streaming.md` не трогал — формулировка «в момент, когда строка успешно парсится как JSON» этому не противоречит.

`TokenChunkMapper._is_complete_json("")` возвращает `False`, поэтому вызов, у которого провайдер
шлёт пустой `args` во всех фрагментах, даёт `tool_call_started` без парного `tool_call_args` —
в ленте зона «ВЫЗОВ» остаётся пустой навсегда. Ни `streaming.md` («в момент, когда накопленная
строка успешно парсится как JSON»), ни тесты этого случая не оговаривают. Уверенности, что это
баг, нет (все наши инструменты имеют аргументы, OpenAI-совместимые провайдеры шлют `{}`), поэтому
minor: либо кейс в `test_token_chunk_mapper.py`, фиксирующий выбранное поведение, либо строка в
`streaming.md`.

### F6. Wire-сюита пинит форму событий против тестовых билдеров, а не против раннера — minor `[test]`, информационная

- [x] **Закрыто (GREEN, `[test]`):** докстринг переписан: сюита отвечает за раскладку события в SSE-кадр и семантику транспорта, а оракул payload'ов — мапперные юниты и сквозные прогоны `tests/agent/test_runner.py`, набор типов — AST-страж. Подъём до раннера не делал.

`backend/tests/chat/test_stream_wire_contract.py:1-13` обещает: «переименование поля здесь должно
краснеть на бэкенде». Фактически сюита гоняет фейковый раннер, то есть сверяет билдеры
`tests/chat/conftest.py` с JSON-кадром; настоящую форму событий держат мапперные юниты (они
ассертят те же литеральные ключи). Билдеры и прод сверены поле-в-поле — расхождений нет, оракул в
сумме есть, но не в этом файле. Правка — формулировка докстринга, чтобы следующий читатель не
принял сюиту за единственный контракт T2.

### Вердикт по трём наблюдениям автора

- [x] **Закрыто (GREEN, `[doc]`):** §1 — `doc/tech/streaming.md` § «Лимиты» переформулирован: флаг `truncated` назван только у событий, которые его несут, усечение `payload`-строк `agent_event` описано как молчаливое; прод не тронут. §2 и §3 забраны оркестратором в бэклог (машинная половина §2 закрыта через F3).

1. **`truncated` у `agent_event`** — `[doc]`, minor. Права таблица событий, ошибается § «Лимиты».
   design-brief § «Лимиты и параметры» обещает флаг только для `args` / `content` / task;
   payload-строки `agent_event` туда не входят вовсе — их усечение вообще добавлено реализацией
   сверх брифа. Таблица `streaming.md` с этим согласована, а § «Лимиты» задним числом растянула
   фразу с флагом на `agent_event`. Тест закрепил таблицу — верно. Фикс — одна фраза в
   `streaming.md` § «Лимиты»; прод не трогать.
2. **Проброс любого `type` через custom-канал** — `[prod]`/`[doc]`, minor, решение за архитектором.
   Констатация автора верна, и я её усиливаю: дыра не только в отсутствии валидации на эмиссии, но
   и в том, что единственный автоматический страж словаря физически не видит субагентский эмиттер
   (F3). Расширение стража закрывает риск опечатки без архитектурного решения; ограничение
   passthrough белым списком lifecycle-типов — уже решение архитектора (forward-compat это не
   ломает: whitelist ставится на **эмиттере**, требование «неизвестный тип не роняет потребителя»
   живёт на другой стороне провода).
3. **pydantic `UserWarning` (`field_name='context'`)** — `[prod]`, minor, **предсуществующее и шире
   трека**. Проверено пробой: варнинг воспроизводится на **любом** инструменте, объявляющем
   `runtime: ToolRuntime`, под настоящим `ToolNode` с `AgentContext` — не субагентская специфика.
   Так аннотированы все инструменты (`knowledge_sphere.py`, `skill_context.py`, `user_memory.py`,
   `artifacts.py`, `image_generation.py`, `subagents.py`), то есть в проде варнинг летит на каждый
   tool-вызов; в существующих тестах он не всплывал только потому, что они зовут `tool.ainvoke`
   напрямую, минуя сериализацию входа `ToolNode`. T1 его не породил, а сделал видимым. Фикс —
   параметризовать `ToolRuntime[AgentContext, ...]`, но это правка вне файлового скоупа трека →
   в бэклог/архитектору, не в GREEN этой итерации.

### Проверенное чисто

- **Тронутое лишнее — чисто.** Во всех пяти существующих файлах суммарно удалено 4 строки, все —
  импорты, тут же расширенные, плюс `security_guard=None` → `security_guard=guard` в
  `test_runner.py:_make_runner` (новый keyword-параметр с дефолтом `None`; поведение существующих
  вызовов не меняется). Ни один чужой ассерт не ослаблен, не переименован и не удалён — A6 соблюдён.
- **Покрытие против таблицы `streaming.md`** — все типы событий имеют оракул: новые закрыты этим
  диффом, `error` / `final_output_review_*` / `security_block` / `done` / `trace_id` /
  `artifact_created` для `generate_image` — существующими суитами, как и заявлено.
- **Мутации, подтвердившие силу набора** (каждая делает тесты красными): снятие `"callbacks": []`
  в `graph.py` (подтверждена заявка автора); перестановка приоритета резолва writer'а в
  `agent_events._resolve_writer` (3 теста); удаление `note_call_announced` из раннера (2 теста);
  снятие `"custom"` из `stream_mode` (3 теста).
- **Ручной хвост оправдан.** {T1.1}/{T1.3} — поведение конкретного провайдера, {T1.2} — реальный
  5-секундный тик, {T1.4} — отзывчивость на настоящем долгом инструменте, {T1.6} — качество
  вердикта guard'а (это eval, не unit), {T1.7} — живой старт с MCP. Две ремарки: в {T1.4}
  «шаги субагента с `parent_call_id`» уже закрыты автотестом (`test_runner.py`), ручной остаётся
  только отмена; Layer 2 «усечение >2000 в SSE и в parts» тоже автоматизировано с обеих сторон —
  это ре-верификация, не пробел.
