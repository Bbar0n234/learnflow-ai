# Summary: feat-002-chat-ux / трек T1 — Backend: контракты чатов + auto-title

## TL;DR

Трек T1 закрыт целиком (T1.1–T1.5). T1.1: `POST /projects/{id}/chats` больше не принимает тело — сервер сам ставит `DEFAULT_CHAT_TITLE = "Новый чат"`; `PUT /projects/{id}/chats/{chat_id}` (`ChatUpdate { title }`, лимит 100) переименовывает чат через `ChatService.rename_chat` → `ThreadViewRepository.update`. `ChatCreate` схема удалена целиком. Общая константа лимита (`MAX_TITLE_LENGTH`) вынесена в `app/services/constants.py` вместе с `DEFAULT_CHAT_TITLE`, применена и к `ProjectUpdate.name` (дрейф-фикс). T1.2: `DELETE /projects/{id}/chats/{chat_id}` → `204`, идемпотентный, с полным каскадом — новый `MCPServerRepository.cleanup_disables_for_thread`, `AgentRunner.delete_thread` в контракте Protocol, `LangGraphAgentRunner` получил keyword-only `checkpointer` (дефолт `None`) и реализацию `delete_thread` через `adelete_thread`, `app.state.checkpointer` прокинут в конструктор runner'а в `main.py`. `ChatService.delete_chat` — сначала транзакция БД (cleanup disables → `repo.delete` → commit), затем best-effort `agent_runner.delete_thread` под барьером. Ownership для DELETE резолвится вручную через новый `ChatService.get_thread_view` (не через `UserThread`-dependency) — сохраняет идемпотентность, зеркалит `delete_project`. T1.3: обязательная секция `title` в `AgentConfig`/`agent.yaml` (модель `deepseek/deepseek-v4-flash`, `extra_body.reasoning.effort: low`), фабрика `create_title_llm()` в `infra/llm.py`, новый таймаут `LLM_TITLE_TIMEOUT_SECONDS` (три места: `Settings`, `.env.example`, `docker-compose.yml`), промпт `configs/prompts/title.txt` + запись `title` в `configs/prompts.yaml` (`source: agent.title`) для авто-seed/sync. T1.4: новый `ChatTitleGenerator` (`backend/app/services/chat_title.py`) — fire-and-forget сервис, реестр `asyncio.Task` по `thread_id` в самом объекте, объект собирается в lifespan (`main.py`) и живёт в `app.state.chat_title_generator`; публичный метод `generate_title(thread_id, content)` возвращает handle задачи или `None`, если генерация для этого чата уже идёт. Задача открывает собственную сессию из `session_factory`, читает `ThreadView` до вызова модели (ранний выход без записи, если чат исчез/`security_blocked`/title уже не плейсхолдер — экономит LLM-вызов), вызывает `create_title_llm()`-модель, пост-обрабатывает ответ (strip → первая строка → усечение до `MAX_TITLE_LENGTH`) и пишет **атомарно-условным** `ThreadViewRepository.apply_generated_title` (`UPDATE ... WHERE security_blocked = false AND title = :placeholder RETURNING thread_id` — решающая проверка живёт в самой БД на момент записи; фикс FX3 ниже); барьер `try/except Exception` вокруг всего тела — `logger.warning(..., exc_info=True)` на любом отказе (включая пустой title после пост-обработки), `logger.info` на успехе; трейс — `get_client().start_as_current_observation(as_type="generation", ...)` под `contextlib.suppress(Exception)`, по образцу `generate_image`. `ChatService` получил опциональный keyword-only `title_generator` (дефолт `None`); `get_chat_service` (`api/deps.py`) прокидывает генератор из `app.state` тем же optional-паттерном, чтобы ASGI-тесты без lifespan не ломались. T1.5 (финальная фаза трека): `ChatService.send_message` запускает генерацию и доставляет её результат в стрим. Триггер — `thread_view.title == DEFAULT_CHAT_TITLE` **и** `title_generator` задан (не `None` в ASGI-тестах без lifespan); запуск — ровно на первом нетривиальном событии агента (после уже существующего `continue`-фильтра `trace_id`), если это событие **не** `security_block` — тогда вызывается `self._title_generator.generate_title(thread_id, content)`, хендл кладётся в локальную `title_task`; если первое событие — `security_block`, задача не создаётся вовсе (флаг-guard `guard_checked` фиксирует, что решение о старте принято, и не даёт повторных попыток на последующих событиях того же рана). Между каждой парой событий агента (после `yield event`, до следующей итерации `async for`) — проверка `title_task.done()`, кроме только что отданного терминального `error`/`security_block`, после которого поток закрыт и поллинг пропускается (фикс R1 ниже): если задача завершилась и вернула непустой title, в поток уходит `StreamEvent(type="title_updated", data={"title": ...})` и хендл сбрасывается в `None` (событие ровно одно за ран, второй раз не проверяется). Если рана не хватило (короткий ответ агента, задача не успела) — события нет, стрим закрывается штатным `done`; фронтовый fallback (трек T2) не в скоупе. `routes/messages.py` не тронут — `_event_generator` сериализует новое событие той же generic-механикой (`{"type": event.type, **event.data}`). Границы файлов не нарушены: диффа нет в `frontend/**`, `doc/**`, `backend/tests/conftest.py`, `packages/testing/**`, `Makefile`; ни один тест-файл не написан и не отредактирован в T1.5. `make check` (включая `lint-imports`, mypy, arch-checker) и `make test-scope P=backend/tests` — зелёные; единственный красный тест во всём прогоне — `tests/agent/test_pricing_external.py::test_active_model_prices_within_drift_tolerance` (`@pytest.mark.external`, живой ценовой дрейф OpenRouter-каталога на `z-ai/glm-5.2`), pre-existing и не связан ни с одной фазой трека (воспроизводился уже на T1.3/T1.4). `backend/tests/chat` отдельно — 51 passed, без единого изменённого ассерта в T1.5 (существующие тесты сервиса конструируют `ChatService` без `title_generator`, дефолт `None` держит `should_generate_title` ложным — поведение до T1.5 не меняется для всех сценариев без генератора).

Поверх трека закрыты находки TEST_REVIEW R1/R3 и blocker-находка ручного прогона F1. F1: `send_message` коммитил `touch(thread_view)` только в конце запроса, поэтому строка чата стояла под row-lock'ом весь стрим, фоновая title-задача висела на нём в своей сессии, `title_updated` был недостижим, а на ранах длиннее 120 с title терялся по `statement_timeout`. Фикс — явный `await self._session.commit()` сразу после `touch`, до входа в relay-цикл (ветка `conventions/db.md` § DB-сессии и commit, прецедент `create_chat`); к этому моменту в сессии запроса только SELECT'ы, поэтому предусловие правила выполняется. На живом стенде `title_updated` теперь приходит внутри стрима (за 4.3 с до `done`), `Lock`/`transactionid` в `pg_stat_activity` — ноль за 56 замеров; `make test-scope P=backend/tests/chat` — 110 passed, `make check` — зелёный. Кейсы `{T1.1}`, `{T1.4}`, `{T1.6}`, `{T1.7}` ждут перепрогона тестировщиком.

Поверх этого закрыты восемь находок CODE_REVIEW (режимы A и B) — секция «Фиксы по находкам CODE_REVIEW» ниже. Существенные для картины трека: у `ChatTitleGenerator` появился `shutdown()`, который lifespan вызывает до `engine.dispose()` (единственный blocker ревью B); решающий guard title-задачи переехал из `session.refresh` в атомарно-условный `UPDATE`, потому что перечитка принципиально не видела `security_blocked`, выставленный mid-stream в незакоммиченной транзакции запроса; `rename_chat` теперь коммитит до return, `delete_chat` требует сессию (fail-fast) вместо тихого пропуска cleanup полиморфных disables.

## Реализовано в фазе T1.1

- `backend/app/services/constants.py` (новый) — `DEFAULT_CHAT_TITLE` и `MAX_TITLE_LENGTH`, в сервисном слое (не в `services/chat.py`), чтобы схемы импортировали константу напрямую без зависимостей чат-сервиса.
- `backend/app/services/chat.py` — `create_chat(*, project_id)` без внешнего `title`, ставит `DEFAULT_CHAT_TITLE`; новый `rename_chat(thread_id, *, title)` — рефетч `ThreadView` по id (тот же паттерн, что уже использует `get_chat`), `EntityNotFoundError` при отсутствии, `ThreadViewRepository.update`, `logger.info("chat renamed", ...)`.
- `backend/app/api/schemas/chats.py` — `ChatCreate` удалена; добавлена `ChatUpdate { title: str }` с `Field(min_length=1, max_length=MAX_TITLE_LENGTH)`.
- `backend/app/api/schemas/__init__.py` — реэкспорт `ChatCreate` → `ChatUpdate` (импорт и `__all__`).
- `backend/app/api/schemas/projects.py` — дрейф-фикс: `ProjectUpdate.name` получила `Field(max_length=MAX_TITLE_LENGTH)`.
- `backend/app/api/routes/chats.py` — `create_chat` без body-параметра; новый `PUT /projects/{project_id}/chats/{chat_id}` → `200 ChatResponse` через `UserThread` (ownership штатной dependency, ручных проверок нет).
- `backend/tests/chat/test_chat_routes.py` — вызовы `POST /chats` приведены к контракту «тело игнорируется»: тест с `{"title": "My chat"}` теперь проверяет, что чужеродное тело не влияет на результат (title остаётся дефолтным); тест на дефолт и тест на 404 для чужого проекта отправляют запрос без тела; ожидание title везде — `"Новый чат"`.
- `backend/tests/chat/test_chat_service.py` — `service.create_chat(project_id=...)` без `title=`; ассерт сверяется с `DEFAULT_CHAT_TITLE` (импортирован из `app.services.constants`), не строковым литералом.

## Реализовано в фазе T1.2

- `backend/app/repositories/mcp_server.py` — новый `cleanup_disables_for_thread(thread_id)` по образцу `cleanup_disables_for_project`: подчищает disables со scope `('thread', thread_id)` и disables, ссылающиеся на `ThreadMCPServer` этого чата. Вызывается до удаления треда.
- `backend/app/services/agent_runner.py` — `AgentRunner` Protocol дополнен `async def delete_thread(*, thread_id: uuid.UUID) -> None`.
- `backend/app/agent/runner.py` — `LangGraphAgentRunner.__init__` получил keyword-only `checkpointer: AsyncPostgresSaver | None = None`; новый `delete_thread` вызывает `self._checkpointer.adelete_thread(str(thread_id))`, при `checkpointer is None` — no-op с `logger.warning`.
- `backend/app/main.py` — `checkpointer=checkpointer` добавлен в конструктор `LangGraphAgentRunner` в lifespan (объект уже был в скоупе как локальная переменная `async with ... create_checkpointer(...) as checkpointer`).
- `backend/app/services/chat.py` — новый `get_thread_view(thread_id) -> ThreadView` (raise `EntityNotFoundError`, если чата нет) — раздельный от тяжёлого `get_chat` (тот тянет историю + trace store); используется и DELETE-роутом для ручного ownership-чека, и самим `delete_chat` как defense-in-depth. Новый `delete_chat(thread_id)`: `get_thread_view` → (если `self._session` задан) `MCPServerRepository(self._session).cleanup_disables_for_thread` → `repo.delete` → `commit` → `logger.info("chat deleted", ...)` → best-effort `agent_runner.delete_thread` под `try/except Exception` + `logger.warning(..., exc_info=True)`. `MCPServerRepository` конструируется inline от `self._session` (см. «Решения» — почему не через конструктор).
- `backend/app/api/routes/chats.py` — новый `DELETE /projects/{project_id}/chats/{chat_id}` → `204`. Ownership резолвится вручную: `service.get_thread_view(chat_id)` в `try/except EntityNotFoundError` → `204` (идемпотентность), иначе сверка `thread_view.project_id != project.id` → `404` (чужой чат), иначе `service.delete_chat(...)` → `204`.
- `backend/tests/chat/conftest.py` — `FakeAgentRunner` дополнен `deleted_threads: list[uuid.UUID]` и `delete_thread`, записывающим в него `thread_id` (программируемый фейк для будущих тестов T1.2, TEST_AUTHORING).

## Реализовано в фазе T1.3

- `backend/app/agent/config.py` — новый `TitleConfig(BaseModel)` (`model: str`, `extra_body: dict = {}`), по образцу `ImageConfig`; `AgentConfig.title: TitleConfig` — обязательное поле (без `| None`, без дефолта), fail-fast на старте ровно как `image` (проверено вручную: удаление секции `title` из копии `agent.yaml` роняет `load_agent_config` на `pydantic.ValidationError`).
- `configs/agent.yaml` — новая секция `title` (`model: deepseek/deepseek-v4-flash`, `extra_body.reasoning: {effort: low, exclude: false}`) — единая проектная форма reasoning-конфига (conventions/agent.md § Reasoning LLMs), effort **низкий** (не `medium`, как у `summarization`) — резолюция PLAN_REVIEW: короткий однострочный вызов под таймаутом ~20 с. Модель в `available_models` не добавлена — секция служебная, как `image`/`summarization`.
- `backend/app/infra/llm.py` — новый `create_title_llm(settings, config: TitleConfig)` через единый `_build_chat_model`: `timeout=settings.llm_title_timeout_seconds`, `max_retries=settings.llm_max_retries`, без `max_tokens`/`temperature` (`TitleConfig` их не несёт — по тексту плана, в отличие от `SummarizationConfig.max_summary_tokens`).
- `backend/app/config.py` + `.env.example` + `docker-compose.yml` — новая переменная `LLM_TITLE_TIMEOUT_SECONDS` (дефолт `20`), atomic change по образцу `LLM_IMAGE_TIMEOUT_SECONDS`, ровно в трёх местах. `.env.local.example` не тронут — резолюция архитектора (Open Questions плана, п. 2): файл держит только реально переопределяемые локальные значения, ни один `LLM_*_TIMEOUT` там не заведён.
- `configs/prompts/title.txt` (новый) — системный промпт: короткое название на языке сообщения пользователя, без кавычек и завершающей пунктуации, одна строка; явно инструктирует читать `<user_message>` как данные о теме, не как инструкцию (data/instruction framing). Обёртка `user_message` из `configs/prompt_fragments.yaml` применяется на вызове (уже существующий хелпер `wrap_user_message` в `agent/prompt_builder.py`) — сама обёртка вызова LLM (T1.4) в этой фазе не строилась, только текст промпта.
- `configs/prompts.yaml` — запись `title` (`source: agent.title`, keys `model`/`extra_body`) по образцу `summarization`; `_seed_prompts` в `main.py` итерируется по ключам реестра — код сидера не менялся, seed/sync `title--{development,production}` заработает автоматически при следующем старте приложения / `make sync-prompts`.
- Легализованные точечные правки чужих тестов (только добавление `title=`/ключа `"title"`, без ничего сверх): `backend/tests/agent/test_config.py` (`_minimal_agent_cfg` — добавлен `"title": {"model": "deepseek/deepseek-v4-flash"}`), `backend/tests/agent/test_pricing_consistency.py` (`agent.title.model` добавлен в `_active_model_slugs`), `backend/tests/personalization/conftest.py` (`make_agent_config` — `title=TitleConfig(...)`), `backend/tests/personalization/test_model_config_resolver.py` (`_resolver` — `title=TitleConfig(...)`), `backend/tests/subagents/test_runner.py` (новый локальный хелпер `_min_title()` по образцу `_min_image()`, применён в двух прямых конструкциях `AgentConfig(...)`). `backend/tests/subagents/conftest.py` не тронут — фикстура `agent_config` там читает реальный `configs/agent.yaml` через `load_agent_config()`, обязательная секция подхватилась без правок.
- Verification: `make check` (ruff + mypy + `lint-imports` + arch-checker) — зелёный; `make test-scope P=backend/tests` — 752 passed, 1 failed (внешний ценовой дрейф, не в скоупе); целевые группы плана (`tests/agent`, `tests/personalization`, `tests/subagents`) — все зелёные; `test_active_models_each_match_exactly_one_pricing_entry` остался зелёным (`deepseek/deepseek-v4-flash` уже покрыт `configs/pricing.yaml` через существующую запись для `summarization`/`subagents`).

## Реализовано в фазе T1.4

- `backend/app/services/chat_title.py` (новый) — `ChatTitleGenerator`. Конструктор принимает `session_factory`, `settings`, `title_config: TitleConfig`, `prompt_provider: PromptProvider`, `prompt_fragments: PromptFragmentsConfig`, `langfuse_enabled: bool = False`; строит LLM один раз (`create_title_llm`) и держит `self._tasks: dict[uuid.UUID, asyncio.Task[str | None]]`.
  - `generate_title(thread_id, content) -> asyncio.Task[str | None] | None` — in-flight guard: `thread_id in self._tasks` возвращает `None` без создания новой задачи; иначе `asyncio.create_task(self._run(...))`, кладёт в реестр, `add_done_callback` делает `self._tasks.pop(thread_id, None)` (discard независимо от исхода — успех/ошибка/cancel).
  - `_run(thread_id, content)` — тело задачи: `async with self._session_factory() as session` → `ThreadViewRepository(session).get_by_id(thread_id)`; ранний `return None` (без записи, без лога — тихий выход), если чат `None`, `security_blocked`, или `title != DEFAULT_CHAT_TITLE`; иначе `_invoke_llm` → если пустой title после пост-обработки, `logger.warning("chat title generation produced empty title", ...)` + `return None`; иначе `repo.update(thread_view, title=title)` + `session.commit()`. Весь блок — `try/except Exception: logger.warning("chat title generation failed", thread_id=..., exc_info=True); return None`; успех — `logger.info("chat title generated", thread_id=..., title=...)` после `try` (недостижимо на любом из ранних `return` внутри `try`).
  - `_invoke_llm(content)` — системный промпт через `prompt_provider.get_prompt("title")` (без слотов — `title.txt` не содержит `{{ }}`), пользовательский текст — `wrap_user_message(prompt_fragments, content)` (тот же хелпер, что и основной граф, для единообразия data/instruction framing); `SystemMessage` + `HumanMessage` → `self._llm.ainvoke(...)`; постобработка модульной функцией `_postprocess_title` (strip → первая строка через `splitlines()[0]` → срез `[:MAX_TITLE_LENGTH]`); трейс — под `if self._langfuse_enabled: contextlib.suppress(Exception): get_client().start_as_current_observation(as_type="generation", name="chat-title", model=title_config.model, input=content, output=title): pass` (образец `generate_image`, резолюция PLAN_REVIEW трека — вне request/graph-контекста).
- `backend/app/services/chat.py` — конструктор `ChatService` получил `title_generator: ChatTitleGenerator | None = None` (keyword-only, в конце списка параметров) → `self._title_generator`; ничего не читает `send_message` в этой фазе (T1.5).
- `backend/app/api/deps.py` — `get_chat_service` тянет `getattr(request.app.state, "chat_title_generator", None)` и прокидывает в конструктор — тот же optional-паттерн, что и `trace_store`/`redis` чуть выше по функции; ASGI-тесты без lifespan получают `None`, конструктор это уже поддерживает.
- `backend/app/main.py` — `ChatTitleGenerator` собирается в lifespan сразу после блока `PromptProvider`/seed-prompts (session_factory, prompt_provider, agent_config, prompt_fragments, langfuse_enabled к этому моменту уже в скоупе) и до блока `Encryption service`; результат — `app.state.chat_title_generator`. Новый импорт `from app.services.chat_title import ChatTitleGenerator`, отсортирован по алфавиту (перед `app.services.encryption`).
- Тест-фикстуры трогать не пришлось: `ChatService` конструируется везде через keyword-args, новый параметр — с дефолтом `None`, ни один существующий вызов (`_build_service` в `test_chat_service.py`, `wired_runner` в `tests/chat/conftest.py`, `get_chat_service` override'ы) не сломался.
- Verification: `make check` (ruff + ruff format + mypy + `lint-imports` + arch-checker) — зелёный, без новых WARN. `make test-scope P=backend/tests` — 752 passed, 1 failed (тот же pre-existing внешний ценовой дрейф `z-ai/glm-5.2`, не в скоупе фазы); `tests/chat` отдельно — 51 passed. Импорт-целостность/подъём приложения — `tests/smoke/test_app_boot.py::test_create_app_registers_api_routes` (часть общего прогона) зелёный; изолированный прогон `uv run pytest tests/smoke -q` подтверждён отдельно. Прямой `python -c "from app.main import create_app"` вне pytest падает на отсутствующем `JWT_SECRET` в окружении шелла — это существующее поведение модуля (`app = create_app()` на уровне модуля плюс `Settings()` без .env в файле), не следствие правок этой фазы; смоук-тест использует тестовое окружение (conftest/.env.test) и проходит.

## Реализовано в фазе T1.5

- `backend/app/services/chat.py::send_message` — добавлен блок auto-title вокруг существующего relay-цикла, сам цикл событий-фильтров (`trace_id`, `artifact_created`, `error`/`security_block`) не тронут:
  - `should_generate_title = self._title_generator is not None and thread_view.title == DEFAULT_CHAT_TITLE` — вычисляется один раз до цикла, из уже прочитанного в начале метода `thread_view` (истории из checkpointer не читаем, ровно по тексту фазы);
  - `guard_checked: bool = False`, `title_task: asyncio.Task[str | None] | None = None` — локальные переменные генератора, не переживают вызов `send_message`;
  - внутри `async for event in ...`, **после** существующего `if event.type == "trace_id": ...; continue` (события `trace_id` не участвуют в решении о старте) и после уже существующих веток `artifact_created`/`error`/`security_block`: `if should_generate_title and not guard_checked: guard_checked = True; if event.type != "security_block": title_task = self._title_generator.generate_title(thread_id, content)`. Флаг `guard_checked` гарантирует, что решение принимается ровно один раз за ран — на первом дошедшем до этой точки событии, а не на каждом;
  - `yield event` — без изменений (позиция сохранена);
  - сразу после `yield event`: `if title_task is not None and title_task.done(): title = title_task.result(); title_task = None; if title: yield StreamEvent(type="title_updated", data={"title": title})`. Позиция "между событиями" в буквальном смысле плана — проверка происходит между обработкой события N и получением события N+1 от `async for`, что и требуется design-brief'ом (эмит "между событиями relay").
- `backend/app/api/routes/messages.py` — не тронут (сверено построчно с планом: `_event_generator` мапит `{"type": event.type, **event.data}` для любого `StreamEvent`, новый тип `title_updated` пройдёт этой же веткой без изменений кода).
- Импорт `asyncio` добавлен в `chat.py` (только для типа локальной переменной `title_task: asyncio.Task[str | None] | None`; сам `asyncio.create_task` вызывается внутри `ChatTitleGenerator`, не здесь).
- Verification: `make check` (ruff + ruff format + mypy + `lint-imports` + arch-checker) — зелёный. `make test-scope P=backend/tests/chat` — 51 passed (без единой правки тест-файлов — все существующие тесты `ChatService`/HTTP-роута конструируют сервис без `title_generator`, поэтому `should_generate_title` всегда `False` и новый код — no-op на этих путях). `make test-scope P=backend/tests` — 752 passed, 1 failed (тот же pre-existing `test_active_model_prices_within_drift_tolerance`, внешний ценовой дрейф `z-ai/glm-5.2`, воспроизводился и на T1.3/T1.4, не в скоупе фазы).

## Решения и обоснования

- **`MCPServerRepository` в `delete_chat` конструируется inline из `self._session`, а не инжектится через конструктор `ChatService`** (в отличие от `ProjectService`, где `mcp_server_repo` — required keyword-only параметр конструктора). Причина — граница теста: `test_chat_service.py::_build_service` (файл вне моего скоупа правки — только `conftest.py` в объёме `delete_thread`) собирает `ChatService` без этого параметра; сделай я его required, все существующие тесты сервиса упали бы на конструкторе. Опциональный параметр с дефолтом `None` тоже обсуждался, но потребовал бы либо силентного no-op cleanup при `None` (несовместимо с design-brief — cleanup обязателен перед `repo.delete`), либо всё равно правки конструктора без чистого выигрыша. `self._session` уже хранится в `ChatService` и используется для commit — `MCPServerRepository` тонкая обёртка вокруг сессии (тот же конструктор `__init__(self, session)`), инстанцирование на месте не создаёт нового паттерна в кодовой базе, просто не документированного явно. План T1.2 не упоминал изменение сигнатуры конструктора `ChatService` — расхождение с `ProjectService` осознанное и оставлено на усмотрение архитектора/будущего harvest, если стиль нужно унифицировать.
- **Ownership для DELETE — новый `ChatService.get_thread_view`, а не прямой `ThreadViewRepository` в роуте.** Первая попытка (repo напрямую в `routes/chats.py`, по образцу `api/deps.py::get_user_thread`) свалила `lint-imports`: контракт `api/routes must not import repositories, storage or agent directly` активен и не даёт новых нарушений (существующие исключения в `mcp_servers.py`/`settings.py` — allow-листнутый backlog, не прецедент для нового кода). `get_thread_view` — лёгкий сервисный метод (только `repo.get_by_id` + `EntityNotFoundError`), не дублирующий тяжёлый `get_chat` (история + trace store); используется дважды за запрос (роут для ownership-чека + `delete_chat` для defense-in-depth) — тот же паттерн двойного фетча, что `ProjectService.get_project`/`delete_project`.
- **Идемпотентность и «чужой чат» разведены на уровне роута, не сервиса.** `delete_chat` сам по себе продолжает поднимать `EntityNotFoundError` при отсутствии треда (симметрично `rename_chat`) — роут же перехватывает эту ошибку из `get_thread_view` до вызова `delete_chat` и превращает в `204`, а несовпадение `project_id` — в `404` до того, как `delete_chat` вообще вызван. Порядок «сначала транзакция БД, затем best-effort `adelete_thread`» из брифа не нарушен — `EntityNotFoundError` в `get_thread_view` не участвует в этой последовательности.
- **Инцидент с `git stash`/`git stash pop` во время верификации.** При воспроизведении внешнего pricing-теста на чистом чекауте (`git stash && ... && git stash pop`) `git stash` захватил ВЕСЬ working tree, включая одновременные несохранённые правки параллельного агента трека T2 (`frontend/**`) и предшествовавшие несохранённые правки T1.1/design-brief.md. `git stash pop` упал на конфликте (`ChatHeader.tsx`, изменённый T2 повторно между `stash`/`pop`) и не применил ничего (git abort — «would be overwritten by merge»). Восстановление: точечный `git checkout stash@{0} -- <path>` для каждого файла, который на тот момент совпадал с `HEAD` (т.е. его правки существовали только в стэше) — весь бэкенд-скоуп T1 + T1.1 + `design-brief.md` + 5 фронтенд-файлов, которые T2 не трогал повторно. Два файла, которые T2 продолжил редактировать после `stash` (`ChatHeader.tsx`, `index.css`), не тронуты — их текущая рабочая версия (новее, чем в стэше) оставлена как есть. Восстановление подтверждено `git diff stash@{0} -- <files>` (пусто для всех восстановленных). Стэш не удалён (`git stash drop` заблокирован классификатором permissions) — безвреден, содержимое полностью извлечено и проверено идентичным. Впредь: не гонять `git stash` без pathspec в shared worktree с параллельным агентом — эскалирую как находку для harvest/конвенций.
- **`test_create_chat_returns_201_with_title` не переведён в bodyless-запрос, а репозиционирован в тест «чужеродное тело игнорируется».** Буквальная фраза плана («приводятся к запросу без тела») при дословном применении ко всем POST-вызовам файла сделала бы этот тест дубликатом `test_create_chat_defaults_title_when_omitted` (оба — bodyless POST, оба проверяют дефолтный title). Вместо дублирования запрос оставлен с телом `{"title": "My chat"}`, но ассерт поменян на `"Новый чат"` — это прямая проверка контрактной строки verification-секции фазы («передача постороннего тела не влияет на результат»), не новый тест, а более осмысленная адаптация существующего. Имя функции не менялось (осталось стилистически неточным — "with_title"), чтобы не расширять диапазон правки за пределы «адаптации ассерта»; финальную ревизию имён тестов оставляю test-author/TEST_REVIEW.
- **`test_create_chat_in_other_users_project_returns_404` — тело убрано целиком** (не заменено на ассерт про игнорирование), так как этот тест не проверяет title вовсе — только ownership-404; сохранение тела там было бы просто мёртвым кодом.
- **`rename_chat` рефетчит `ThreadView` по `thread_id`, хотя роут уже резолвил chat через `UserThread`.** Выбран этот паттерн (а не передача уже резолвленного объекта в сервис), чтобы остаться консистентным с существующим `get_chat` — тот тоже принимает `thread_id` и рефетчит, несмотря на то что вызывающий роут уже имеет `thread: UserThread`. Разошёлся бы стиль сервисных методов, если бы `rename_chat` стал единственным, принимающим доменный объект напрямую.
- **`MAX_TITLE_LENGTH` — общее имя, не `CHAT_TITLE_MAX_LENGTH`.** Плейн-имя выбрано умышленно, поскольку константа с самого начала используется и для `ChatUpdate.title`, и для `ProjectUpdate.name` (дрейф-фикс) — специфичное к «chat» имя было бы вводящим в заблуждение уже в T1.1.
- **`ProjectUpdate.name` получила только `max_length`, без `min_length=1`.** Дрейф, зафиксированный в брифе, — отсутствие лимита длины; про нижнюю границу бриф не говорит явно (RegisterRequest.name — не буквальный образец для `ProjectUpdate`, а для чат-`ChatUpdate.title`, где план явно требует `min_length=1`). Добавление `min_length` к `ProjectUpdate.name` осталось бы отдельным архитектурным решением вне текста плана — не внесено, чтобы не расширять scope дрейф-фикса.
- **`FakeThreadViewRepo` (conftest) не тронут.** Ни один текущий тест T1.1-скоупа не вызывает `rename_chat`/`repo.update` через фейк — добавление метода `update` в фейк было бы упреждающей работой на тесты, которые ещё не существуют (пишет test-author), и вышло бы за рамки «не оставлять красными существующие тесты».
- Verification фазы подтверждена вручную по коду (не через новый интеграционный тест — это задача TEST_AUTHORING): `require_unblocked_thread` остаётся только на `POST /messages` (`backend/app/api/routes/messages.py`) — `rename_chat` под этой dependency не стоит, значит доступен и для `security_blocked` чатов.
- **Публичный метод генератора назван `generate_title(thread_id, content)`** — план (T1.4) не фиксирует имя, только сигнатуру `(thread_id, content)` и семантику возврата (handle или `None`). Имя выбрано для T1.5 (следующая фаза, тот же трек): вызов из relay-цикла будет выглядеть как `self._title_generator.generate_title(thread_id, content)` — это контракт, на который T1.5 может опираться напрямую, не изобретая имя заново.
- **`_run` коммитит сессию вручную (`await session.commit()`), а не через `async with session.begin()`** (в отличие от `image_generation.py`, где транзакция — `async with session_factory() as session, session.begin(): ...`). Причина — ранние `return None` (чат исчез/`security_blocked`/title не плейсхолдер/пустой title) должны молча закрыть сессию без записи, а `session.begin()` в форме контекст-менеджера коммитит по выходу из блока при отсутствии исключения — при раннем `return` он всё равно коммитит (пустую транзакцию, что безвредно, но концептуально не то же самое, что «выйти, ничего не записав» из read-only пути). Явный `commit()` только на успешном пути записи делает семантику «когда действительно пишем» явной в коде, а не полагается на то, что до `return` не было мутаций.
- **Отдельный флаг `guard_checked`, а не «`title_task is None`» как маркер «ещё не решали».** Первая версия проверяла только `title_task is None` перед попыткой старта — но это не различает «ещё не смотрели на первое событие» от «посмотрели, первое событие было `security_block`, задачу сознательно не создали». Без отдельного флага при `security_block` первым событием код продолжил бы пытаться создать задачу на каждом следующем событии рана (a `security_block` обычно терминален и следующих событий не будет, но полагаться на это — привязка к поведению runner'а, которой план не требует; фаза прямо говорит «при первом событии `security_block` задача не создаётся вовсе», то есть решение однократное и по значению именно первого события). `guard_checked` фиксирует факт «решение о старте принято» независимо от исхода.
- **Проверка `title_task.done()` — сразу после `yield event`, не перед ним.** Design-brief формулирует эмит как «между событиями агента» и явно завязывает on `task.done()` внутри цикла, не как отдельный шаг после цикла перед `done`. Разместить проверку до `yield` означало бы вставлять `title_updated` перед событием, которое его спровоцировало (нелогично: событие, на котором стартовала задача, ещё не гарантированно готово к моменту своего же yield — тайминг LLM-вызова не привязан к скорости агента). После `yield` — `title_updated` естественно оказывается между текущим и следующим событием агента, как в sequence-диаграмме брифа (`text_chunk ... ; title_updated ; ...`).
- **`title_task.result()` вызывается без `try/except`.** `ChatTitleGenerator._run` (T1.4) — весь корпус обёрнут в `try/except Exception: ...; return None`, то есть `_run` физически не может завершиться с исключением, кроме `asyncio.CancelledError` (не перехватывается барьером `except Exception`, но ничто в этом ране задачу не отменяет — ни `send_message`, ни вызывающий код `cancel()` не имеют ссылки на `title_task`, только `ChatTitleGenerator` внутренне управляет своим реестром). Дополнительный `try/except` вокруг `.result()` был бы defensive-код без реального сценария отказа в границах этой фазы — не добавлен, чтобы не маскировать потенциальный будущий баг тихим `except Exception`.
- **Трейс оборачивает готовый `title` post-hoc** (`get_client().start_as_current_observation(...)` с `input`/`output`, тело `with`-блока — `pass`), а не оборачивает сам `ainvoke`-вызов — тот же паттерн, что `generate_image`: наблюдение фиксирует факт и результат вызова для Langfuse, не измеряет тайминг самого вызова через контекст-менеджер. Согласуется с резолюцией PLAN_REVIEW плана (образец — `generate_image`, не `summarization`).

### Фиксы по находкам TEST_REVIEW (fixer, attempt 1)

- **R1 — `title_updated` мог уйти после терминального события** (`backend/app/services/chat.py`). Первопричина: поллинг `title_task.done()` стоял сразу за `yield event` и смотрел только на готовность задачи, игнорируя тип только что отданного события. Флаг `had_error` (выставляется на `error`/`security_block` до `yield`) в условии не участвовал, поэтому если генерация финишировала в окне между последним нетерминальным и терминальным событием, в поток уходила последовательность `… → security_block → title_updated`. Это ломает `doc/tech/streaming.md` § Protocol Overview («после терминального события поток закрывается»); фронт к этому моменту уже вызвал `endStream()`. Фикс: перед поллингом стоит `if had_error: continue` — на терминальном событии хендл не опрашивается вовсе, готовый title просто не попадает в стрим (в БД он всё равно записан задачей, клиент подхватит его штатным рефетчем — ровно fallback-ветка design-brief § Доставка title). Вариант «`break` после терминала» отвергнут: он менял бы потребление генератора runner'а, а фикс должен трогать только доставку title. Проверено ad-hoc-прогоном `send_message` с хендлом, который резолвится ровно перед терминальным событием: до фикса — `text_chunk, error, title_updated`, после — `text_chunk, error`; на здоровом ране `title_updated` по-прежнему эмитится (`text_chunk, text_chunk, title_updated, done`).
- **R3 — перечитка `ThreadView` стояла перед вызовом модели, а не перед записью** (`backend/app/services/chat_title.py`). Первопричина: единственная guard-проверка выполнялась сразу после `get_by_id` в начале `_run`, а запись — после `_invoke_llm`, то есть всё время LLM-вызова (до `LLM_TITLE_TIMEOUT_SECONDS` ≈ 20 с) состояние чата не перепроверялось: `security_blocked`, выставленный mid-stream, запись не отсекал, а пользовательский rename во время вызова молча затирался. Design-brief § Auto-title модуль и план T1.5 (обещание «блокировка после старта задачи … запись отсекается перечиткой `ThreadView`») требуют проверку **перед записью**. Фикс: guard вынесен в модульный предикат `_title_write_blocked(thread_view)` (`security_blocked` или title уже не плейсхолдер) и вызывается дважды — раннее, до вызова модели (чтобы не жечь LLM-вызов зря; эта проверка дополнительно ловит `thread_view is None`), и решающе, непосредственно перед `repo.update`.
- **Почему перечитка сделана через `session.refresh`, а не вторым `repo.get_by_id`.** `ThreadViewRepository.get_by_id` — это `session.get`, а `Session.get` по контракту SQLAlchemy 2.0 отдаёт объект из identity map **без SQL**, если он не expired (проверено по докстрингу установленного `sqlalchemy 2.0.48`, `orm/session.py:3595`), — второй `get_by_id` был бы перечиткой только на бумаге. `session.refresh(thread_view)` экспайрит атрибуты и делает настоящий SELECT; строка, удалённая за время вызова, даёт `ObjectDeletedError` — он перехвачен точечно и означает тихий выход без записи (как остальные guard-ветки), а не `warning "chat title generation failed"` от общего барьера. Вариант «открыть вторую сессию под запись» (тоже честная перечитка, к тому же освобождает соединение на время LLM-вызова) отвергнут по границе A6: существующие тесты ассертят `sessions.sessions[0].commits`, и смена числа сессий потребовала бы правки чужих тест-файлов.
- Ре-верификация после фиксов: `make test-scope P=backend/tests/chat` — **103 passed**, ни один тест-файл не тронут; `make check` — зелёный (ruff, ruff format, mypy 255 файлов, `lint-imports` 9/9 KEPT, arch-checker), новых WARN нет. Замечание для test-author'а (находка R2): unit-суита генератора работает на `FakeAsyncSession`, у которой `refresh` — no-op, поэтому решающая перечитка на фейке неотличима от ранней проверки; кейс «`security_blocked`/rename во время LLM-вызова» на текущем фейке не наблюдаем без его доработки (нужен `refresh`, подтягивающий состояние из общей `threads`-мапы).

### Фикс находки прогона F1 (fixer, attempt 1)

- **F1 — фоновая запись auto-title блокировалась row-lock'ом транзакции запроса** (`backend/app/services/chat.py::send_message`). Первопричина, подтверждённая диагностикой тестировщика и перечитанная по коду: `send_message` делает `await self._thread_view_repo.touch(thread_view)` — `UPDATE thread_views SET updated_at=now()` по строке этого же чата — в сессии запроса, а `get_db_session` (`api/deps.py:50-59`) коммитит её только после того, как SSE-генератор исчерпан. Незакоммиченный UPDATE держит row-lock всю длительность стрима, и `ChatTitleGenerator._run` в собственной сессии виснет на нём внутри `repo.update` (`chat_title.py:139`) с `wait_event=transactionid`. Следствия ровно те, что в находке: `title_updated` физически недостижим (запись разблокируется уже после закрытия потока), а на ране длиннее `DB_STATEMENT_TIMEOUT_SECONDS` (120 с) UPDATE задачи убивается `statement_timeout` и title теряется совсем.
- **Выбранная ветка конвенции — явный `await self._session.commit()` сразу после `touch`, до входа в relay-цикл** (`conventions/db.md` § DB-сессии и commit, правило «эффект должен пережить выход из handler'а» / прецедент `create_chat`, который коммитит до return в том же файле). Предусловие правила проверено по всему пути запроса, а не «на глаз»: к моменту `touch` в сессии нет ни одной незакоммиченной записи — dependency-цепочка `POST /messages` (`get_current_user` → `get_user_project` → `get_user_thread` → `require_unblocked_thread` → `get_by_id` в самом сервисе) состоит исключительно из SELECT'ов. Значит, ранний commit не фиксирует ничего, что должно было бы откатиться при ошибке ниже по стриму, а сам `touch` («чат использован в момент T») — как раз тот эффект, который обязан пережить и падение стрима, и его длительность. Записи, идущие позже в том же запросе (`mark_security_blocked` из security-пайплайна, `set_message_id` на артефактах), просто открывают новую транзакцию и по-прежнему коммитятся `get_db_session` в конце запроса — их семантика не изменилась.
- **Отвергнутая альтернатива — вынести `touch` в отдельную короткую сессию** (вторая ветка того же правила конвенции). Она тоже снимает блокировку, но применяется тогда, когда ранний commit невозможен, т.е. когда в сессии уже лежит что-то, что должно откатываться. Здесь такого нет, а цена отдельной сессии — лишнее соединение из пула на каждое сообщение и второй способ писать `thread_views` рядом с существующим `ThreadViewRepository(session)`-путём. Третье направление из находки («убрать `touch` из `send_message`») не рассматривалось как фикс: `updated_at` — источник сортировки в `list_by_project`/`list_recent`, его обновление на отправке сообщения — само поведение, а не побочный эффект.
- **Дрейф, исправленный тем же фиксом**: докстринг `RuntimeSecurityEnforcer._mark_blocked` (`backend/app/agent/runtime_security.py`) обосновывал переиспользование request-сессии тем, что «транзакция запроса уже держит row-lock на `thread_views` (через `touch`), поэтому вторая сессия здесь встанет в дедлок». После фикса это утверждение ложно — lock'а больше нет. Поведение не менялось (сессия запроса по-прежнему переиспользуется), переписано только обоснование: флаг принадлежит запросу, который его породил, и едет в его транзакции, а блокировка после `mark_security_blocked` держится лишь мгновение — блок терминален и поток закрывается сразу.
- Живая верификация на стенде (Postgres+Redis в docker, backend `make dev`, реальные ключи): новый чат → `POST /messages` («Объясни, как решать квадратные уравнения»), стрим 14:09:01.4–14:09:11.1. В потоке `text_chunk × 526 → title_updated ("Квадратные уравнения") в 14:09:06.820 → final_output_review_* → done в 14:09:11.135` — событие пришло **внутри** стрима, за 4.3 с до `done` (до фикса — 0 событий на 4 прогона). `chat title generated` в логе в 14:09:06.622, т.е. на 4.5 с раньше конца потока, а не через 7–200 мс после него. Параллельный опрос `pg_stat_activity` (56 замеров с шагом 1 с через весь стрим): `Lock`/`transactionid` — **0 совпадений**; сессия запроса весь стрим `idle in transaction` на read-only снимке (`SELECT user_settings …`), сессия title-задачи открывается и закрывается независимо. `thread_views.title = "Квадратные уравнения"`. Повторное сообщение в тот же чат: ни `title_updated`, ни второй `chat title generated` — триггер по плейсхолдеру не сработал, как и требует `{T1.1}`.
- Ре-верификация: `make test-scope P=backend/tests/chat` — **110 passed** (ни один тест-файл не тронут, целостность A6 сохранена); `make check` — зелёный (ruff, ruff format, mypy 255 файлов, `lint-imports` 9/9 KEPT, arch-checker), новых WARN нет.

### Фиксы по находкам CODE_REVIEW (fixer, attempt 1)

Восемь замечаний двух ревьюеров (`review-a.md`, `review-b.md`), в том числе единственный blocker ревью B. Прогнать backend-тесты при этих фиксах было нельзя — публикация портов Docker сломана на хосте (loopback-DNAT: хендшейк проходит, данные не идут), поэтому testcontainers-фикстуры не поднимаются; вместо прогона по каждому фиксу верификация — `make check` плюс временные in-memory зонды в scratchpad (удалены после). Перечень тестов к перепрогону — в конце секции.

- **FX1 — у `ChatTitleGenerator` не было teardown** (blocker ревью B; `backend/app/main.py`, `backend/app/services/chat_title.py`). Первопричина по контракту: `conventions/api.md` § Владение состоянием требует, чтобы ресурс, владеющий фоновыми задачами, гасился в lifespan (`create_task` + cancel после `yield`), а прецедент этого паттерна стоит пятью строками выше — `security_publisher_task`. Реестр title-задач из этого паттерна выпал: после `yield` его никто не трогал, а `await engine.dispose()` выполнялся, пока in-flight задача держала сессию из того же `session_factory` и могла висеть в `ainvoke` до `LLM_TITLE_TIMEOUT_SECONDS` (20 с). Итог на каждом рестарте — «Task was destroyed but it is pending» либо ошибка на уже уничтоженном пуле. Фикс: метод `ChatTitleGenerator.shutdown()` — снимок реестра (done-callback мутирует его по ходу отмены), `task.cancel()` каждому, затем `asyncio.gather(..., return_exceptions=True)`; вызов в lifespan сразу после блока остановки publisher'а и **до** `engine.dispose()`. Обращение к `app.state.chat_title_generator` безусловное: объект создаётся до `yield`, а код после `yield` не выполняется вовсе, если старт упал раньше.
- **FX2 — `title_task.result()` без защиты от `CancelledError`** (парно к FX1; `backend/app/services/chat.py`). До FX1 это было латентно и так и записано в решениях T1.5 («ничто в этом ране задачу не отменяет»); ровно с введением cancel утверждение перестало быть верным — барьер `_run` ловит `Exception`, а `CancelledError` в 3.11+ наследуется от `BaseException`, так что `.result()` поднял бы отмену внутрь SSE-генератора, и `_event_generator` превратил бы её в терминальный `error` на ровном месте. Фикс: `title = None if title_task.cancelled() else title_task.result()` — отменённая задача трактуется как «title не готов», ровно как штатная ветка «генерация не успела» (design-brief § Доставка title, fallback). Хендл всё равно сбрасывается в `None`, повторных опросов нет.
- **FX3 — решающий pre-write guard не видел mid-stream `security_blocked`** (`backend/app/services/chat_title.py`, `backend/app/repositories/thread_view.py`). Первопричина: `session.refresh(thread_view)` читает в транзакции задачи, а `RuntimeSecurityEnforcer._mark_blocked` только **флашит** `security_blocked=true` в сессии запроса — коммит происходит в `get_db_session` уже после закрытия потока. Под READ COMMITTED снимок задачи этот флаг увидеть не мог **в принципе**, никакая перечитка тут не помогает; guard пропускал запись, `repo.update` вставал на row-lock той же строки и после коммита запроса дописывал title в заблокированный чат. Бриф § Auto-title модуль требует обратного («задача … не пишет title, если чат … помечен `security_blocked`»), то есть для security-ветки контракт не выполнялся. Фикс: запись стала атомарно-условной — новый `ThreadViewRepository.apply_generated_title(thread_id, *, title, placeholder) -> bool` выполняет `UPDATE thread_views SET title=… WHERE thread_id=… AND security_blocked IS false AND title = :placeholder RETURNING thread_id`, то есть предикат проверяет сама БД в момент записи (после снятия row-lock'а Postgres перечитывает актуальную версию строки — EvalPlanQual — и на заблокированном чате не матчит ни одной строки). `bool` возвращается по `RETURNING` (у `Result` нет `rowcount` — это атрибут `CursorResult`, mypy такой доступ не пропускает), `synchronize_session=False` — вызывающий после записи в свой снимок строки больше не смотрит. `onupdate=func.now()` Core-апдейт применяет так же, как прежний ORM-путь (проверено компиляцией statement'а), поэтому поведение «переименованный чат всплывает в recents» не изменилось. Ранняя проверка до вызова модели (`_title_write_blocked` по снимку) сохранена — она экономит LLM-вызов и ничего не решает; её докстринг это теперь и говорит. Ветки «чат удалён» и «пользователь переименовал» продолжают работать тем же условием (0 строк совпало → `return None` без коммита) — проверено зондом на in-memory сессии, интерпретирующей UPDATE: happy path пишет и коммитит, block/rename/delete «во время LLM-вызова» не пишут и не коммитят, ранний выход не доходит до модели. Отмечу для ясности: `session.refresh` из фикса R3 (TEST_REVIEW, выше) этим фиксом **вытеснён** — механизм заменён, требование «решать по состоянию на момент записи» осталось.
- **FX4 — `assert` вместо сужения типа** (`backend/app/services/chat.py`). Единственный `assert` во всём `backend/app`: под `python -O` он вырезается, то есть в проде это не проверка, а комментарий; норма — не заводить новых приёмов без нужды (`conventions.md` § Code Quality). Фикс без нового приёма и без второй переменной: флаг `should_generate_title` заменён на локальную `title_generator = self._title_generator if thread_view.title == DEFAULT_CHAT_TITLE else None`, и цикл проверяет `title_generator is not None` — mypy сужает тип по этой же проверке, триггер и точка вызова стали одним условием.
- **FX5 — `rename_chat` не коммитил до return** (`backend/app/services/chat.py`). Норма — `conventions/db.md` § DB-сессии и commit, пункт «эффект нужен клиенту следующим запросом»: `useUpdateChat.onSuccess` немедленно инвалидирует `projects.chats` и `chats.recent`, эти GET'ы идут в своих сессиях, а `get_db_session` коммитит только после отправки ответа — окно на чтение старого названия. По этому же правилу уже коммитят `create_chat`, `delete_chat` и (после F1) `send_message`, так что внутри одного файла правило применялось непоследовательно. Фикс: `await self._session.commit()` перед `return` под тем же `if self._session is not None`, что и в `create_chat`. Предусловие правила выполнено — к этому моменту в сессии только SELECT'ы плюс сам UPDATE. `refresh` после commit не нужен: сессии и прода (`infra/db.py`), и тестового харнесса (`packages/testing/learnflow_testing/db.py`) создаются с `expire_on_commit=False`.
- **FX6 — `delete_chat` при `session is None` молча пропускал cleanup и commit** (`backend/app/services/chat.py`). Противоречие было двойным: `conventions.md` § Обработка ошибок запрещает молчаливую деградацию (тред удалялся, полиморфные disables оставались, а `logger.info("chat deleted")` и `delete_thread` отрабатывали как при успехе), и собственное решение T1.2 отвергло опциональный `mcp_server_repo` именно аргументом «потребовал бы силентного no-op cleanup при `None`» — а шипнутый код делал ровно этот no-op. Выбрана ветка «cleanup безусловен»: приватный `_require_session(operation)` возвращает сессию или поднимает `RuntimeError`, cleanup + `repo.delete` + `commit` идут без условий. Почему fail-fast, а не безусловный вызов с `None`: `session: AsyncSession | None = None` существует только ради sociable-unit-конструктора тестов, в проде `get_chat_service` сессию даёт всегда — значит `None` здесь не режим работы, а ошибка проводки, и она должна быть громкой. Порядок: сначала `get_thread_view` (его `EntityNotFoundError` роут превращает в идемпотентный 204 — контракт клиента важнее диагностики проводки), затем `_require_session`. Альтернатива «сделать `mcp_server_repo` обязательным параметром конструктора, как в `ProjectService`» — честнее по стилю, но ломает конструкторы во всех тест-файлах сервиса, поэтому оставлена в Follow-ups как была.
- **FX7 — два докстринга расходились с кодом** (`backend/app/services/constants.py`, `backend/app/services/chat.py`). `DEFAULT_CHAT_TITLE` обещал инвариант «must never collide with a user- or LLM-chosen title», которого код не держит и не может держать: триггер — обычное сравнение строк, и переименование чата в «Новый чат» снова включает генерацию (бриф § Триггер принимает этот edge case как безобидный). Формулировка ослаблена до фактической — вариант «отсекать плейсхолдер на входе `ChatUpdate`» не выбран: это продуктовое решение, а не правка докстринга. Докстринг `delete_chat` называл cleanup обязательным при условном коде — после FX6 условности нет, и докстринг дополнен явным «сессия обязательна, потому что …». Заодно переписан докстринг `_title_write_blocked` (FX3): он больше не выдаёт себя за решающий guard.
- **FX8 — ранний commit из F1 снял неявную сериализацию «нельзя удалить чат, пока он стримит».** Фиксирую как **осознанно принятое следствие**, а не как найденный ревью дефект. До F1 незакоммиченный `touch` держал строку `thread_views` весь стрим, поэтому `DELETE /chats/{id}` физически ждал конца стрима; теперь `delete_chat` коммитит удаление сразу, а `send_message` после входа в relay-цикл существование треда не перепроверяет — поток честно доигрывает до `done` для чата, которого уже нет. Деградация в этом окне тихая и безопасная: `mark_security_blocked` становится UPDATE по нулю строк, `artifacts.thread_id` уходит в `SET NULL` (`ondelete`), сгенерированный title не пишется (FX3: 0 строк совпало). Единственный жёсткий исход — вставка **нового** артефакта тем же раном упрётся в FK на удалённый `thread_views`; она уже под общим барьером стрима и даёт терминальный `error` в поток, который пользователь всё равно закрыл. Окно узкое (пользователь удаляет чат ровно во время его же стрима), а альтернатива — вернуть сериализацию через row-lock — это буквально возврат бага F1. Возможное косметическое улучшение (проверять `EntityNotFoundError` в post-hoc блоке и закрывать поток `error` вместо `done`) не делаю: это расширение контракта SSE вне брифа, кандидат в harvest.
- Ре-верификация: `make check` — **зелёный** (ruff, ruff format, mypy 256 файлов, `lint-imports` 9/9 KEPT, arch-checker), новых WARN нет — все шесть тех же, что и до фиксов. `make check-fe`/`make test-fe` не запускались: диффа во `frontend/**` нет. Backend-тесты прогнать не удалось по причине окружения (см. преамбулу секции), ни один тест-файл не тронут.
- **Что перепрогнать, когда инфраструктура тестов оживёт** (и почему я уверен в остальном). `backend/tests/chat/test_chat_title_generator.py` целиком — FX3 меняет способ записи, и часть кейсов упрётся в фейк (см. `[test]`-находку в Follow-ups). `backend/tests/chat/test_auto_title_row_lock.py` — фикс FX3 его не касается (тест пишет через `repo.update` из своей сессии, а не через генератор), но это единственный уровень, где вообще наблюдаемы row-lock'и, поэтому он в списке первым после генератора. `backend/tests/chat/test_auto_title_relay.py` и `test_auto_title_stream.py` — из-за FX2/FX4: relay-логика переписана в двух местах, хотя внешнее поведение не менялось (фейковые хендлы — `asyncio.Future`, `cancelled()` на них `False`, ветка ведёт себя ровно как раньше). `backend/tests/chat/test_chat_management_routes.py` — из-за FX5 (новый commit в `rename_chat`) и FX6; харнесс джойнит сессию в `create_savepoint`, так что commit релизит савпоинт, как уже делают `create_chat`/`delete_chat` в тех же тестах. `backend/tests/chat/test_chat_management_service.py` — из-за FX6. `backend/tests/smoke/` — из-за FX1 (правка lifespan).

### Doc-решения (docs-updater, фаза DOC_UPDATE)

- **Auto-title не получил отдельный документ.** Проверено по критериям `aidd-methodology` (кросс-сервисность, самодостаточность, объём, собственный жизненный цикл, ключевая доменная абстракция): модуль — один сервис в одном слое одного бэкенда, описание укладывается в несколько предложений внутри `backend.md` § Layered Architecture / Module Structure, без собственной иерархии подсекций. Секция в `backend.md` — по прецеденту `image`/`summarization` (те тоже не вынесены отдельно). Диаграмма Layered Architecture дополнена узлом `ChatTitleGenerator` (Service Layer) с рёбрами к `LLM`/`PromptProvider`/`repositories` — проверено рендером через `mermaid` MCP.
- **ADR не заводился ни на один из трёх кандидатов, рассмотренных явно:**
  1. *Fire-and-forget сервис с реестром задач в `app.state`* — не новый паттерн: прецедент `security_publisher_task` (тот же lifespan-механизм) уже существовал до итерации и уже задокументирован как конвенция (`conventions/api.md` § Владение состоянием, пункт про `asyncio.create_task` + cancel после `yield`). Итерация применяет существующее решение ко второму случаю, а не принимает новое архитектурное — ADR фиксировал бы то, что уже зафиксировано.
  2. *Доставка `title_updated` — non-terminal SSE-событие + fallback-инвалидация* — применение уже существующего расширения протокола (прецедент `final_output_review_*`, `pending images`), не новая протокольная механика. Trade-off уже полно задокументирован в design-brief § Доставка title (таблица отвергнутых альтернатив) и перенесён тезисно в `streaming.md`; по SSOT дублировать его в ADR избыточно — design-brief остаётся историческим источником обоснования, `streaming.md` — источником текущего поведения.
  3. *Порядок каскада delete: сначала транзакция БД, потом best-effort checkpoints* — узкое, не переиспользуемое за пределами одного метода решение (`ChatService.delete_chat`); рациональность (какой рассинхрон безопаснее) специфична для этой пары хранилищ и уже зафиксирована в design-brief таблицей каскада + F1/FX8 в этом summary. Схожий будущий кейс (чистка checkpoints при удалении проекта, backlog) — не автоматически идентичен: там нет промежуточных полиморфных disable-записей, решение придётся передумывать заново с учётом контекста. ADR на сегодняшний прецедент рисковал бы создать ложное ощущение переиспользуемого правила.

  Все три — не архитектурные развилки с несколькими живыми альтернативами на будущее, а либо повтор существующей конвенции, либо узко-локальное решение, полностью объяснённое в design-brief/summary. `doc/tech/adr/` проверен (029 — следующий свободный номер, не использован).
- Три пункта из «Незамеченного дрейфа документации» review-b (§ 13–15: исключение ownership в идемпотентном DELETE, смягчение правила «четыре места env», инвариант F1 про row-lock) — **не** правились в `conventions.md`/`conventions/api.md` напрямую: это правки самих конвенций, landing которых — решение архитектора на pre-commit gate (инструкция задачи). Переформулированы и переданы в отчёте docs-updater для harvest.

## Follow-ups

- **`[test]` для test-author'а — FX3 требует `execute` у `FakeAsyncSession`** (`backend/tests/chat/conftest.py`). Запись title больше не идёт через `repo.update` (ORM-мутация снимка + flush), а выполняется как `session.execute(update(...).returning(...))`. Фейковая сессия такой операции не знает: вызов упадёт в `AttributeError`, барьер задачи превратит его в `warning "chat title generation failed"`, и по факту сломается `test_generate_title_stores_postprocessed_model_output` (все 4 параметризации — title не запишется) и `test_generate_title_skips_write_when_the_chat_is_deleted_mid_call` (ассерт «ни одного warning»). Кейсы `test_generate_title_skips_write_when_the_chat_changes_mid_call` формально останутся зелёными, но станут вакуумными — пройдут по ошибке фейка, а не по guard'у. Правка: научить `FakeAsyncSession.execute` интерпретировать условный UPDATE против общей `threads`-мапы (снимок для этого больше не нужен — предикат целиком в statement'е). Побочный выигрыш: mid-call block/rename станут наблюдаемы честно, без моделирования изоляции снимков.
- **`[test]` для test-author'а — FX6 ломает `test_delete_chat_warns_but_completes_when_checkpoint_cleanup_fails`** (`backend/tests/chat/test_chat_management_service.py`). Тест строит `ChatService(session=None)` и вызывает `delete_chat`, который теперь на этом поднимает `RuntimeError`. Соседний `test_delete_chat_missing_thread_raises_not_found` не затронут — `EntityNotFoundError` поднимается раньше проверки сессии. То же поведение (warning при падении checkpoint-cleanup, чат всё равно удалён) уже покрыто на уровне роута с настоящей сессией — `test_chat_management_routes.py::test_delete_chat_survives_a_failing_checkpoint_cleanup`, — так что вариантов два: дать unit-тесту сессию или снять его как дубль.
- Стиль внедрения `MCPServerRepository` в `ChatService.delete_chat` (inline из `self._session`) расходится с `ProjectService` (required constructor param). Если унификация важна — понадобится также обновить `test_chat_service.py::_build_service` (вне моего скоупа правки в T1.2).
- Наблюдение для владельца git-гигиены worktree: `git stash` без pathspec в shared worktree с параллельно работающим агентом захватывает чужие несохранённые правки — риск гонки/потери данных. Возможная мера — предупреждение в conventions.md § Параллельная разработка о недопустимости `git stash` без явного pathspec в момент, когда рядом активен другой агент.

## SOFA-посты (id / применил / результат)

(пусто)
