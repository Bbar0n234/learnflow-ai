# Summary: feat-009 — Web search MCP: замена Firecrawl на Jina AI

## Что реализовано

- `configs/agent.yaml`: добавлен блок `mcp_servers.jina` (`enabled: true`, `transport: http`, `url: https://mcp.jina.ai/v1?include_tools=search_web,read_url`, `api_key_env: JINA_API_KEY`, `allowed_tools: [search_web, read_url]`); блок `firecrawl` переведён в `enabled: false` (fallback, остальное не тронуто); блок `tavily` удалён целиком. В `subagents.registry` у всех трёх субагентов (`judge`, `web-research`, `general-purpose`) списки `tools` заменены с firecrawl-трио на `[search_web, read_url]`. По ходу — драйф рядом: описания `web-research` и `general-purpose` буквально называли инструменты «search/scrape/extract», хотя extract-инструмента больше нет; поправлено на «search/read».
- `backend/contracts/agent-tool-names.json`: перегенерирован каноническим скриптом (см. ниже) — `tavily_extract`/`tavily_search` ушли, `search_web`/`read_url` добавились, `firecrawl_*` остались (см. «Решения»). Сортировка файла сохранена (генератор сортирует сам).
- `.env.example`: в секции «MCP External Tools» добавлен `JINA_API_KEY` первым, `FIRECRAWL_API_KEY` оставлен (fallback), `TAVILY_API_KEY` удалён.
- `docker-compose.yml`: в env backend-сервиса добавлена строка `JINA_API_KEY: ${JINA_API_KEY:-}` рядом с `FIRECRAWL_API_KEY`, строка `TAVILY_API_KEY` удалена. Комментарий над блоком не менялся.
- `.env.local.example`: не тронут — MCP-ключей там не было и их читают через `os.environ`, не через `Settings` (подтверждено комментарием в `docker-compose.yml` и отсутствием этих переменных в файле).
- `backend/app/main.py:561`: комментарий про инструменты `web-research` актуализирован (`firecrawl_*` → `search_web/read_url`), код не менялся.
- Тесты с зашитыми firecrawl-именами актуализированы под новую номенклатуру:
  - `backend/tests/agent/test_token_chunk_mapper.py`, `test_stream_events.py`, `test_runner_stream_contract.py`, `backend/tests/chat/test_chat_routes.py`, `test_stream_wire_contract.py` — везде это произвольные примеры имени tool-вызова в контрактах стриминга (не привязаны к реальному резолву конфига); `firecrawl_search` → `search_web`, `firecrawl_scrape` → `read_url`.
  - `backend/tests/subagents/test_runner.py` — здесь имена были содержательны: фейковый tool-pool (`_fake_firecrawl_pool`) резолвил инструменты реальных `SubagentSpec` из `configs/agent.yaml`, поэтому трио `firecrawl_search/scrape/extract` заменено на пару `search_web`/`read_url` (переименован и сам пул — `_fake_search_pool`), докстринг и синтетический `tools=["firecrawl_search"]` в тесте override-модели тоже обновлены.
  - `backend/tests/security/test_llm_defense_toggle.py` — оставлен без изменений: `_builtin_server(name: str = "firecrawl")` — родовой стенд для generic-валидации `_validate_builtin_mcp` с фиктивным `allowed_tools=["search"]`, не привязан к реальным именам инструментов; имя «firecrawl» здесь по-прежнему валидно как пример объявленного (пусть и выключенного) сервера.
- `make check` и `make test` прогнаны из корня worktree — оба зелёные (числа ниже).

## Решения и обоснования

- **Механика `agent-tool-names.json`**: контракт — не файл ручного сопровождения, а артефакт `scripts/generate_tool_names_fixture.py`, тонкой обёртки над `app.agent.tools.registry.build_tool_name_fixture(agent_config)`; та же функция гоняется в `backend/tests/agent/test_tool_names_fixture.py` как drift-gate (`test_fixture_matches_generated_output`). Обновление — регенерация, не ручная правка. Регенерировал: `PYTHONPATH=backend uv run --package learnflow-backend python scripts/generate_tool_names_fixture.py`.
- **Находка**: `builtin_mcp_tool_names()` (registry.py) собирает `allowed_tools` со **всех** объявленных `mcp_servers`, независимо от `enabled` — по явному дизайну (докстринг: «any `enabled`... since it can be turned on elsewhere»). Поскольку бриф требует оставить `firecrawl` в конфиге как выключенный fallback (без изменения его `allowed_tools`), контракт после регенерации содержит **и** `firecrawl_search/scrape/extract`, **и** `search_web`/`read_url` — 5 записей `builtin_mcp` (было 5: 3 firecrawl + 2 tavily; теперь 3 firecrawl + 2 jina), а не «заменить три записи на две», как буквально сформулировано в брифе (§ п.2 сценария). Это не рассинхрон и не баг — прямое следствие того, что `firecrawl`-блок физически остаётся в `mcp_servers`. Обновил контракт каноническим способом (regen), результат отражает реальный `agent.yaml`; расхождение с формулировкой брифа фиксирую здесь, правку не делал, т.к. решение (оставить firecrawl предзаявленным, а не удалить блок) — уже архитектурное и стоит выше этой фазы.
- **Тесты со стенд-именами firecrawl**: часть упоминаний (`test_stream_events.py` и стриминг-контракты) — это произвольные имена tool-вызовов в фикстурах, никак не резолвящиеся через `configs/agent.yaml` (сам маппер токенов/контракт SSE не знает о реестре инструментов). Переименование туда — не обязательное с точки зрения корректности тестов, но убирает шум «мёртвого» имени инструмента из тестовой базы, как просил бриф (п.7).
- **`_fake_firecrawl_pool` → `_fake_search_pool`**: единственное место, где имена firecrawl были содержательны — пул реально резолвил инструменты `SubagentSpec` из `configs/agent.yaml` (docstring прямо говорил «all specs use the firecrawl trio»). Переименование обязательно, иначе тест резолвил бы несуществующие инструменты.

## Отклонения

- Контракт `agent-tool-names.json` после регенерации содержал 5 `builtin_mcp`-записей (`firecrawl_extract`, `firecrawl_scrape`, `firecrawl_search`, `read_url`, `search_web`), а не 2, как буквально написано в задаче фазы («заменить три firecrawl-записи на две»). Причина и обоснование — см. «Решения» выше; итог соответствовал каноническому генератору и фактическому `configs/agent.yaml` (firecrawl оставался объявленным, но `enabled: false`). Фикс-раунд (FIX-5, см. ниже) убрал `firecrawl_extract` из `allowed_tools` — после регенерации записей 4 (`firecrawl_scrape`, `firecrawl_search`, `read_url`, `search_web`).
- Свежий `uv sync` (без `--all-packages`) в этом worktree один раз временно снёс backend/services-зависимости из общего `.venv` (root-проект не тянет их как свои deps) — исправлено повторным `uv sync --all-packages`. К изменениям кода/конфига не относится, упомянуто для прозрачности прогона проверок.
- `make test` — единственный красный тест, `backend/tests/agent/test_pricing_external.py::test_active_model_prices_within_drift_tolerance` (`@pytest.mark.external`), падает и на немодифицированной ветке (проверено через `git stash`) из-за дрейфа реальных цен провайдера моделей относительно `pricing.yaml` — не связано с этой итерацией, не чинил (вне scope, требует решения архитектора по `pricing.yaml`).

## Фиксы по code review

- **FIX-1 (blocker). Реестр подписей фронта** — `frontend/src/shared/config/agent-tools.ts`. Было: реестр знал только про `firecrawl_*`/`tavily_*`, живых `search_web`/`read_url` не было. Сделано: добавлены записи `search_web` (label «Ищу в интернете», иконка `Search`, аргумент `query`) и `read_url` (label «Читаю страницу», иконка `Globe`, аргумент `url`) — по образцу `firecrawl_search`/`firecrawl_scrape`; удалены записи `tavily_extract`/`tavily_search` и `firecrawl_extract` (инструмента больше нет); докстринг про «объявленный, но выключенный сервер» переведён с примера `tavily_*` на `firecrawl_*`. Побочно: после удаления обеих `extract`-записей осиротели импорт иконки `FileSearch` и хелпер `firstUrl` (использовались только там) — убраны, иначе `eslint`/`tsc` упали бы на неиспользуемом коде.
- **FIX-2 (blocker). Промпты субагентов** — `configs/prompts/subagent-general-purpose.txt:6`, `subagent-web-research.txt:13`, `subagent-judge.txt:14`. Было: формулировки «web search, scrape, and extract tools» / «search, scrape, and extract tools» — модель ожидала несуществующий extract-инструмент. Сделано: заменено на «web search and page-reading tools» / «web search and page-reading tools» (остальной текст промптов не тронут).
- **FIX-3 (blocker). Env-мануал Codex Cloud** — `doc/tech/setup/codex-cloud.md:145-146`. Было: в списке Environment variables — `TAVILY_API_KEY`. Сделано: строка заменена на `JINA_API_KEY` (в стиле соседних строк), `FIRECRAWL_API_KEY` остался (fallback).
- **FIX-4 (nit). Backlog-идентификаторы** — `doc/backlog.md:78`. Было: пункт P2 про graceful-деградацию старта называл устаревшие `mcp.firecrawl.dev` и `firecrawl_*`. Сделано: заменено на `mcp.jina.ai` и `search_web`/`read_url`; заодно выровнена формулировка «недоступном Firecrawl MCP» → «недоступном built-in MCP» для согласованности с обновлёнными идентификаторами. Приоритет и суть пункта не менялись.
- **FIX-5 (approved nice-to-have). Честный fallback-блок** — `configs/agent.yaml`. Было: `mcp_servers.firecrawl.allowed_tools` включал несуществующий в Firecrawl MCP v2 `firecrawl_extract`; блок не был помечен как fallback. Сделано: `firecrawl_extract` удалён из `allowed_tools`; над блоком добавлен комментарий `# Выключенный fallback-провайдер — возврат одной правкой (enabled: true).`; контракт перегенерирован (`PYTHONPATH=backend uv run --package learnflow-backend python scripts/generate_tool_names_fixture.py`) — `firecrawl_extract` ушёл из `backend/contracts/agent-tool-names.json` (было 5 `builtin_mcp`-записей, стало 4). Синхронно обновлено устаревшее «Отклонение» в этом summary (см. выше), которое ещё указывало старое число записей.
- **FIX-6 (nit). Фронтовые тесты — синхронная чистка** — `frontend/src/shared/config/agent-tools.test.ts`, `frontend/src/pages/chat/ui/MessageList.test.tsx`, `MessageItem.test.tsx`. Было: тестовые кейсы использовали мёртвые стенд-имена `firecrawl_search`/`firecrawl_scrape`/`firecrawl_extract`. Сделано: имена переведены на живые `search_web`/`read_url` (label и arg-ожидания те же, т.к. подписи перенесены 1:1); кейс подписи `firecrawl_extract` (label «Извлекаю данные со страницы») удалён целиком — соответствующего инструмента больше нет. Содержательность тестов не потеряна: те же сценарии (подпись из аргументов, разворот, усечение, статусы) теперь проверяются на реальных именах инструментов.
- **FIX-7 (nit). Основной системный промпт** — `configs/prompts/system.txt:33`. Было: «built-in MCP tools (e.g. web search, scraping)». Сделано: «built-in MCP tools (e.g. web search, page reading)».

Все семь фиксов применены без отклонений от заданной формы.

### Проверки

- `make check` — зелёный (ruff check, ruff format --check, mypy × 3, import-linter, arch-checker).
- `make test` — 1039 passed, 1 failed: `backend/tests/agent/test_pricing_external.py::test_active_model_prices_within_drift_tolerance` (внешняя сверка цен провайдера против `pricing.yaml`, падает и на чистой ветке — известное исключение, не чинилось).
- `make check-fe` — зелёный (tsc -b --noEmit, eslint, prettier --check). Потребовалось предварительно поставить зависимости фронта (`npm install` — `node_modules` отсутствовал в worktree), к изменениям кода не относится.
- `make test-fe` — зелёный: 40 test files passed, 395 tests passed.

## E2E-верификация (живой стек, Claude in Chrome)

Стек поднят из worktree (`make docker-up`, свежая БД, `JINA_API_KEY` в локальном `.env`), сценарии пройдены оркестратором через браузер.

- **Старт приложения**: чистый. `mcp tools loaded: servers_active=1, servers_disabled=0, servers_total=2, tool_count=2` — Jina подключился по streamable HTTP (protocol 2025-11-25), Bearer-auth и `?include_tools=` отработали, security-валидация `tools/list` пройдена, `run_subagent tool registered` (субагентский пул зарезолвился fail-fast без ошибок).
- **Сценарий 1 — поиск + чтение основного агента** (запрос по-русски про последнюю версию Python): `search_web` 2.9 с ✓ → `read_url` (docs.python.org/3/whatsnew/3.14.html) 6.7 с ✓; ответ фактически корректен (Python 3.14, PEP 750/649/749). Подписи в ленте — «Искал в интернете» / «Прочитал страницу» (реестр фронта из FIX-1 работает, fallback-подписей нет).
- **Сценарий 2 — субагент web-research, русскоязычный ресёрч** (изменения ЕГЭ по информатике 2026): субагент выполнил 3 русскоязычных поиска (2–4 с, все ✓) и ~8 чтений страниц — выдача релевантная, профильные источники (fipi.ru, sdamgia, РБК, lenta.ru); итоговая сводка содержательна и корректна. Русскоязычное качество поиска Jina — приемлемое (первый живой смоук, слепое пятно ресёрча закрыто).
- **Наблюдение (не дефект итерации)**: первый вызов субагента упал по `GraphRecursionError` — упёрся в `subagents.recursion_limit: 10` (agent.yaml), т.к. читал страницы по одной за цикл. Resilience отработала штатно: tool вернул ошибку, главный агент перезапустил субагента с более сфокусированной задачей, второй прогон завершился успешно. Латентность `read_url` при повторном чтении тех же URL падает с 12–19 с до 2–4 с (кэш Jina).

## Follow-ups

- `subagents.recursion_limit: 10` тесноват для read-heavy ресёрча с per-URL чтением (Jina `read_url` принимает один URL за вызов, в отличие от batch-fetch у некоторых альтернатив) — наблюдение для решения архитектора: поднять лимит, оставить как есть (retry главным агентом работает) или учесть при следующем шаге лестницы отступления.
- После merge добавить `JINA_API_KEY` в `.env` основного checkout и прод-окружения (в worktree добавлен локально, в git не попадает).
- Красный внешний тест `test_active_model_prices_within_drift_tolerance` (дрейф цен против `pricing.yaml`) — существовал до итерации, требует отдельного решения.
