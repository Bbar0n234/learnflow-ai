# Code Review Report — режим B (соответствие контракту)

Итерация feat-011-execution-runtime, диапазон `git diff develop...HEAD` (наши коммиты `f448076..185896b`, без изменений, пришедших с merge develop/feat-009). Сгенерированное (`uv.lock`, `backend/contracts/agent-tool-names.json`) и детерминированные проверки (arch-checker: AST passed, import-linter 9/9 KEPT) не перепроверялись.

### Summary

- **blocker: 0 / nit: 10 / pre-existing: 2**

Нарушений норм уровня blocker не найдено. Реализация соответствует design-brief и ADR-031/032 по всем проверенным контрактам; отступления, зафиксированные в `summary.md` треков (единый uid, `security_opt`, `tini`, `EXECUTOR_RUNTIME`-override, chown `/workspaces`, санитайз unicode, партиция `uv.lock`, `ArtifactWorkspaceService`/`UploadWorkspaceService` под import-linter, отдельный `register_workspace_path_error_handler`), проверены как решения — все обоснованы и соразмерны.

### Замечания

| Severity | Намерение | Файл:строка | Норма | Замечание | Предложение |
|---|---|---|---|---|---|
| nit | Устаревший докстринг + мёртвая ветка | `backend/app/agent/checkpoint_history.py:70,79` | CLAUDE.md § «Исправляй дрейф на месте»; § «без временных метапометок» | Докстринг утверждает «`generate_image` (pre-T1.7, still PG-backed) keys its own as `"id"`», и код держит фолбэк `item.get("path") or item.get("id", "")`. T1.7 уже в этой же ветке: `generate_image` эмитит `path` (`tools/image_generation.py:148`). Ветка недостижима, текст врёт следующему читателю | Снять фолбэк на `"id"` и переписать докстринг под фактическое состояние (все артефактные инструменты дают `path`) |
| nit | Тот же дрейф в API-схеме | `backend/app/api/schemas/chats.py:85-86` | там же | «`generate_image` (still PG-backed until T1.7) carries its PG id here instead» — состояния, которого больше нет | Переписать на текущее состояние |
| nit | Маркеры фаз итерации в проде | `backend/app/api/routes/artifacts.py:1`, `uploads.py:1`, `agent/tools/files.py:1`, `execution.py:1`, `infra/executor.py:1`, `services/artifact_workspace.py:9` (и др.) | CLAUDE.md § «Документация описывает текущее состояние — без временных метапометок» | Докстринги ссылаются на «T1.3/T1.4/T1.6/T1.8». Номера фаз не глобальны: `agent/subagents/runner.py:5,10,51` уже несёт T1.3/T1.5/T1.6 **другой** итерации — читатель не может разрешить ссылку. Правило написано про архитектурные документы, но здесь работает та же причина гниения | Оставлять ссылку на ADR/бриф (они стабильны), номер фазы — убирать. Кандидат в harvest как конвенция |
| nit | Устаревшая справка Makefile | `Makefile:27` | CLAUDE.md § «Исправляй дрейф на месте» | Help-строка `docker-build-executor` говорит «not wired into docker-compose.yml yet» — T1.11 подключила `executor` в compose (`docker-compose.yml:130`) | Обновить текст цели |
| nit | Расхождение payload с брифом | `backend/app/agent/tools/execution.py:257-258`, `agent/stream_events.py` (ветка `updated`), `frontend/src/shared/api/sse.ts` (тип `artifact_updated`) | design-brief § «Создание vs обновление — два события»: «превышение любого → дельта `null`»; «для бинарных — тоже `null`» | Для бинарного/oversized обновления ключ `diff` в SSE-payload **отсутствует**, а не приходит `null`: `_artifact_item` кладёт `diff` только когда он не `None`. Фронтовый тип объявляет `diff: {...} \| null` обязательным полем. Сейчас безвредно (SSE-диспетчер `diff` не читает, карточка берёт его из `ArtifactPart`, где `null` сериализуется корректно), но провод и его тип разошлись | Либо всегда класть `"diff": None` в конверт `updated`, либо сделать поле опциональным в `sse.ts` |
| nit | Промпт как второй источник правды о тулчейне | `configs/prompts/system.txt:64-65` | conventions.md § «Env vs константы» (бизнес-инвариант — в коде, один источник); conventions/agent.md § «Промпты — capability-язык» | Блок `<execution_environment>` перечисляет точный состав образа, включая `pillow` и `lxml`, которых нет в `services/executor/pyproject.toml` — они приезжают транзитивно (matplotlib → pillow, python-docx → lxml). Мажорный апгрейд соседа молча уносит заявленную промптом возможность; связи «промпт ↔ манифест» нет | Объявить `pillow`/`lxml` прямыми зависимостями executor'а (раз промпт на них ссылается); состав тулчейна — кандидат в проверяемый смоук (`smoke/smoke_fonts.py`-стиль), чтобы дрейф ловился воротами релиза образа |
| nit | Неограниченное чтение файла в память | `backend/app/storage/workspace.py:285` (`read_text`), `backend/app/services/artifact_workspace.py` (`get_artifact_detail` — лимита нет вовсе) | design-brief § Executor (принцип «потолок вывода — иначе OOM всего сервиса»); conventions.md § Восстановление | `workspace_read_limit_chars` применяется **после** полного `read_text()`; detail-эндпоинт читает файл целиком без какого-либо лимита. Файл, записанный джобой (гигабайтный `cat`-выход), кладёт процесс backend'а — тот самый класс отказа, ради которого потолок вывода поставлен в executor | Читать до лимита (+1 символ на признак усечения), а не усекать после чтения; detail — тот же knob. Пересекается с режимом A |
| nit | Вход без валидации | `backend/app/api/schemas/messages.py:8` (`attachments: list[str] = []`) | design-brief § Вложения: «Пометку формирует backend… он единственный знает канонический путь (сам его выдал)» | Клиент может прислать произвольные строки и произвольной длины: они попадают в model-facing пометку (`agent/runner.py:213-222`) и в `additional_kwargs` чекпоинта. Эскейпа нет (чтение всё равно идёт через `Workspace`), но «backend — источник пути» держится только на добросовестности клиента | Ограничить список по длине и потребовать префикс `uploads/` (или проверять существование) на входе схемы |
| nit | Серверный вызов мимо хука | `frontend/src/shared/api/uploads.ts:18`, вызовы `pages/chat/ui/ChatThread.tsx:175`, `ChatDraft.tsx:40`, `pages/project-chats/ui/ChatList.tsx:57` | conventions/frontend.md § «Ось состояния»: «Компоненты вызывают хуки, не API-функции напрямую» | `uploadFile` дёргается из компонентов без `useMutation`. Решение зафиксировано (плана T2 и докстринг модуля: кэшировать нечего, `uploads` только POST), прецедент есть (`downloadArtifact`, `ArtifactView.tsx:90`), ошибки читаются штатным `getApiErrorMessage`. Формально конвенция исключения для write-only не содержит | Либо завести тонкую `useUploadFile`-мутацию, либо дописать в конвенцию исключение «вызов без кэша — императивный» (harvest) |
| nit | Дрейф в самом документе норм, пройденный мимо | `doc/tech/conventions.md:278,391,394,519`; `conventions/api.md:27` | CLAUDE.md § «Исправляй дрейф на месте» | `conventions.md` правился на этой ветке (§ «Диаграмма на каждое нетривиальное решение»), но его же примеры остались указывать на снесённый этой итерацией код: `wkhtmltopdf` в карте «источник → статус» (`:278`), `ArtifactRepository` как образец Repository (`:391`), `BlobStorage`/`PgBlobStorage` как **дискриминатор** Repository vs Storage (`:394`), `PostgreSQL<br/>artifacts · artifact_blobs` как эталон нотации Mermaid (`:519`), `wkhtmltopdf` как эталонный случай `anyio.to_thread` (`api.md:27`). Это дороже обычного дрейфа: документ норм читают на каждой задаче, а его примеры теперь не находятся в кодовой базе | Правку взять в DOC_UPDATE первой: заменить примеры на живые (`Workspace`/`TraceStore` для Storage, executor/MCP для «внешний инструмент недоступен») |
| pre-existing | Size-чек | `backend/app/storage/workspace.py` (556 строк), `backend/app/agent/tools/` (12 модулей), `backend/app/services/` (19 модулей) | conventions.md § «Size-чек» (warning-only) | WARN'ы, которые оркестратор пометил как унаследованные, частично **новые**: `workspace.py` перешагнул порог 500 в этой итерации, `tools/` — порог 10 модулей. Режим warning-only, решение о разбиении за архитектором | Зафиксировать в harvest, чтобы порог не размывался молча |
| pre-existing | Покрытие arch-checker'ом | `pyproject.toml:71` (`root_packages` без `executor`) | conventions.md § Enforcement | Новый сервис `services/executor` не покрыт ни одним import-linter-контрактом. Бриф относит это к harvest явно («покрытие executor'а arch-checker'ом — кандидат в harvest») — не отступление, фиксирую как открытый долг | Взять в harvest |

### Blocker без прецедента в conventions

Нет.

### Незамеченный дрейф документации

Список для `docs-updater` (DOC_UPDATE идёт после этой фазы — всё ниже **ожидаемая** работа, не blocker; blocker'ом было бы молчаливое противоречие, а таких нет). Смысл секции — показать реальный масштаб: **бриф называет чек-листом только `streaming.md`** («источник `artifact_created`, payload, `artifact_updated`, invalidation-таблица, `ArtifactPart`, уход `set_message_id`»), фактически затронуто **тринадцать** документов, включая сам документ норм и четыре ADR.

**Приоритет 1 — документы, по которым работают на каждой задаче:**

1. **`doc/tech/conventions.md` + доменные** — вынесено отдельной строкой таблицы выше: `:278` (`wkhtmltopdf` в карте статусов), `:391` (`ArtifactRepository`), `:394` (`BlobStorage`/`PgBlobStorage` как дискриминатор Repository vs Storage), `:519` (`artifacts · artifact_blobs` как эталон нотации Mermaid), `conventions/api.md:27` (`wkhtmltopdf` как эталон `anyio.to_thread`), `conventions/agent.md:19,37` (custom-канал без `artifact_updated`; «артефакт по атрибуту» — атрибут стал списком, `kind` элемента выбирает тип события), `conventions/frontend.md:33` (перечень событий, не трогающих ленту, без `artifact_updated`).
2. **`doc/tech/backend.md`** — самый большой пласт. Слои: `:27`, `:129`, `:433` (`ArtifactService` в списке CRUD-сервисов), `:36`, `:176`, `:435` (`tools/ — KS, artifacts…`), `:43`, `:132`, `:441`, `:461` (Storage через `BlobStorage`/`PgBlobStorage`), `:144` (строка таблицы «Agent tools → Storage: `image_generation` → `PgBlobStorage`»), `:443` (`models/` с `Artifact`), `:159` (`get_artifact_media` «читает блоб через `BlobStorageDep`» — прецедент прямого инжекта, которого больше нет). REST: `:236-239` и `:381-394` (UUID-сегменты, `download?format=md|pdf`, `type` как `markdown|image`, prompt в `content`), `:241` (pdfkit + wkhtmltopdf), `:243` (immutable-кэш и `mime_type` блоба). Схемы: `:337` (`MessageOut.artifacts`), `:360` (`MessageCreate` без `attachments`). Персистентность: `:469`, `:470`, `:481`, `:514`, `:517-518`, `:533`, `:540-543` (таблицы, ER, связи `Project 1→N Artifact`, `Artifact→ThreadView`). Конфигурация: `:596-609` — ни одного из девяти новых knob'ов. Отсутствуют вовсе: файловый слой `app/storage/workspace.py`, `ArtifactWorkspaceService`/`UploadWorkspaceService`, `POST /projects/{id}/uploads`, `app/infra/executor.py`, том `workspaces` как ярус хранения, non-root uid образа.
3. **`doc/tech/streaming.md`** (единственное, что бриф назвал) — `:34`, `:51`, `:117`, `:146`, `:219` (payload `{id,…}`, имя секции, отсутствие `artifact_updated`); `:239`, `:242` («копит `artifact_created.id`», `ArtifactRepository.set_message_id`); `:264` (таблица typed parts без `artifact`), `:268` (`artifacts` как поле `MessageOut`); `:288` (invalidation — теперь точечная по пути + список с `exact: true` + довоз на `done`/`cancelled`/`error`). Не описан вход вложений (`attachments` в `POST /messages` → `MessageOut.attachments`).
4. **`doc/tech/agent-runtime.md`** — `:38` («artifact linking» у ChatService), `:83-84` (custom-канал), `:203-212` (таблица Artifacts: `create_artifact`, `generate_image` в `artifact_blobs`, «metadata артефакта (`id`, `title`, `type`)»), `:241`, `:319`, `:324`, `:334` (`input_artifact_ids` и резолв через `ArtifactRepository`), `:104` (скелет промпта без `<execution_environment>`/`attachment_note`), `:338` (пул tools субагента как простое объединение), `:369-374` (таблица деградаций без нового режима «execution runtime unavailable»). Пяти новых инструментов нет ни строкой.
5. **`doc/tech/frontend.md`** — `:41-42` (маршрут `:aid`, «скачивание md/pdf»), `:92` (карточка «по событию `artifact_created`» + `type === "image"`), `:112-113` (плоский список вместо дерева; caption-prompt; «блоба нет» вместо «файл больше не существует»), `:180-182` (query-keys с `aid`), `:195` (`staleTime: Infinity`, «контент иммутабелен»), `:208`, `:301` (invalidation), `:278-279`, `:295` (`isArtifactMediaNotFound` → `isArtifactNotFound`; нет `uploads.ts`), `:388`, `:446`, `:450`, `:474-476` (раскладка: нет `artifact-category.ts`, `use-composer-attachments.ts`, `use-file-drop.ts`, `AttachmentChip.tsx`, `DragOverlay.tsx`, нет композера вложений).
6. **`doc/tech/security-events.md`** — новый тип `agent.runtime.path_denied` (`siem_contracts/vocabulary.py:39`, эмитится `app/storage/workspace.py:264`) **не заведён в каталоге** (`:69-71`), домен описан как «Stream abortion, canary leaks» (`:18`). Файл на ветке правился (метаданные `deterministic_hit`) — пропуск именно этого события легко не заметить; conventions.md § Security Event Logging называет документ полным каталогом.

**Приоритет 2 — архитектурная карта и решения:**

7. **`doc/vision.md`** — `:64` («артефакты» в списке содержимого основной PostgreSQL) и `:20-66` (диаграмма + проза системной архитектуры знают три компонента; четвёртый standalone-сервис `services/executor` — FastAPI :8002, `runtime: runsc`, сеть `exec` с `internal: true`, общий том `workspaces`, uid 10001 — отсутствует, как и сам том).
8. **`doc/index.md`** — `:12-30` и `:47-50`: нет точки входа по executor'у (аналога `tech/siem-service.md`; документа `doc/tech/executor.md` не существует вовсе); `:73-76` — ADR-031/032 не в подборке (подборка выборочная — решение за архитектором).
9. **ADR** — `ADR-027-artifact-blob-storage.md:3-5` статус «Принято» без пометки, что ADR-032 его уточняет (обратной ссылки нет), тело `:45,47,54,56,58,90,91` описывает снесённые сущности; `ADR-007-mcp-external-tools.md:34` (`create_artifact` как app-managed persistence); `ADR-028-product-subagents.md:53,76` (`input_artifact_ids`, `ArtifactRepository`, `create_artifact`).
10. **`doc/security/architecture.md`** — правился на ветке под unicode-исключение, но `:141` держит `input_artifact_ids`, а новых границ доверия (executor: недоверенный код в изолированном контейнере/сети — ADR-031; граница путей workspace — `agent.runtime.path_denied`) в модели нет; `:258` не ссылается на ADR-031/032.
11. **`doc/tech/design-system.md:132`** — «За флагом сегодня: … вьюеры slides и audio»: `SlidesViewer.tsx`/`AudioViewer.tsx` удалены этой итерацией.
12. **`doc/tech/arch-checker.md`** — `:92` реестр инвариантов не отражает, что у backend теперь **два** места регистрации problem-handler'ов (`register_problem_handlers` + вынесенная за миррор `register_workspace_path_error_handler`, `backend/app/api/problem.py:216`); отдельно `:55-56` против `backend.md:159`: документы противоречат друг другу насчёт того, ходит ли `routes/artifacts.py` в storage напрямую (после этой итерации прав arch-checker.md — маршрут ходит через сервис).
13. **`doc/product/roadmap.md:157`** — «file attachments, PDF-экспорт» как план: вложения отгружены этой итерацией, а PDF-экспорт перестал быть бэкендовой фичей (стал выходом джобы).

Отдельно (не документация, но того же класса): `services/executor` не входит в `root_packages` import-linter (`pyproject.toml:71`) — бриф относит это к harvest явно.
