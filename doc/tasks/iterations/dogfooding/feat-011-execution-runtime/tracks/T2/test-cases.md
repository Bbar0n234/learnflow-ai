# Test Cases: feat-011 execution-runtime / трек T2 (frontend)

Трек переучил фронт на файловую модель: артефакт больше не строка в PostgreSQL с UUID, а файл, чья идентичность — путь. Отсюда всё остальное — REST и кэш адресуются путём, карточка артефакта рождается из части хода (`ArtifactPart`), перезапись видна бейджем «обновлён · +N −M», список стал деревом, вид вьюера выбирает словарь расширений. Вместе с этим приехал вход файлов от пользователя: вложения в композере, которые до отправки живут только в браузере.

Поведение меняется почти везде, поэтому кейсы страхуют не «ничего не сломалось», а сам новый контракт. Автотесты закрывают то, что видно в jsdom: словарь категорий, сборку дерева из плоских путей, точечные инвалидации кэша по SSE и довоз списка на терминальных ветках хода, перенос `ArtifactPart` в карточку с бейджем, тайминг вложений (✕ до отправки не ходит в сеть, upload идёт ровно в момент отправки, провалившаяся загрузка не отправляет сообщение и не стирает набранное) и различие «файла больше нет» против «сервер упал». Ручной хвост узкий: визуальная сверка с мокапом в обеих темах, настоящий drag&drop и системный диалог, реальное скачивание файла и ETag-ревалидация картинки — всё, чего среда тестов не исполняет. Сквозные сценарии приёмки (A3–A8, A11–A14) требуют живого бэкенда и уходят в INTEGRATION_TEST.

## Конвенции прохождения (инлайн — это рамка тестировщика)

**Статус и run-log.** У каждого кейса — текущий статус плюс опциональный run-log, если кейс прогонялся не раз:

- `- [x]` + лаконичный результат: что проверялось, что получилось, значимые нюансы. По заполненному чек-листу должно быть видно, что всё работает, без перепрохождения.
- `- [ ] ⚠️` + причина, если кейс не пройден или требует отдельного внимания.
- Кейсы с 👤 — требуют ручного действия / решения архитектора (UI, браузер); тестировщик помечает и эскалирует.
- **Доменные маркеры**: `📊` — проверка наблюдаемости; `🔴` — проверка реальных инъекций / security-событий; `[auto]` — кейс закрыт автотестом; `*(регресс)*` — кейс страхует «поведение не сломалось».
- **run-log** (только у перепрогнанных кейсов) — строка-история флипов с причиной:
  `runs: r1 ✅ → r2 ❌ (после фикса review #3: регрессия инвалидации) → r3 ✅`.
  Один прогон — run-log не нужен. Перепрогон после правки кода обязателен (см. ре-верификацию).

**Ре-верификация.** Правка кода аннулирует прошлый зелёный статус затронутого. После фиксов: детерминированный гейт (`make check-fe`, `make test-fe`) — перепрогон всегда; ручные/UI-кейсы — перепрогон только затронутой области. Каждый перепрогон → запись в run-log.

**Диагностика — через наблюдаемость, не догадки.** Один кейс — одна попытка диагностики: не сошлось — повтори (мог быть транзиент); не сошлось второй раз — fail + эскалация, без долгой отладки. Инструменты фронта: DevTools → Network (запросы, SSE-кадры, заголовки `Content-Disposition`/`ETag`), DevTools → Console (логи через `@/shared/lib/logger`), React Query Devtools для состояния кэша. Код читать только там, где поведение иначе не наблюдаемо (явно отмечать). Код тестировщик не правит: прод-баги, вскрытые кейсом, чинит **fixer**, не сам тестировщик.

**Скоуп по трекам.** Кейсы с префиксом трека (`{T2.x}`) гоняются на своём треке + Layer 0; cross-cutting (Layer 2/3 без префикса) — в INTEGRATION_TEST. Не пропускать кейсы молча — неприменимый помечать причиной.

### Процесс (тестировщик поднимает стенд сам)

1. Инфраструктура: `make docker-up-db`, `make migrate`, backend `make dev`, фронт `make dev-fe` (либо `make docker-up` целиком). Для сценариев с исполнением кода нужен поднятый executor трека T1.
2. Актор: обычный пользователь через UI register; проект с непустым workspace (хотя бы один артефакт в поддиректории — `lecture-1/…`).
3. Прогон сверху вниз; каждый failed-кейс — повторная попытка, затем фиксация в run-log + `## Решения и обоснования` summary трека.
4. После прогона — сводка (pass / failed / **deferred**). Deferred — кейсы 👤/заблокированные, отдельным счётчиком с причиной по каждому.

### Где смотреть состояние

| Что | Место |
|-----|-------|
| Фронт | `http://localhost:5173` (Vite) |
| Main app | `http://localhost:8000`, structlog stdout |
| Сеть / SSE | DevTools → Network (кадры `artifact_created`/`artifact_updated`, тело `POST /messages`, `POST /uploads`) |
| Кэш запросов | React Query Devtools (ключи `["projects", id, "artifacts", path]`) |
| Логи фронта | DevTools → Console (через `@/shared/lib/logger`) |

---

## Дизайн автотестов

Прогон: `make test-fe` — **507/507 зелёных, 45/45 файлов** (было 405 до трека тестов: добавлено 102 кейса в 5 новых и 8 расширенных суитах). `make check-fe` (tsc + ESLint с FSD-boundaries + Prettier) — 0 замечаний.

**Покрываем автотестом:**

### `artifact-category.test.ts` — словарь «расширение → вид вьюера»

1. **Файл**: `frontend/src/shared/lib/artifact-category.test.ts` — solitary-unit, без дублей (чистая функция).
2. **Тестирует**: `shared/lib/artifact-category :: getArtifactCategory`
3. **Суть**: Проверяет единственное место, где расширение файла превращается в вид представления, — а значит, страхует сразу трёх потребителей (вьюер, дерево списка, карточку чата) от того, что png покажется заглушкой «бинарный файл», а `.csv` из джобы — нечитаемым бинарником. Отдельно фиксирует, что отсутствие `content` в detail не перебивает распознанное расширение: тело картинки приезжает с media-эндпоинта, а не из detail.
4. **Кейсы**: `md` → markdown; пять растровых расширений → image; нераспознанное с содержимым (`txt`/`csv`/`py`/`json`/`yaml`) → text; нераспознанное без содержимого (`pdf`/`docx`/`xlsx`/`zip`) → binary; `png`/`md` без content остаются image/markdown; верхний регистр расширения категорию не меняет; дефолт без знания о content — text.

### `artifacts.test.ts` (расширен) — адресация артефакта путём и скачивание

1. **Файл**: `frontend/src/shared/api/artifacts.test.ts` — integration, сеть через MSW, свежий `QueryClient`.
2. **Тестирует**: `shared/api/artifacts :: getArtifact / getArtifactMedia / downloadArtifact / useArtifact`, фабрику ключей `shared/api/query-keys`.
3. **Суть**: Проверяет, что путь доезжает до сервера целиком и попадает в ключ кэша, а не теряет вложенность по дороге. Отдельно страхует имя сохраняемого файла: формата экспорта у файловой модели больше нет, и без фолбэка на basename конспект сохранялся бы под выдуманным расширением.
4. **Кейсы**: detail запрашивается с полным путём `lecture-1/slides.md`; media — тем же путём, без сегмента-идентификатора; одноимённые файлы разной вложенности не делят кэш (два хука на одном клиенте отдают разные данные); имя загрузки берётся из `Content-Disposition`; без заголовка — последний сегмент пути.

### `agent-feed.test.ts` (расширен) — артефакт как часть хода

1. **Файл**: `frontend/src/shared/lib/agent-feed.test.ts` — solitary-unit, чистая модель ленты.
2. **Тестирует**: `shared/lib/agent-feed :: fromMessageParts / groupFeedBlocks` (вариант `ArtifactPart`).
3. **Суть**: Проверяет, что связь «ход ↝ файл» переезжает из истории в модель без потери полей и что карточка получает собственный блок, а не растворяется строкой в ленте действий. Второе — не косметика: слипшись с соседними вызовами, карточка потеряла бы и превью, и бейдж, и ссылку.
4. **Кейсы**: перенос пути, заголовка, типа, `kind` и дельты строк; `diff: null` сохраняется как «счётчиков нет» (не нулевая дельта); `created` отличается от `updated`; карточка идёт блоком после текста; две карточки подряд не сливаются; карточка между двумя вызовами разрывает ленту на две.

### `useAgentStream.test.ts` (расширен) — свежесть кэшей файлового слоя и вложения в теле хода

1. **Файл**: `frontend/src/pages/chat/model/useAgentStream.test.ts` — integration, MSW со стриминговым телом `text/event-stream`.
2. **Тестирует**: `pages/chat/model/useAgentStream :: send` (ветки `artifact_created`/`artifact_updated`, терминальные `done`/`cancelled`/`error`, тело `POST /messages`).
3. **Суть**: Проверяет два разных механизма свежести и то, что они не съедают друг друга: событие про один файл освежает точечно его деталь вместе с медиа и список артефактов, но не трогает соседний артефакт, а завершение хода довозит список целиком — потому что событий удаления и переименования в контракте нет и список иначе копил бы призраков. Заодно страхует, что пути вложений уезжают в теле того же POST и переживают ретрай после протухшего токена.
4. **Кейсы**: `artifact_created` и `artifact_updated` (параметризовано) инвалидируют ключ пути + его media + список, не задевая соседний путь; список довозится на каждом из трёх терминальных событий, оставляя открытые детали нетронутыми; тело несёт `attachments: [пути]`; ход без файлов шлёт тело без этого поля; ретрай после 401 повторяет вложения.

### `uploads.test.ts` — транспорт вложения

1. **Файл**: `frontend/src/shared/api/uploads.test.ts` — integration, MSW + локальный полифилл `@/test/blob-polyfill`.
2. **Тестирует**: `shared/api/uploads :: uploadFile`
3. **Суть**: Проверяет, что файл уезжает одним multipart-запросом в проект и что фронт использует **возвращённый** сервером путь, а не имя исходного файла: сервер санитайзит имена и разводит коллизии суффиксом, так что сообщение со «своим» путём сослалось бы на несуществующий файл. Ошибка лимита проверяется той же цепочкой, что видит пользователь, — текстом из ответа сервера.
4. **Кейсы**: тело содержит поле `file` с именем и содержимым, заголовок несёт boundary, а возвращается путь ответа — имя файла и путь в фикстуре намеренно расходятся, поэтому путь, собранный из `file.name`, ассерт не проходит; 413 доезжает читаемым `detail` через `getApiErrorMessage`.

### `use-composer-attachments.test.ts` — состояние прикреплённых файлов

1. **Файл**: `frontend/src/shared/lib/use-composer-attachments.test.ts` — unit, `renderHook`.
2. **Тестирует**: `shared/lib/use-composer-attachments :: useComposerAttachments / formatFileSize`
3. **Суть**: Проверяет, что до отправки вложения живут исключительно в состоянии композера и что идентичность чипа — не имя файла: два одноимённых вложения обязаны сниматься независимо, иначе ✕ на одном уносила бы оба.
4. **Кейсы**: порядок выбора сохраняется; второй выбор дописывается к первому; снимается ровно указанный чип; два одноимённых файла независимы; сброс очищает всё; пустой выбор (отменённый диалог) состояние не трогает; таблица форматирования размера Б/КБ/МБ.

### `ChatInput.test.tsx` — композер существующего чата

1. **Файл**: `frontend/src/pages/chat/ui/ChatInput.test.tsx` — integration (компонент + его хуки), сеть не нужна: сетевого пути у композера нет вовсе — файлы грузят `ChatThread`/`ChatList`. Что до отправки не уходит ни одного запроса, наблюдаемо только там, где загрузка живёт (`ChatList.test.tsx`, A13); здесь проверяется состояние композера.
2. **Тестирует**: `pages/chat/ui/ChatInput` вместе с `shared/lib/use-file-drop`, `shared/ui/AttachmentChip`, `shared/ui/DragOverlay`.
3. **Суть**: Проверяет тайминг вложений глазами пользователя: файл прикрепляется чипом с именем и размером, ✕ до отправки выбрасывает его, не заводя отправку, отправка допустима с пустым текстом при наличии файла, а неудачная отправка сохраняет и текст, и чипы для повторной попытки. Отдельно страхует окно загрузки — повторный клик по «Отправить» не заводит вторую загрузку той же партии.
4. **Кейсы**: чип с именем и размером; ✕ до отправки убирает чип и не заводит отправку; два одноимённых файла — два независимых чипа; пустой композер не отправляется; пустой текст с вложением отправляется; успех очищает поле и чипы; `false` из `onSend` сохраняет всё; во время отправки кнопка и ✕ заблокированы, `onSend` вызван один раз; drag&drop — подсказка появляется, переживает переход курсора между частями композера (счётчик глубины), брошенный файл становится чипом.

### `ArtifactView.test.tsx` — вьюер артефакта

1. **Файл**: `frontend/src/pages/artifact/ui/ArtifactView.test.tsx` — integration, MSW + `routerAt` с `?path=`.
2. **Тестирует**: `pages/artifact/ui/ArtifactView` (диспетчер категорий, состояния ошибок), транзитивно `ImageViewer`.
3. **Суть**: Проверяет, что вид вьюера выбирает расширение, что скачивание предлагается по фактическому расширению файла, а обещания экспорта в PDF на экране больше нет. Ключевое различие — исчезнувший файл против упавшего сервера: путь переименовывается и удаляется штатно, поэтому 404 обязан читаться объяснением с самим путём, а не красной ошибкой загрузки, и наоборот — 5xx не имеет права выдавать себя за удалённый файл.
4. **Кейсы**: markdown показывает содержимое, шапку `md · изменён` и кнопку `.md`; кнопки с PDF нет; нераспознанное текстовое расширение рендерится текстом с кнопкой `.csv`; бинарный файл без содержимого предлагает скачивание вместо ошибки; png открывается картинкой; запрос уходит полным путём с поддиректориями; 404 → «Файл больше не существует» с путём; 500 → «Не удалось загрузить артефакт».

### `ArtifactList.test.tsx` (расширен) — дерево артефактов

1. **Файл**: `frontend/src/pages/artifacts/ui/ArtifactList.test.tsx` — integration, MSW + router.
2. **Тестирует**: `pages/artifacts/ui/ArtifactList` (сборка дерева, раскрытие, сортировка).
3. **Суть**: Проверяет, что фронт собирает вложенность из плоского списка путей так, как её видит пользователь: директории — группы со счётчиком всего поддерева, файлы корня — плоские строки, свежие выше давних. Отдельно страхует авто-раскрытие предков открытого файла: переход по карточке чата и перезагрузка страницы приводят прямо к вложенному пути, и в свёрнутом дереве открытый файл был бы не виден вовсе.
4. **Кейсы**: директория собирается из вложенных путей, счётчик считает всё поддерево; свёрнута по умолчанию, раскрывается кликом и ведёт на `?path=` с закодированным слэшем; схлопывается обратно; предки открытого пути раскрыты сразу на обоих уровнях; файлы корня отсортированы по дате изменения; подпись строки — расширение и дата.

### `MessageItem.test.tsx` (расширен) — чипы вложений и карточки хода в истории

1. **Файл**: `frontend/src/pages/chat/ui/MessageItem.test.tsx` — integration, router + `QueryClient`.
2. **Тестирует**: `pages/chat/ui/MessageItem` (ветка user-сообщения с `attachments`, блоки артефактов), транзитивно `ArtifactCard`.
3. **Суть**: Проверяет, что чип вложения в истории рисуется из metadata, а не парсится из текста, и подписан выбранным пользователем именем, а не путём, которым файл переименовал сервер. С другой стороны сообщения — что карточки артефактов встают из частей хода в их порядке и что перезапись помечена бейджем с дельтой.
4. **Кейсы**: чип на каждое вложение рядом с чистым текстом; подпись — имя, не путь; чип некликабелен (ни ссылки, ни кнопки); заблокированное сообщение не показывает ни чипов, ни текста; карточка файла после текста ответа ведёт на `?path=`; бейдж «обновлён · +12 −3»; две записи в один ход — две карточки.

### `ChatThread.test.tsx` (расширен) — отправка с вложениями

1. **Файл**: `frontend/src/pages/chat/ui/ChatThread.test.tsx` — integration, MSW (история, upload, SSE).
2. **Тестирует**: `pages/chat/ui/ChatThread :: handleSend / dispatchSend` и очередь входа из router state.
3. **Суть**: Проверяет сквозной порядок отправки: файл уезжает первым, и только подтверждённая загрузка заводит ход — сообщение с путём, которого ещё нет на сервере, агент прочитать не смог бы. Обратная ветка страхует, что провалившаяся загрузка не отправляет сообщение **вовсе** (а не «отправляет, но не показывает») и оставляет текст с чипами для повторной попытки — в том числе на партии из нескольких файлов, где отказ одного роняет всю отправку, а уже загруженный файл остаётся сиротой (принято брифом). Отдельно закрыт вход «одни вложения без текста» — путь догфудинга «лекция №1».
4. **Кейсы**: порядок `upload` → `message`, тело несёт путь; после успеха чип виден ровно один раз и уже вне композера (✕ у него нет — это и отличает состояние «отправлено» от «набрано»); при 413 на загрузке ни одного `POST /messages`, ошибка под композером, текст и чип на месте; два файла, 413 на втором — обе загрузки ушли, сообщение не ушло, оба чипа и текст сохранены; очередь входа с пустым текстом и вложением заводит ход; вырожденная очередь без текста и файлов не заводит ничего.

### `ChatList.test.tsx` (project-chats, расширен) — вложения в поле первого сообщения

1. **Файл**: `frontend/src/pages/project-chats/ui/ChatList.test.tsx` — integration, MSW + router-пробник состояния перехода.
2. **Тестирует**: `pages/project-chats/ui/ChatList :: handleCreate`
3. **Суть**: Проверяет вторую точку входа, с которой начинается догфудинг: файл прикрепляется, когда чата ещё нет, поэтому загрузка идёт в проект и обгоняет создание чата — иначе провалившаяся загрузка оставляла бы пустой чат без сообщения. Готовые пути уезжают в создаваемый чат через router state.
4. **Кейсы**: порядок `upload` → `create`, пути доезжают в состоянии перехода; один файл без текста отправляется (кнопка разблокирована вложением); при 413 чат не создаётся, ошибка видна, текст и чип сохранены; ✕ до отправки убирает файл, не тронув сервер.

### `agent-tools.test.ts` (расширен) — подписи файловых и исполняющих инструментов

1. **Файл**: `frontend/src/shared/config/agent-tools.test.ts` — solitary-unit, ожидания из мокапа.
2. **Тестирует**: `shared/config/agent-tools :: describeToolCall` (записи `read_file`, `write_file`, `list_files`, `execute_code`, `run_command`).
3. **Суть**: Проверяет, что новые инструменты читаются в ленте по-русски действием с осмысленным дополнением, а не машинным именем, и что упразднённый `create_artifact` подписи больше не имеет — оставшаяся запись врала бы о доступном инструменте. Полноту реестра против фикстуры бэкенда сторожит существующая часть суиты.
4. **Кейсы**: четыре подписи завершённых вызовов с дополнением (путь / команда); путь идёт без кавычек; запись файла подписана одинаково при создании и перезаписи; корень `"."` дополнением не становится; `execute_code` остаётся без дополнения; `create_artifact` резолвится fallback'ом.

### `src/test/blob-polyfill.ts`, `src/test/file-input.ts` — локальные тест-хелперы (не автотесты)

`jsdom` не реализует у `Blob` ни `arrayBuffer()`, ни `text()`, ни `stream()`, и перехватчик MSW под XHR не может прочитать тело с файлом: multipart-запрос **зависает до таймаута теста**, без ошибки, объясняющей причину. Хелпер ставит минимальные реализации поверх `FileReader` — по образцу уже живущих рядом `match-media-polyfill.ts` / `pointer-event-polyfill.ts` (локальные помощники, которые тест импортирует явно; замороженный `setup.ts` не тронут). Импортируется тремя суитами, где есть загрузка файла. Ограничение среды: `request.formData()` отдаёт часть-файл объектом чужого realm (`instanceof File` ложен), поэтому форма тела проверяется по сырому multipart-тексту — он приезжает целиком.

`file-input.ts` — вторая половина той же среды: выбор файла пользователь делает системным диалогом, которого в jsdom нет, поэтому отдать выбор можно только событием `change` на скрытом `<input type="file">`, а у скрытого input нет ни роли, ни доступного имени. Структурный селектор здесь неизбежен, но он ровно один на весь набор, спрятан за именем действия `attachFiles()` и не участвует ни в одном ассерте — кейсы читают результат по видимому чипу. Три суиты со вложениями (`ChatInput`, `ChatThread`, `ChatList`) берут хелпер отсюда, а не держат свою копию.

**Осознанно не покрываем автотестом:**

- Подсветка выбранной строки дерева артефактов — выражена только классами рамки/фона, доступного признака (`aria-current`) у строки нет, а проверка по `className` запрещена конвенцией → ручной кейс `{T2.3}` 👤.
- Соответствие мокапу и обеим темам (чипы, бейдж, дерево, оверлей drag&drop), полоса прогресса на чипе — jsdom не исполняет CSS и анимации, сторожа такому свойству тестовая среда дать не может → ручные кейсы `{T2.5}`, `{T2.8}` 👤.
- Настоящий drag&drop из файлового менеджера и системный диалог по скрепке — тест синтезирует события и подставляет файлы в скрытый input, реального `DataTransfer` и диалога ОС в jsdom нет → ручные кейсы `{T2.8}` 👤.
- Фактическое сохранение файла браузером (кликнутая ссылка, содержимое на диске) — проверено только имя, которое фронт предлагает; сама загрузка вне среды → ручной кейс `{T2.2}` 👤.
- ETag/Last-Modified-ревалидация media после перезаписи картинки — это поведение браузерного кэша, а не кода фронта → ручной кейс `{T2.1}` 👤 + сквозной A6.
- Вложения в draft-композере (`/chats/new`) отдельной суитой не покрыты: `ChatDraft` передаёт файлы в тот же `ChatInput` без собственной логики вложений, а загрузка и переход в чат уже покрыты `uploads.test.ts` и суитой поля первого сообщения → ручной кейс `{T2.8}`.
- Соответствие форм на проводе живому бэкенду (`ArtifactPart` с `artifact_type`/`kind`/`diff`, `artifact_updated`, `attachments: string[]` в `POST /messages`, `{path,title}` в metadata, 404 на detail несуществующего пути, `Content-Disposition` на download) — фронт реализован против контракта брифа, а не против запущенного сервиса → Layer 2, INTEGRATION_TEST.
- `StudioPanel` (dev-only заглушка за выключенным флагом) — недостижимый в проде код за статически ложным условием → мягкий гейт.

**Замеченные прод-баги (для fixer'а, сам не чиню):** не найдено. Все проверенные ветви ведут себя по контракту брифа; расхождений, потребовавших бы подгонки ассерта, не встретилось.

**Мутационная проверка сторожей** (прод-механизм ломался точечной правкой, набор прогонялся, правка откатывалась; дерево чистое, `make test-fe` после откатов — 507/507):

| Мутация | Покраснело | Те ли |
|---------|-----------|-------|
| Снят `exact: true` у инвалидации списка артефактов в ветке `artifact_*` | 2 | да — оба кейса «не задевая соседний артефакт» |
| Снята инвалидация списка на терминальной ветке `cancelled` | 1 | да — «довозит список артефактов на терминальном событии cancelled» |
| В catch провалившейся загрузки добавлен вызов отправки | 1 | да — «на провале загрузки сообщение не уходит» |
| `getArtifactCategory` всегда возвращает `text` (снята ветка бинарника) | 5 | да — четыре кейса словаря + «бинарный файл предлагает скачивание» |
| `hasContent` композера перестал учитывать вложения | 2 | да — «отправка с пустым текстом» и «не заводит вторую отправку» |
| Счётчик директории считает только прямых потомков | 1 | да — «считает файлы всего поддерева» |
| Снята инвалидация списка артефактов в ветке `artifact_*` целиком | 2 | да — оба параметризованных кейса `artifact_created`/`artifact_updated` (проверка после снятия дубля R1: сторож на месте) |
| Отказ одной загрузки перестал ронять партию — сообщение уходит с уцелевшими путями (полный отказ по-прежнему бросает) | 1 | да — «роняет всю партию, если из двух вложений не взлетело одно»; однофайловый 413 такую мутацию не различает и остаётся зелёным |
| `ChatInput` перестал очищать чипы на успешной отправке | 1 | да — «показывает отправленное сообщение с чипом сразу» |

Первая версия кейса «сообщение не уходит» мутацию **не** ловила: без зарегистрированного обработчика отправки ушедшее сообщение падало на MSW, а кейс оставался зелёным. Кейс усилен подсчётом обращений к `POST /messages` — и после этого мутация его красит.

Кейс «показывает отправленное сообщение с чипом сразу» проверен и на **тайминг** (находка R2): с той же мутацией и искусственно замедленной загрузкой прежний ассерт «ровно один чип с именем» оставался зелёным — первая проверка `waitFor` приходила ещё до отправки, на чипе композера. Усиленный ассерт (в том же `waitFor` — ✕ у чипа больше нет) на замедленной загрузке краснеет с мутацией и зеленеет без неё: состояния «набрано» и «отправлено» он теперь различает.

### Layer 0: Automated gate

- [ ] `make check-fe` — tsc `-b` strict + ESLint (включая `boundaries/dependencies`) + Prettier `--check` → **0 ошибок**.
- [ ] `make test-fe` — **507/507** зелёных, 45/45 файлов.
- [ ] `make build-fe` — прод-сборка проходит (`tsc -b` с `noUncheckedIndexedAccess` строже, чем `--noEmit`).
- [ ] `grep -rn "artifacts/\${" frontend/src` и `grep -rn "format=pdf\|message.artifacts" frontend/src` → пусто (следов PG-модели и экспорта не осталось).

---

## Ручные кейсы + статусы

### Layer 1: Трек T2 — фронт файловой модели и вложений

- [ ] `{T2.1}` 👤 Открыть png-артефакт во вьюере → попросить агента перезаписать этот же файл → после завершения хода вернуться на вкладку вьюера: показана новая картинка, не старая (Network: повторный запрос media, ответ не из кэша без ревалидации).
- [ ] `{T2.2}` 👤 Во вьюере markdown-артефакта нажать кнопку скачивания → файл сохраняется под своим именем и с расширением исходного файла; в шапке нет кнопки экспорта в PDF.
- [ ] `{T2.3}` 👤 Открыть вложенный артефакт из карточки чата → в дереве раскрыты его директории-предки, строка файла подсвечена как активная; схлопнуть группу вручную → она остаётся схлопнутой при повторных рендерах.
- [ ] `{T2.3}` 👤 Сверить дерево артефактов с мокапом `mockups/attachments-artifacts.html` (блок 3) в светлой и тёмной теме: отступы вложенности, иконки папки и файла, счётчик справа, подпись «тип · изменён».
- [ ] `{T2.5}` 👤 Сверить карточку артефакта с мокапом (блок 1, вариант A) в обеих темах: миниатюра png, путь под заголовком, бейдж «обновлён · +N −M» и бейдж без счётчиков у бинарной перезаписи.
- [ ] `{T2.6}` 👤 В ленте завершённого хода прочитать строки новых инструментов: «Прочитал файл · uploads/…», «Записал файл · artifacts/…», «Выполнил команду · …», «Выполнил код» без дополнения; развернуть строку — сырое имя и аргументы на месте.
- [ ] `{T2.8}` 👤 Перетащить файл из файлового менеджера на композер → появляется оверлей «Отпустите, чтобы прикрепить»; провести курсором над полем ввода и кнопками, не отпуская, — оверлей не мигает; отпустить → чип с именем и размером.
- [ ] `{T2.8}` 👤 Нажать скрепку → открывается системный диалог выбора файлов; выбрать несколько файлов разом → чипы на каждый.
- [ ] `{T2.8}` 👤 Отправить сообщение с вложением → на чипах видна полоса прогресса, ✕ на время загрузки недоступна, сообщение появляется в истории после завершения загрузки.
- [ ] `{T2.8}` 👤 Повторить прикрепление и отправку в draft-композере (`/chats/new`): чат создаётся, ход стартует, чипы видны у первого сообщения.
- [ ] `{T2.2}` 👤 Проверить читаемость страницы вьюера в обеих темах для трёх видов: markdown, текстовый (`.csv`), бинарный (`.pdf`).

### Layer 2: Integration (cross-cutting, в INTEGRATION_TEST)

- [ ] `A3` «Сохрани конспект» → после завершения хода карточка артефакта в чате, файл открывается из списка; в Network виден кадр `artifact_created` с путём.
- [ ] `A4` «Построй график matplotlib в png» → джоба пишет файл, приезжает `artifact_created`, карточка показывает миниатюру, вьюер открывает картинку.
- [ ] `A5` Артефакт в поддиректории (`lecture-1/slides.md`) открывается по ссылке со слэшем, адрес читаемый, перезагрузка страницы вьюер сохраняет; дерево показывает поддиректорию.
- [ ] `A6` «Поправь этот артефакт» → карточка с бейджем «обновлён · +N −M», вьюер и миниатюра показывают новую версию.
- [ ] `A6b` Перезапись артефакта джобой: у текстового файла бейдж с дельтой, у бинарного — бейдж без счётчиков (`diff: null` на проводе).
- [ ] `A6c` Джоба удалила или переименовала файл → после завершения хода список без призрака; клик по исторической карточке даёт «Файл больше не существует» с путём, а не красную ошибку.
- [ ] `A8` Перезагрузка страницы и открытие старого чата дают те же карточки артефактов, они кликаются (инвариант «live = история» там, где карточка рисуется).
- [ ] `A11` Прикрепить `.md`-заметку с вопросом → агент прочитал файл и ответил по содержимому; в теле `POST /messages` — путь `uploads/…`.
- [ ] `A12` Прикрепить PDF → агент извлёк текст через executor и работал с ним.
- [ ] `A13` Чип виден в композере до отправки; ✕ до отправки не порождает ни одного запроса (Network пуст); после отправки чип у сообщения в истории и после перезагрузки страницы.
- [ ] `A14` Файл сверх лимита → под композером текст ошибки от сервера, сообщение не ушло, текст и остальные вложения сохранены; после снятия лишнего файла отправка проходит.
- [ ] `B9` «Стоп» во время долгой джобы → ход остановлен, список артефактов после остановки актуален (сироты видны, призраков нет), следующее сообщение работает.
- [ ] `C3` REST на путях: list/detail/media/download отвечают по `?path=`, detail бинарного — метаданные без `content`, `download?format=pdf` больше не существует.

### Layer 3: E2E (cross-cutting, в INTEGRATION_TEST)

- [ ] `A7` 👤 Новый чат того же проекта: «продолжи вчерашнюю работу» → агент видит и читает файлы прошлого хода, лента показывает `list_files`/`read_file` с путями.
- [ ] Полный догфудинг-путь «лекция №1» 👤: на странице проекта прикрепить PDF лекции без текста → чат создаётся, агент извлекает текст, пишет конспект в `lecture-1/`, карточка и дерево показывают файл, вьюер его открывает.

---

## Находки ревью [severity+owner]

> Пишет **test-reviewer** (adversarial-ревью тестов против контракта, read-only). Каждая находка —
> severity (**blocker** / **major** / **minor**) + владелец фикса: `[test]` (test-author) /
> `[prod]` (fixer) / `[infra]` / `[doc]`. Чисто — секция пустая.

### Прогон 1 (2026-08-11, независимый ревьюер, свежий контекст)

Прогнано: `make test-fe` — 508/508, 45/45 (заявка подтверждена); `make check-fe` — 0 замечаний
(подтверждено). Diff тест-файлов сверен с `develop` построчно; прод-код трека прочитан там, где
зелёный тест мог бы лгать. Два подозрения на false-green проверены эмпирически временным
пробником (создан вне скоупа трека, прогнан, удалён; дерево чисто, прод и тесты не тронуты).

- **R1** minor `[test]` `frontend/src/pages/chat/model/useAgentStream.test.ts:474–507` — кейс
  «инвалидирует список артефактов проекта на `artifact_created`» **не может покраснеть по своей
  причине**: перед ассертом в поток кладётся `done` (`:499`), а терминальная ветка
  (`useAgentStream.ts:333–336`) инвалидирует тот же ключ тем же `exact: true`. Убери ветку
  `artifact_*` целиком — кейс останется зелёным. Сторожем работает параметризованный кейс `:546`,
  который ассертит до `done`. → удалить дубль либо перенести ассерт до `live.push({type:"done"})`.
  **✅ Закрыто (test-author):** дубль удалён, его мотивировка («карточку в живом ходе SSE не
  рисует, за событием остаётся побочный эффект») переехала комментарием к параметризованному
  сторожу. Что покрытие не потеряно, подтверждено мутацией: снятие инвалидации списка в ветке
  `artifact_*` красит ровно два параметризованных кейса.
- **R2** minor `[test]` `frontend/src/pages/chat/ui/ChatThread.test.tsx:531–559` — ассерт
  `getAllByText("notes.md")).toHaveLength(1)` (`:556`) одинаково выполняется **и до отправки**
  (чип композера), **и после** (чип истории), поэтому `waitFor` вправе резолвиться на первой же
  проверке в «неинтересном» состоянии. Второй ассерт (`:558`) разницы не вносит: проверено —
  RTL матчит `getByText` на значении контролируемого `<textarea>` (React пишет его в
  `defaultValue` → `textContent`), то есть строка находится и в композере, и в пузыре сообщения.
  Эмпирически на текущей машине первая проверка приходит уже после загрузки (чип композера снят),
  то есть **сегодня кейс зелёный по правильной причине**, но различия состояний он не делает и при
  более медленном планировании молча пройдёт мимо цели. → добавить разделитель состояний, напр.
  `expect(screen.queryByRole("button", { name: "Убрать notes.md" })).not.toBeInTheDocument()`.
  **✅ Закрыто (test-author):** оба ассерта сведены в один `waitFor` — «✕ у чипа нет» **и** «чип
  ровно один». Пройти его может только состояние после отправки: до неё ✕ на месте, во время —
  чипов два. Разделитель проверен эмпирически: с замедленной загрузкой и мутацией «композер не
  очищает чипы» прежний ассерт зелёный, усиленный — красный (см. § Мутационная проверка).
- **R3** minor `[test]` `frontend/src/pages/chat/model/useAgentStream.test.ts:494`,
  `frontend/src/shared/lib/agent-feed.test.ts:353,359` — фикстуры кладут `path: "artifacts/notes.md"`
  / `"artifacts/a-1.md"`, тогда как design-brief § Артефакты фиксирует идентичность артефакта как
  путь **относительно** зоны `artifacts/` (`lecture-1/slides.md`, без префикса). Фронту путь
  непрозрачен, поведение не задето, но при разборе INTEGRATION_TEST эта форма уведёт в сторону.
  Соседние кейсы (`:512–513`, `agent-feed.test.ts` § «артефакт из истории») используют форму
  контракта — расхождение внутри одного трека. → привести фикстуры к контракту.
  **✅ Закрыто (test-author):** фикстуры `agent-feed.test.ts` переведены на `lecture-1/notes.md`;
  фикстура `useAgentStream.test.ts:494` ушла вместе с дублем R1. Пути в аргументах инструментов
  (`agent-tools.test.ts`, `write_file` с `artifacts/lecture-1/konspekt.md`) не тронуты намеренно:
  там путь адресует workspace целиком, а не идентичность артефакта.
- **R4** minor `[test]` `frontend/src/pages/chat/ui/ChatInput.test.tsx:25–33`,
  `frontend/src/pages/chat/ui/ChatThread.test.tsx:481–489`,
  `frontend/src/pages/project-chats/ui/ChatList.test.tsx:255–261` — хелпер `attach()` через
  `container.querySelector('input[type="file"]')` утроен в трёх суитах.
  `conventions/testing.md` § Frontend прямо запрещает `container.querySelector` и ставит
  `getByTestId` последним разрешённым средством; общие тест-утилиты не дублируются. Обоснование
  «у скрытого input нет роли» верно, но выбор средства и трёхкратный дубль — нет. → один хелпер в
  `src/test/` (и/или доступный признак у input'а вместо селектора).
  **✅ Закрыто (test-author):** три копии сведены в `src/test/file-input.ts :: attachFiles` — тот же
  локальный паттерн, что у `blob-polyfill.ts`. Доступный признак у input'а потребовал бы правки
  прод-кода, которая тесту не принадлежит; селектор остался, но один на набор, спрятан за именем
  действия и вне ассертов (обоснование — в самом модуле и в § blob-polyfill / file-input).
- **R5** minor `[test]` `frontend/src/pages/chat/ui/ChatInput.test.tsx:54–67` — имя обещает «не
  порождает ни одного запроса», но у `ChatInput` сетевого пути нет вовсе (загрузку делает
  `ChatThread`/`ChatList`), поэтому наблюдать запрос суите нечем: единственный ассерт — `onSend`
  не вызван. Настоящий сторож A13 — `ChatList.test.tsx:347`. → переименовать в то, что кейс
  действительно проверяет, чтобы заявка не выглядела покрытой дважды.
  **✅ Закрыто (test-author):** кейс переименован в «✕ до отправки убирает чип и не заводит
  отправку»; в комментарии и в шапке суиты указано, что «ни одного запроса» наблюдаемо только в
  `ChatList.test.tsx`. § Дизайн автотестов поправлен там же.
- **R6** minor `[test]` `frontend/src/shared/api/uploads.test.ts:50–63` — кейс «отдаёт серверный
  путь, а не имя исходного файла» повторяет ассерт из `:38` (`result` равен телу ответа
  обработчика): оба ловят одну мутацию — «путь собран из `file.name` вместо ответа». Сигнал один,
  кейса два. → свести в один.
  **✅ Закрыто (test-author):** сведены в один. Слияние сделано в сторону сильной фикстуры: имя
  файла (`lecture.pdf`) и путь ответа (`uploads/lecture-2.pdf`) намеренно расходятся — прежний
  первый кейс с совпадающими `notes.md`/`uploads/notes.md` мутацию `uploads/${file.name}` как раз
  **не** различал, различал её только второй. Форма multipart-тела в кейсе сохранена целиком.
- **R7** minor `[test]` — **пробел на ветке множественных вложений.** `ChatThread.tsx:172`
  грузит файлы `Promise.all`, поэтому отказ на одном роняет всю партию: сообщение не уходит, все
  чипы остаются, а уже загруженные файлы становятся сиротами и при повторной отправке грузятся
  второй раз (сервер разведёт суффиксом). Покрыт только однофайловый 413
  (`ChatThread.test.tsx:~576`, `ChatList.test.tsx:~316`). Поведение брифом принято (§ Вложения,
  «upload прошёл, send упал»), но сторожа у него нет. → кейс на частичный отказ (2 файла, второй
  413) либо явная запись в «осознанно не покрываем».
  **✅ Закрыто (test-author):** добавлен кейс «роняет всю партию, если из двух вложений не взлетело
  одно» (`ChatThread.test.tsx`): обе загрузки ушли, `POST /messages` — ни одного, текст и оба чипа
  на месте. Сиротство уже загруженного файла зафиксировано ассертом на состав загрузок и
  комментарием как принятое следствие. Мутация «частичная отправка уцелевшими путями» красит ровно
  этот кейс — однофайловый 413 её не различает.
- **R8** minor `[doc]` — **вопрос архитектору, за пределами T2.** Реестр инструментов уехал
  дальше брифа: `search_web`/`read_url` → `tavily_search`/`tavily_extract`, добавлен
  `firecrawl_extract` (`backend/contracts/agent-tool-names.json`,
  `frontend/src/shared/config/agent-tools.ts:214,303`). design-brief § Инструменты про эти
  переименования молчит и утверждает «firecrawl MCP — без изменений». Тесты T2 следуют фикстуре
  бэкенда (это правильный оракул — `agent-tools.test.ts:44–96`), но правки существующих суит
  (`MessageItem.test.tsx`, `MessageList.test.tsx` — сплошная замена `search_web` →
  `firecrawl_search`) выглядят как подгонка под реализацию, пока бриф не догнал. → подтвердить
  намеренность и дописать в бриф/`streaming.md`, либо откатить переименование.
  **Не в работе test-author'а:** владелец `[doc]`, разбирает оркестратор. Имена инструментов в
  тестах оставлены как есть — оракул здесь фикстура бэкенда, а не бриф.

**Закрытие находок (GREEN, 2026-08-11).** R1–R7 (все `[test]`, minor) закрыты правками тестов;
прод-код не тронут — мутации применялись точечно и откатывались обратной правкой, дерево прод-кода
чистое. Счёт набора: 508 → **507** (−1 дубль R1, −1 дубль R6, +1 кейс R7). `make test-fe` —
507/507, 45/45; `make check-fe` — 0 замечаний. Прод-багов по ходу закрытия не вскрылось.

**Целостность (A6) — нарушений не нашёл.** Удалённые кейсы соответствуют удалённому функционалу:
`ImageViewer` — два кейса про промпт-подпись (промпт-колонка упразднена брифом § `generate_image`),
`artifacts.test.ts` — `isArtifactMediaNotFound` переименован в `isArtifactNotFound` вместе с
кейсами, `ArtifactCard` — три кейса переписаны под `ArtifactPart` **с сохранением и усилением**
ассертов (добавлены путь под заголовком и три кейса бейджа). Ослабленных ассертов выжившего
поведения против `develop` не обнаружено; счётчик кейсов вырос во всех тронутых суитах.

**`src/test/blob-polyfill.ts` — законен.** Замороженный `setup.ts` не тронут (проверено: файл в
`git status` не значится), полифилл — локальный модуль с явным импортом по образцу соседних
`match-media-polyfill.ts` / `pointer-event-polyfill.ts`, ставит недостающие в jsdom методы `Blob`
и ничего не подменяет в поведении приложения. Ограничение realm'а для `request.formData()`
задокументировано в самом модуле и в § Дизайн автотестов — проверка формы тела по сырому
multipart-тексту оправдана.

**Чисто:** контракт брифа (значение `artifact_type` = расширение, `diff {added,removed}|null`
против «нулевой дельты», `kind: created|updated`, path-query адресация, `attachments: string[]` в
теле `POST /messages`, `{path,title}` в metadata) в тестах взят из брифа, а не из реализации —
тавтологии и enshrined-багов не нашёл. Точечность инвалидаций (`exact: true` против префиксной)
проверена обеими сторонами: событие не задевает соседний путь, довоз списка не сносит открытую
деталь. Флака не нашёл: свежий `QueryClient` с `retry:false` в каждом рендере, Zustand сбрасывается
харнессом, фейковые таймеры включаются точечно и снимаются в общем `afterEach`, MSW стоит на
`onUnhandledRequest: "error"`, реальной сети нет. Дубли по правилу: сеть — только MSW, ручных моков
`fetch` нет; единственные `vi.spyOn`/`vi.fn` стоят на границе браузера (`URL.createObjectURL`,
`HTMLAnchorElement.click`) — там, где вызов и есть контракт. Мутационная таблица автора сверена с
кодом: заявленные счётчики и адреса покрасневших кейсов сходятся с прод-механизмами
(`useAgentStream.ts:257–266,364–367`, `ChatInput.tsx:60,163`, `artifact-category.ts:41–43`).
