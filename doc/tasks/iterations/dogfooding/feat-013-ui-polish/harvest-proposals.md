# Harvest-кандидаты: feat-013 — UI/UX polish

> Кандидаты на перенос в `doc/backlog.md` / `doc/tech/conventions.md`. **Только предложения** —
> landing после апрува архитектора на pre-commit gate. Секции ниже пополняет `harvester` на
> финализации; записи «anytime» вносит оркестратор по ходу итерации.

## Anytime-кандидаты (от оркестратора, по ходу итерации)

- **Свежий worktree не готов к работе: нет цели bootstrap.** На старте конвейера `make check` и
  `make check-fe` упали не по вине кода — в worktree отсутствовали `.venv` и `frontend/node_modules`,
  потребовались ручные `uv sync --all-packages` и `npm ci`. Конвенция (`conventions.md` § Git →
  «Lifecycle итерации») описывает создание worktree, но не его подготовку, а `make check` при этом
  заявлен предусловием старта итерации. Кандидат: цель `make bootstrap` (или `worktree-init`),
  выполняющая обе установки, + строка в lifecycle-разделе конвенции. Тип: конвенция + Makefile.
  Приоритет: P3 (разовая ручная работа на каждый новый worktree, ловушка для автономного агента —
  падение выглядит как сломанный код).

- **~~Фирменные цвета чужих брендов и запрет hex в `.tsx`~~ — кандидат снят.** Оркестратор разрешил
  брендовые hex внутри SVG-знаков провайдеров как исключение и предложил зафиксировать границу
  правила в конвенции. Code review (режим B) показал, что предлагать нечего: `design-system.md`
  § Границы **уже разбирает этот случай** — «когда значение должно жить вне темы (тёмные слайды,
  скрим линзы) — оно всё равно заводится CSS-переменной (`--slides-*`, `--scrim-overlay`), а не
  инлайн-литералом, чтобы граница „нет hex в компонентах“ оставалась абсолютной». Резолюция была
  ошибкой чтения нормы, а не пробелом в ней; правка конвенции не нужна, код приведён к правилу
  токенами `--provider-*`. Оставлено в реестре как след разбора, чтобы вопрос не заводили заново.
- **Англоязычный копирайт в `AuthGate.tsx` осознанно не тронут.** Автор тестов T4 нашёл в файле
  `placeholder="Username" / "Password" / "Confirm password"` и литеральное `...` — тот же класс блока 2
  «Заодно», но файл не принадлежит ни одному треку партиции. Решение оркестратора: **не трогать**.
  feat-008 перестраивает вход на страницу `/login`, и этот компонент уходит вместе с ней — перевод
  сейчас был бы выброшенной работой плюс лишний конфликт merge в файле, который итерация и так
  делит с соседней. Кандидат: проверить на гейте feat-008, что новый экран входа не унаследовал
  английские подписи (брендовый auth-дизайн от T6 их уже не содержит). Тип: проверка на стыке
  итераций. Приоритет: P3.

- **Мутационный прогон в общей ветке однажды не откатился — и это поймал сосед, а не автор.**
  Автор тестов T4 прогонял мутацию «убрать catch-all `path="*"`» и не восстановил строку: маршрут
  404 исчез из `app/router.tsx`, остался только неиспользуемый импорт. Собственный точечный прогон
  автора этого не показал (его кейсы краснеют на **отсутствии** мутации, а не на её остатке), тесты
  роутера в его прогоне уже были переписаны. Обнаружилось косвенно — трек T6 сообщил про `TS6133`
  в чужом файле, оркестратор проверил и восстановил маршрут. Конвенция (`testing.md` § Целостность
  тестов) требует откатывать мутацию точечной правкой, но не требует **доказательства** отката.
  Кандидат: дописать в правило обязательный шаг — после серии мутаций сверить рабочее дерево
  (`git diff` по прод-файлам скоупа, для untracked — сверка содержимого) и приложить результат к
  отчёту; в общей ветке цена пропущенного отката — тихая потеря чужой фичи. Тип: конвенция.
  Приоритет: **P2** — единственный случай за итерацию, когда в дереве оказался не тот код, который
  все считали сданным.

- **Чем искать элемент, у которого нет ни текста, ни роли (скелетон, плашка-резерв).**
  `testing.md` § Frontend задаёт лестницу запросов «роль → метка → текст → `getByTestId` как последнее
  средство», но плашка скелетона не имеет ни одного из первых трёх и при этом не является тестовым
  артефактом: у неё есть канонический `data-slot="skeleton"` от shadcn. Треки T4 и T6 независимо
  пришли к поиску по `data-slot`, оба ревьюера по существу это приняли (ассерт только на наличие,
  направление отказа безопасное), но оба отметили расхождение с буквой конвенции — то есть находка
  будет заводиться заново каждым следующим ревьюером. Кандидат: дописать в § Frontend ступень
  «канонический `data-slot` примитива дизайн-системы» между текстом и `getByTestId`, с оговоркой, что
  ею проверяется **выбор ветки**, а не геометрия. Тип: конвенция. Приоритет: P3.

- **Англоязычный копирайт вне инвентаризации блока 2 — долг старше итерации.** Финальный прогон
  нашёл на затронутых экранах то, чего бриф поимённо не перечислял: `MCPServerForm.tsx` целиком
  («Saving...», «Add Server», «Cancel», `placeholder="Bearer token"`) — форма рендерится на обеих
  страницах настроек рядом с уже локализованной `MCPServersSection`; ASCII-многоточия в
  `ChatInput.tsx:73` и `ReviewIndicator.tsx:11`. Ни один из трёх файлов не входит в закрытый список
  партиции, и оркестратор их **не трогал** — расширять скоуп на финальной фазе значило бы править код
  без плана, ревью и сторожа. Кандидат: отдельный проход «остатки локализации UI» по всему
  `frontend/src` с грепом по `alt=`, `title=`, `aria-label=`, `placeholder=`, `\.\.\.` и латинице в
  JSX-тексте — итерация показала, что точечная инвентаризация по брифу такие хвосты не ловит. Тип:
  backlog. Приоритет: P3.

- **Ошибки мутаций и валидации собираются вручную мимо `ErrorCard`.** `SecurityRules.tsx:130` и
  `RuleForm.tsx:180` рисуют карточку ошибки своей вёрсткой. Это не форк шаблона состояний — блок 4
  брифа канонизировал форму для **per-query** ошибок, а здесь ошибки мутации и формы, которые бриф
  явно оставил в тост-канале. Но визуально это та же карточка, собранная дважды. Кандидат: решить,
  распространяется ли канон `ErrorCard` на ошибки мутаций и форм, и если да — перевести оба места.
  Тип: конвенция + backlog. Приоритет: P3.

- **Скелетон таблицы SIEM продублирован в трёх файлах.** `SecurityEvents`, `SecurityAlerts` и
  `SecurityRules` несут по 21 идентичной строке разметки. Решение было осознанным и процессным:
  директория отдана треку **поимённым списком файлов**, а не целиком, поэтому завести общий компонент
  значило бы создать файл вне закрытого списка партиции. Обоснование истекло вместе с итерацией —
  а долг остался и в follow-ups треков записан не был (находка review-a). Кандидат: вынести общий
  скелетон таблицы в `pages/security/ui/`. Тип: backlog (tech debt). Приоритет: P3.

- **Однослотовые `abortRef` / `isCancellingRef` при формализованной модели двух живых потоков.**
  Итерация ввела владельца потока и owner-guard, то есть **признала**, что потоков может быть два
  одновременно (поток A дотекает, поток B начат). Но ссылки на отмену остались однослотовыми —
  второй `send()` перетирает `abortRef` первого. Брифом в scope не вносилось, поведение не определено,
  автотестом сознательно не фиксировалось (вопрос `question` в review-a, наблюдение в `{T2.4}`).
  Два конкретных следствия, которые ревью развернуло по коду (`useAgentStream.ts:92-93,389,414,428-438`):
  (1) поток, брошенный при переключении чата, не абортится никогда — на unmount убивается только
  последний контроллер, остальные дотекают до конца ответа сервера впустую; (2) `isCancellingRef`
  глобален — отмена хода в B выставляет флаг, и пришедший следом терминальный `error` догорающего хода
  A будет проглочен (пользователь не увидит, что ход в A упал). Кандидат: определить контракт отмены
  при двух живых потоках (карта по `chatId` вместо одного слота) — либо подтвердить, что это осознанно
  вне scope до следующей итерации. Тип: backlog. Приоритет: P3.

  **Процессное усиление находки «мутация без доказательства отката» (см. выше).** Трек T4 на
  собственном ревью уже применил именно ту практику, которую этот пункт предлагает закрепить
  конвенцией: до серии мутаций снял `sha256` пяти затронутых прод-файлов, после серии сверил
  контрольные суммы и состав `git status` — «ни одного лишнего и ни одного пропавшего» (`T4/test-cases.md:148`).
  То есть решение уже проверено практикой одного трека той же итерации, что снижает риск при переносе
  в конвенцию.

## Финализация (harvester)

Ниже — то, что добавилось при сведении шести `## Follow-ups` треков, секций «Осознанно не покрываем» /
«Находки ревью» из шести `test-cases.md` и обоих code review к уже записанным anytime-кандидатам
оркестратора. Часть находок ревью на момент финализации уже устранена в дереве (см. «Отсеяно») —
эти в кандидаты не идут.

### В backlog

- **P2 — `ErrorCard` не объявляет ошибку ассистивным технологиям.** `shared/ui/StateScreen.tsx:176`
  (`ErrorCard`) не несёт ни `role="alert"`, ни `aria-live` — на auth-экране (и на любом другом из
  семи потребителей канона) ошибка после сабмита появляется на экране молча для скринридера.
  Соседний примитив того же файла, `LoadingState`, `role="status"` уже получил по находке review-a
  (`StateScreen.tsx:138-152`) — граница применена не симметрично внутри одного файла. Находка была
  прямо вынесена треком T6 архитектору как вопрос вне его скоупа: «фиксить в каноне `ErrorCard` (тогда
  кейс на объявление ошибки — в суиту T1) или принять как есть» (`T6/test-cases.md`, находка R8, статус
  «вынесена в follow-up, вне скоупа трека»). На момент финализации не устранено. Тип: backlog
  (accessibility, фундаментный shared-компонент). Приоритет: **P2** — не блокер, но канон общий на весь
  продукт, и молчание для AT после сабмита формы — систематический, а не разовый дефект.

- **P2 — `AuthLayout` грузит `auth-hero` даже когда брендовая колонка скрыта ниже `lg`.**
  `shared/ui/AuthLayout.tsx:43-53`: колонка выключена классом `hidden lg:flex`, но `<Illustration
  scene="auth-hero">` внутри неё остаётся в DOM — браузеры в общем случае всё равно скачивают `src`
  у `<img>` внутри `display:none`. Источник: review-a nit. Цена — ~0.7–0.85 МБ на самом первом экране
  мобильной сессии, до всякого кэша. На момент финализации не устранено (иллюстрация в разметке
  безусловна). Тип: backlog (perf). Приоритет: **P2** — реальные байты на критичном первом экране, не
  косметика.

- **P2 — `ProjectSettingsPage`: секция «Удалить проект» не имеет ни одного теста.**
  Кнопка `deleteProject`/«Удалить проект…» (`pages/project-settings/ui/ProjectSettingsPage.tsx:254-264`)
  вызывает необратимую мутацию, но 486-строчный `ProjectSettingsPage.test.tsx` не содержит ни одного
  упоминания «Удалить»/`delete` — проверено грепом. Находка автора тестов T3, явно зафиксирована как
  «вне контракта T3» и «кандидат в follow-up, не долг трека» (`T3/test-cases.md:129`), но нигде не
  всплыла в секции `## Follow-ups` резюме трека — рисковала потеряться. Тип: backlog (test gap на
  необратимом действии). Приоритет: **P2** — единственный непокрытый деструктивный путь на всей
  странице, которую T3 иначе довёл до нулевого покрытия почти целиком.

- **P3 — Четыре потребителя `StateScreen` в error-режиме собирают один и тот же рецепт вручную.**
  `pages/chat/ui/ChatThread.tsx:190-202`, `pages/artifact/ui/ArtifactView.tsx:24-36`,
  `pages/sphere/ui/SphereView.tsx:24-36`, `app/components/ErrorBoundary.tsx:28-40` — одинаковые `scene`,
  `alt`, `illustrationClassName`, дословно одинаковая подпись, одинаковая кнопка `variant="outline"`,
  одинаковый `className="h-full"`. Источник: review-a suggestion. Правка формулировки подписи в одном
  месте сейчас разъезжается молча — вынести тонкую обёртку `QueryErrorScreen({ title, onRetry })` в
  `shared/ui`, заголовок остаётся единственным варьируемым параметром. Тип: backlog (dedup). Приоритет: P3.

- **P3 — Пустая сфера знаний показывает две неотличимые по доступному имени кнопки «Редактировать».**
  `pages/sphere/ui/SphereViewer.tsx:24-33` и `:49-58` — идентичная разметка (тот же `variant`, `size`,
  классы, иконка, подпись, `onEdit`), обе видны одновременно на пустой сфере. Источник: review-a nit +
  T4 test-cases, наблюдение 2 (`T4/test-cases.md:180`). Скринридер объявляет две одинаковые кнопки без
  различия; тест уже платит цену — 20 строк хелпера, обходящего DOM вверх по предкам, чтобы их
  различить. Дать CTA собственную подпись («Заполнить сферу» / «Начать») и вынести дублирующуюся
  разметку в локальный компонент. Тип: backlog (accessibility + dedup). Приоритет: P3.

- **P3 — Module-level состояние refresh-интерцептора в `shared/api/client.ts`.**
  `isRefreshing`/`pendingRequests` — module-level `let` (`client.ts:36-37`), что отступает от жёсткого
  правила CLAUDE.md «никаких module-level синглтонов» (состояние — в `app.state`/замыканиях/DI). План
  T5 явно исключил переписывание в треке («кандидат в Follow-ups»); зафиксировано автором как известное
  отступление, решение — за архитектором в отдельной итерации (`T5/summary.md` § Follow-ups). Тип:
  backlog (architecture). Приоритет: P3.

- **P3 — Мелкие CSS-нестыковки, оставленные параллельной работой шести треков.** Проверено по коду на
  момент финализации, не устранено: (1) `scrollbar-gutter: stable` продублирован инлайн-стилем в двух
  местах (`app/layouts/AppLayout.tsx:24` и `pages/chat/ui/MessageList.tsx:186`) вместо одной утилиты
  рядом с остальной механикой скроллбаров в `index.css`; (2) `pages/chat/ui/ChatThread.tsx:193` несёт
  лишний `w-full` в `illustrationClassName`, хотя примитив `StateScreen.tsx:111` уже добавляет `w-full`
  сам — три соседних потребителя передают только `max-w-[280px]`. Источник: review-a suggestion «мелкие
  расхождения между треками». Каждое по отдельности не стоит отдельного тикета — тот самый след
  параллельной работы, который позже объясняют «а у нас так исторически». Тип: backlog (cleanup).
  Приоритет: P3.

- **P3 — `rowMenuTriggerFor` дословно продублирован в двух тест-файлах (pre-existing, старше итерации).**
  `pages/project-chats/ui/ChatList.test.tsx:61-72` и `app/components/Sidebar.test.tsx:73-84` — идентичный
  хелпер с идентичным JSDoc, оба уже были в `develop`, итерация только правила соседние строки этих
  файлов. Источник: review-a pre-existing. Обход DOM вверх по предкам — самый хрупкий вид тестового
  селектора, живёт в двух копиях, будет расходиться молча. Вынести в `@/test/`-хелпер при следующем
  заходе в любой из двух файлов. Тип: backlog (tech debt, test harness). Приоритет: P3.

- **P3 — Вес PNG-иллюстраций (pre-existing, старше итерации, но усугублено тремя новыми сценами).**
  `shared/assets/illustrations/{light,dark}/*.png` — 9 сцен × 2 темы ≈ 17 МБ, отдельные сцены —
  0.65–1.46 МБ; три новые сцены итерации (0.70–0.84 МБ) на лёгком краю диапазона. Источник: review-a
  pre-existing. Каждое empty-/error-состояние стоит пользователю почти мегабайта. Отдельная задача —
  конвейер оптимизации ассетов (WebP/AVIF или пережатие + `srcset`), вне scope любой продуктовой
  итерации. Тип: backlog (perf, infra). Приоритет: P3.

### В конвенции

- **`doc/tech/conventions/testing.md` § Frontend — флак `userEvent`-тяжёлых кейсов под параллельной
  нагрузкой и дефолтные таймауты харнесса.** Наблюдение теста трека T2, расширенное ревью: под
  искусственной параллельной нагрузкой (четыре одновременных прогона скоупа) изредка краснеет любой
  `userEvent`-тяжёлый кейс фронта по таймауту `waitFor`/`userEvent` — истекают дефолтные таймауты, а не
  плывёт детерминизм теста (на последовательном прогоне тот же скоуп зелёный восемь раз подряд, ни один
  новый кейс трека ни разу не флипнул). Наблюдалось у `useAgentStream`/`ArtifactCard` в прогоне автора и
  у `ChatDraft` (файл вне трека) в перепрогоне ревьюера — `Duration 266s` против штатных 10с
  последовательно (`T2/test-cases.md`, наблюдение по харнессу + находка R5). SOFA-роль отдельно
  классифицировала это как не-Question («причина известна — голодание CPU, решение известно — таймауты
  харнесса, вопроса нет») и передала дальше как backlog/harvest, не как содержательный пост. Кандидат:
  поднять дефолтные таймауты `waitFor`/`userEvent` для `userEvent`-тяжёлых сценариев (или задокументировать
  как известный класс флака при параллельном запуске, с рецептом «перепрогони последовательно перед тем,
  как считать тест сломанным»). Тип: конвенция (testing.md § Frontend, харнесс). Приоритет: P3.

### Known-trivial (осознанно не заводим отдельным кандидатом)

- **Дублированный магический литерал `maxLength={100}` для имени проекта.** Для чатов есть
  `CHAT_TITLE_MAX_LENGTH` (`shared/api/chats.ts:88`), для проектов аналога нет — литерал уже сегодня
  живёт в двух местах (`ProjectSettingsPage.tsx`, `CreateProjectModal`). Источник: T3/summary.md
  Follow-ups. Дёшево поправить при следующем касании любого из двух файлов (завести
  `PROJECT_NAME_MAX_LENGTH` в `shared/api/projects.ts`) — цена бездействия минимальна (согласованность
  двух чисел, не поведенческий риск), отдельного тикета не стоит.
- **Динамический `import()` в четырёх хелперах `useAgentStream.test.ts` при статически импортированном
  модуле.** `frontend/src/pages/chat/model/useAgentStream.test.ts:119-143` — четыре хелпера дергают
  `await import("@/stores/stream-store")`, хотя модуль уже импортирован статически в шапке файла и новый
  `streamOwner()` обращается к нему напрямую. Источник: review-a nit. Чистая стилистика теста без риска
  для сигнала — читатель гадает, есть ли разница между двумя способами (её нет), но это не долг с ценой,
  а причёсывание при следующем касании файла.

## Отсеяно

Рассмотрено при сведении находок шести треков и обоих code review, в кандидаты не пошло:

- **Хардкод hex/rgba в `ProviderButton.tsx`** (review-b blocker) — на момент финализации код уже несёт
  блок токенов `--provider-google-*`/`--provider-github-fg`/`--provider-yandex-*` в `index.css` и
  комментарий в `ProviderButton.tsx`, объясняющий приём (тот же, что у `--slides-*`/`--scrim-overlay`).
  Landed, тот же вывод, что и в резолюции оркестратора выше по файлу.
- **Firefox-фолбэк скроллбаров не доезжал до вложенных контейнеров** (review-a/review-b nit) —
  устранено: `index.css` объявляет `scrollbar-width: thin` на `*` внутри гарда `@supports not
  selector(::-webkit-scrollbar)`, а не только на `:root`. Landed; знание об этой ловушке ушло отдельным
  TIL в `sofa-proposals.md` (P1) — не дублирую здесь.
- **`ErrorCard` рисовала сырой `<button>`** (review-b nit) — заменена на `Button variant="link"`
  (`StateScreen.tsx:184-192`), фокус-кольцо и `disabled`-семантика приезжают из примитива. Landed.
- **Кнопка сабмита auth-экрана теряла доступное имя во время отправки** (review-a nit, «…» вместо
  подписи) — заменено на глагольные формы `SUBMITTING_LABELS` («Входим…»/«Создаём аккаунт…»),
  `LoginScreenView.tsx:80,203`. Landed.
- **Неразрывный пробел в подписи Яндекса** (review-b nit) — стоит: побайтовая проверка подтверждает
  U+00A0 между «Яндекс» и «ID» (`ProviderButton.tsx:26`). Landed.
- **`confirmedName` в `ProjectSettingsPage` мог залипнуть навсегда** (review-a nit) — переписан на
  сравнение со `staleName` вместо совпадения с записанным именем, комментарий в коде прямо ссылается на
  находку code review A. Landed.
- **`LoadingState` не объявляла себя ассистивным технологиям** (review-a suggestion) — `role="status"`
  уже стоит на корне (`StateScreen.tsx:138-152`). Landed (в отличие от `ErrorCard` — та осталась
  непокрытой, см. кандидат в backlog выше, это не тот же дефект).
- **Наблюдение H2 брифа** (транзиентная сетевая ошибка `getMe` на холодном старте роняет его в error без
  пути восстановления) — design-brief сам явно относит это к «второму рангу», решение уже принято самим
  брифом: «вне scope, если репро не покажет обратного» (`design-brief.md:166`). Репро в этой итерации не
  проводилось, новых оснований для пересмотра решения нет — не завожу отдельным кандидатом поверх уже
  зафиксированного в брифе.
- **Мутационная приёмка тестов / каталог форм ложной зелени** — переведено в SOFA write-back W3
  (`sofa-proposals.md`), не дублирую здесь по прямому указанию роли («не дублируй SOFA-кандидатов»).
- **Blueprint по единому шаблону состояний `StateScreen`/`LoadingState`/`ErrorCard`/`Skeleton`** — SOFA-роль
  явно отклонила публикацию («стандартная практика дизайн-систем, дешевле находимая в доках», наш угол
  инвентаризации переносимого знания не добавляет); harvest этот же материал не переоткрывает вторым
  каналом.
- **`data-slot` примитива как ступень лестницы запросов в тестах** — уже кандидат в текущем anytime-реестре
  выше (testing.md § Frontend), не дублирую.
- **Дубль скелетона SIEM-таблицы в трёх файлах** — уже кандидат в текущем anytime-реестре выше (backlog,
  tech debt), не дублирую.
- **Живая сверка `{T5.1}`–`{T5.4}` на реальном стенде (Network-сигнатура фикса интерцептора)** — решением
  оркестратора перенесена на финальный прогон INTEGRATION_TEST; backend не поднимался осознанно (порт
  занят docker-стеком чужой итерации). Это штатный `👤 deferred`-хвост, не процессный долг — architect
  прогоняет его сам на живом стенде перед мержем, отдельного harvest-кандидата не требует.
