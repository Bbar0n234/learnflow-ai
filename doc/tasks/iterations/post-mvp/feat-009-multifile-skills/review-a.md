# Code Review Report — режим A (качество кода)

## Summary

- **blocker:** 0
- **nit:** 3
- **pre-existing:** 1

Diff корректен по существу. Path traversal закрыт двухслойно и действительно работает (первый слой — allowlist по сегментам + отсев `..`/absolute; второй — `resolve()` + `is_relative_to`, ловит симлинк-эскейп). Регрессия однофайловых скиллов исключена и покрыта тестом. Ошибки не глотаются (`UnicodeDecodeError` перехвачен точечно, `OSError`/`PermissionError` не маскируются), текст ошибок не утекает внутренностями (нет абсолютных путей, `is_relative_to`-отказ отдаёт обобщённое сообщение). Тесты падают при реальной поломке и имеют позитивные якоря против вакуумного прохода. Ниже — три нита (в основном по тестовому покрытию и краевым рассинхронам автосписка) и один pre-existing по блокирующему I/O.

## Замечания

| Severity | Намерение | Файл:строка | Замечание со свидетельством | Предложение |
|----------|-----------|-------------|------------------------------|-------------|
| nit | suggestion | `backend/tests/personalization/test_skills.py:229-246` | Второй слой защиты (`resolve()` + `is_relative_to`, `skills.py:64-66`) тестами не задействован. Все четыре `bad_file` (`../neighbor/SKILL.md`, `/etc/passwd`, `sub/../../neighbor/SKILL.md`, `""`) отклоняются уже на первом слое `_is_safe_relative_path` (`..` и ведущий `/` режутся до `resolve()`). Единственный путь, достигающий второго слоя, — симлинк внутри скилла, ведущий наружу: имя-сегмент проходит allowlist, а `resolve()` уводит за пределы `skill_dir`. Именно эта ветка (ради которой введён второй слой) непокрыта. | Добавить кейс: создать в `multi-skill` симлинк на `neighbor/SKILL.md` (или на `tmp_path`-файл вне дерева), вызвать `load_skill(..., file="<symlink>")`, проверить `Error:` и что `NEIGHBOR-SECRET` не утёк. Закрывает ветку `skills.py:65-66`. |
| nit | question | `backend/app/agent/tools/skills.py:110-118` vs `84-98` | Автосписок (`_list_skill_files`) и валидатор загрузки (`_is_safe_relative_path`) расходятся в критериях: rglob+`is_file()` следует по симлинкам и не фильтрует не-ASCII имена, поэтому в футер может попасть файл, который затем нельзя загрузить — симлинк-наружу отклонит второй слой, не-ASCII имя отклонит ASCII-only паттерн `_SAFE_PATH_SEGMENT_RE` первым слоем. Агент увидит в футере путь и получит `Error: invalid file path` на попытке его открыть. Влияние низкое (скиллы — доверенный контент, модули ASCII-именованы), но футер как контракт progressive disclosure может рекламировать незагружаемое. Осознанно ли? | Либо задокументировать как приемлемое (доверенный контент), либо согласовать критерии: в `_list_skill_files` пропускать симлинки (`path.is_symlink()`) и/или имена, не проходящие `_is_safe_relative_path`, — тогда футер листит ровно то, что грузится. Не blocker: на реальных скиллах проекта не наблюдаемо. |
| nit | question | `backend/app/agent/tools/skills.py:68` | Бинарник корректно листится в футере и даёт внятную ошибку при загрузке (покрыто тестами), но краевые не-ASCII/симлинк-имена (см. выше) при загрузке отдают `Error: invalid file path '<file>'` / `Error: invalid file path` — без подсказки «доступные файлы», в отличие от ветки «не найден» (`68-74`). Для LLM это менее наводящая ошибка, чем «not found → список». Мелочь диагностики. | По желанию: в отказах первого/второго слоя тоже приложить `Available files: <list>`, как в ветке not-found, для единообразия наводящих ошибок. |
| pre-existing | question | `backend/app/agent/tools/skills.py:18,49,64,77,110` | `load_skill` — `async def`, но весь I/O синхронный и блокирует event loop: `read_text` (было и до итерации), а диф добавляет обход директории `rglob("*")` + `is_file()`/`resolve()` на каждый вызов без `file`. При конкурентных вызовах в async-графе это блокировки, а не гонки (общего мутируемого состояния нет — функции чистые над ФС). Директории скиллов малы, эффект пренебрежимо мал; паттерн унаследован (исходный `read_text` уже был sync). | Если когда-либо появятся крупные скиллы — вынести в `asyncio.to_thread`. Сейчас — не действие, фиксирую для полноты (пункт чек-листа A о блокирующем I/O в async-пути). |

## Прочее (проверено, замечаний нет)

- **Обработка ошибок / ресурсы:** `except UnicodeDecodeError` узкий — `OSError`/`PermissionError` не проглатываются (обосновано в T1 summary). `read_text` закрывает дескриптор, утечек нет.
- **Утечка содержимого/внутренностей:** отказ `is_relative_to` (`skills.py:66`) отдаёт обобщённое `invalid file path` без резолвнутого пути; в футер/ошибки не попадают абсолютные пути (`as_posix()` относительный). Тест traversal явно проверяет отсутствие `NEIGHBOR-SECRET`.
- **Регрессия однофайловых скиллов:** ветка `if not skill_files: return content` (`51-52`) + тест `test_load_skill_single_file_skill_omits_footer` — поведение без футера сохранено.
- **Швы:** `security/corpus.py:55-58` осознанно исключает контент скиллов из fragment-корпуса; контент модулей — тот же класс доверенного tool-результата, новых FP-рисков диф не вносит.
- **Сложность/дизайн:** двухслойная защита оправдана (не over-engineering: слой два — единственная защита от симлинков), хелперы module-level и без состояния, комментарии объясняют «почему».
- **Контент скилла `skills/tech-article-writing/`:** технических проблем (битые кросс-ссылки, остаточные `author-voice/`/абсолютные пути) не выявлено — согласуется с самопроверкой T2.2 (grep пуст).
