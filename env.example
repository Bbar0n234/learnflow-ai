# Docker environment configuration
# This file is for Docker Compose deployment
# Copy this file to .env and fill in your API keys

# API Keys
OPENAI_API_KEY=your_openai_api_key_here

# Alternative LLM Providers (Optional)
# OpenRouter - Multi-provider aggregator
# OPENROUTER_API_KEY=your_openrouter_api_key

# Fireworks AI - Fast inference provider
# FIREWORKS_API_KEY=your_fireworks_api_key

# LangFuse Configuration (Docker internal network)
LANGFUSE_PUBLIC_KEY=your_langfuse_public_key
LANGFUSE_SECRET_KEY=your_langfuse_secret_key
LANGFUSE_HOST=http://langfuse-web:3000

# Project Configuration
MAIN_DIR=./data
PROMPTS_CONFIG_PATH=./configs/prompts.yaml
GRAPH_CONFIG_PATH=./configs/graph.yaml

# LearnFlow FastAPI Service (Docker internal)
LEARNFLOW_HOST=graph
LEARNFLOW_PORT=8000

# Logging
LOG_LEVEL=INFO

# Security Settings
SECURITY_ENABLED=true
SECURITY_FUZZY_THRESHOLD=0.85
SECURITY_MIN_CONTENT_LENGTH=10

# Database Settings (Docker internal network)
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/learnflow?sslmode=disable

# Telegram Bot
TELEGRAM_TOKEN=your_telegram_bot_token_here

# Web UI Configuration (for docker-compose build args)
WEB_UI_BASE_URL=http://localhost:3001
VITE_ARTIFACTS_API_URL=/api
VITE_LEARNFLOW_API_URL=http://localhost:8000
VITE_POLLING_INTERVAL=5000
VITE_MAX_RETRIES=3

# Prompt Configuration Service (Docker internal)
PROMPT_CONFIG_DATABASE_URL=postgresql+asyncpg://postgres:postgres@postgres:5432/prompts_db
PROMPT_CONFIG_SERVICE_HOST=0.0.0.0
PROMPT_CONFIG_SERVICE_PORT=8002
PROMPT_CONFIG_LOG_LEVEL=INFO
PROMPT_CONFIG_INITIAL_DATA_PATH=/app/prompt-config-service/initial_data.yaml
PROMPT_CONFIG_CACHE_TTL=300

# Service URLs for Bot (Docker Compose networking)
PROMPT_SERVICE_URL=http://prompt-config-service:8002
ARTIFACTS_SERVICE_URL=http://artifacts-service:8001

# Authentication Settings
BOT_API_KEY=your_secure_bot_api_key_here
ARTIFACTS_JWT_SECRET_KEY=your_secure_jwt_secret_key_here
ARTIFACTS_BOT_API_KEY=your_secure_bot_api_key_here

# Optional: GitHub Integration
# GITHUB_TOKEN=your_github_token
# GITHUB_REPOSITORY=username/repository

# Optional: Local Artifacts Storage
ARTIFACTS_BASE_PATH=data/artifacts
ARTIFACTS_ENSURE_PERMISSIONS=true
ARTIFACTS_MAX_FILE_SIZE=10485760