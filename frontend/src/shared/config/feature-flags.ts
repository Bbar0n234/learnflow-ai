/**
 * Фиче-флаги фронтенда.
 *
 * `SHOW_GROUP_B_STUBS` — незрелые фичи группы B (feat-004): версионирование
 * Сферы, SSE-peek карточка записи в Сферу, studio-панель и линза, просмотрщики
 * slides/audio/image. Реализованы на фронтенд-заглушках (mock-данные) — бэкенд-
 * контракта под них пока нет (см. doc/backlog.md, секция Backend).
 *
 * Привязан к `import.meta.env.DEV`: в dev-сборке (`make dev-fe`) фичи видны для
 * локального догфудинга; в любой собранной/задеплоенной сборке (`vite build`,
 * MODE=production) флаг статически равен `false`, ветки не рендерятся, а
 * мок-данные выпадают из бандла через dead-code elimination. Прод-пользователь
 * фейковых данных не видит.
 *
 * Снимать по мере появления реального бэкенда: когда у фичи группы B есть
 * контракт, её код раз-гейтится индивидуально в месте рендера.
 */
export const SHOW_GROUP_B_STUBS = import.meta.env.DEV;
